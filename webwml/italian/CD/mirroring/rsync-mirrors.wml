#use wml::debian::cdimage title="Elenco dei mirror rsync per immagini dei CD Debian" BARETITLE=true
#use wml::debian::translation-check translation="1.4" maintainer="Ireneo Funes"

<p>Questi sono gli indirizzi dei mirror <a
href="http://rsync.samba.org/">rsync</a> delle immagini dei CD
Debian. Alcuni dei mirror potrebbero non essere completamente
aggiornati. Questa lista contiene solo siti con immagini Debian
<em>ufficiali</em>.</p>

<p><strong>N.B.</strong> - Questi siti necessariamente <em>non</em>
offrono le immagini via FTP o HTTP - &egrave; inutile provare! Invece
utilizzano il protocollo rsync, molto pi&ugrave; efficiente per i nostri
scopi. Se si sta cercando i link per il download HTTP/FTP, si consulti l'<a
href="../http-ftp/">elenco dei mirror HTTP/FTP</a>.</p>

<p>Se si ottiene "Access Denied" da molti siti, probabilmente ci si trova
dietro un firewall che blocca la porta rsync (873). Si chieda al proprio
amministratore di sistema di riconfigurare il firewall.</p>

#use wml::debian::countries
#include "$(ENGLISHDIR)/CD/mirroring/cdimage_mirrors.list"
