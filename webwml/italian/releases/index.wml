#use wml::debian::template title="Debian Releases"
<!--translation 1.7-->

      <h2>Releases ordinate per il loro stato</h2>

<p>

Le seguenti versioni sono presentate in base al loro stato.
Questi collegamenti saranno sempre disponibili, poiché Debian ha
sempre due versioni che sono mantenute in maniera attiva: ``stable'' (stabile)
e ``unstable'' (non stabile).

<dl>
	<dt><a href="stable/">stable</a>
	<dd>
La distribuzine ``stable'' contiene la versione più recente ufficialmente
rilasciata di Debian. È quella che deve essere usata per gli ambienti di
produzione Debian. La attuale distribuzione ``stable'' è <em>slink</em>.
</dd>
	<dt><a href="unstable/">unstable</a>
	<dd>
La distribuzione ``unstable'' è quella in cui si sta portando avanti lo
sviluppo di Debian. Di norma, questa distribuzione è usata dagli sviluppatori
che amano avere sempre tutto aggiornato. La attuale versione ``unstable''
è chiamata <em>potato</em>.

<: if ( -d 'frozen' ) { _:>
	<dt><a href="frozen/">frozen (bloccata)</a>
	<dd>
La distribuzione ``frozen'' è la candidata a divenire la prossima
distribuzione ``stable''. In questo momento c'è una distribuzione ``frozen'';
normalmente ci sono solo la ``stable'' e la ``unstable''.
La attuale versione ``frozen'' è <em>
<: print readlink('frozen'); # FYI, this doesn't work, I had it above too :>
</em>
<: } :>

      </dl>

<h2>Releases ordinate per nome</h2>
<p>

La seguente lista è solo un altro modo di identificare le distribuzioni
descritte sopra.
<ul>
<: if ( -d 'hamm' ) { _:>
	<li><a href="hamm/">Debian 2.0 (hamm)</a>
<: } :>
<: if ( -d 'slink' ) { _:>
	<li><a href="slink/">Debian 2.1 (slink)</a>
<: } :>
<: if ( -d 'potato' ) { _:>
	<li><a href="potato/">Debian 2.2 (potato)</a>
<: } :>
</ul>

<H2>Precedenti versioni di Debian</H2>
<P>Debian 1.3 (chiamata Bo) e 2.0 (chiamata Hamm) possono esssere trovate presso
<A href="ftp://ftp.debian.org/debian-archive/">ftp://ftp.debian.org/debian-archive/</A>,
<A href="ftp://debian.midco.net/debian-archive/">ftp://debian.midco.net/debian-archive/</A>,
<A href="ftp://ftp.infodrom.north.de/pub/debian/dists/">ftp://ftp.infodrom.north.de/pub/debian/dists/</A>
o <A href="http://archive.debian.org/debian-archive/">http://archive.debian.org/debian-archive/</A>.

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:".doctype"
End:
-->
