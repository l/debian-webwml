#use wml::debian::template title="Debian ``woody'' Release Information" BARETITLE="true"
<!--translation 1.3-->

<p>Il nome in codice per la prossima maggiore versione Debian dopo <a
href="../potato/">potato</a> è ``woody''. È possibile che questa versione
sia numerata ``2.3'' o ``3.0''.

<p>Questa versione è attualmente considerata ``unstable'' (instabile.) Vale a
dire che alcune cose <em>certamente</em> non funzioneranno.
Chiunque utilizzi ``unstable'' dovrebbe informarsi sulle relative
problematiche seguendo le liste di messaggi per gli sviluppatori.

<p>come sempre, <strong>nessuno</strong> scopo specifico &egrave; stato deciso
durante questa fase iniziale dello sviluppo.

<p>Abbiamo per&ograve; deciso di riscrivere completamente boot-floppies,
sostituendolo con una nuova procedura d'installazione. Altri aggiornamenti
importanti saranno: Linux kernel 2.4, GLibC 2.2, GCC 3.0, XFree86 4.0 e Perl
5.6. Probabilmente il pacchetto <code>debconf</code> diventer&agrave;
obbligatorio per tutti i pacchetti che necessiteranno di una interazione con
l'utente durante l'installazione.
