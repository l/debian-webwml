#use wml::debian::template title="Debian ``potato'' Release Information"
<!--translation 1.3-->

<p>

Il nome in codice delle prossima versione di Debian sarà ``potato'',
che la Debian 2.2.

<p>

Questa versione è attualmente frozen (bloccata). Vale a dire
che alcune parti del sistema sono ancora in fase di test prima della
distribuizione come prissima versione stabile.

<p>

Debian ha un regolamento che non permette di rivelare in anticipo
la date del rilascio delle prossime versioni; attualmente questa
data è in effetti sconosciuta.


<h2>Principali cambiamenti in ``potato''</h2>

I principali cambiamenti che faranno parte di potato sono:

<ul>

  <li> sarà basata sul kernel Linux 2.2 (per le architetture basate su Linux).

  <li> si fonderà sulla libreria GNU libc 2.1 (con compatibilità binaria verso la GNU libc 2.0)

  <li> La versione <a href="http://egcs.cygnus.com/">egcs</a> del
compilatore C <tt>gcc</tt>, altrimenti nota come <tt>egcc</tt>.

</ul>

Ci sono anche forti possiblità che la potato includa il supporto per
le architetture powerpc e arm.

<p>

Ci saranno molti altri cambiamenti in questa distribuzione, ma attualmente non
sono tutti conosciuti.

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:"../.doctype"
End:
-->
