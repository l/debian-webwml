#use wml::debian::template title="Debian GNU/Linux 2.2 (`potato') Release Information" BARETITLE=yes
#use wml::debian::release
#include "$(ENGLISHDIR)/releases/potato/release.data"

<!--translation 1.11-->

<define-tag about_lang_setting>
<p>Se hai attivato correttamente la localizzazione del tuo browser, puoi usare
i link sottostanti direttamente per avere i documenti nella tua lingua -- vedi
<a href="$(HOME)/intro/cn"><setlang></a>. Altrimenti, scegli la corretta
architettura, la lingua e il formato che preferisci dalle tabelle.
</define-tag>


<ul>
       <li><a href="#general">Informazioni generali</a>
       <li><a href="#release-notes">Release Notes</a>
       <li><a href="#new-inst">Nuove installazioni</a>
       <li><a href="#errata">Errata</a>
</ul>

<h2><a name="general">Informazionii generali</a></h2>

<p>Il nome in della prossima distribuzione Debian sarà ``potato'', che è
Debian GNU/Linux 2.2.

<p>Questa versione è attualmente frozen (bloccata). Vale a dire
che alcune parti del sistema sono ancora in fase di test prima della
distribuizione come prissima versione stabile.

<p>Debian ha un regolamento che non permette di rivelare in anticipo
la date del rilascio delle prossime versioni; attualmente questa
data è in effetti sconosciuta.

<p>Le seguenti architetture saranno supportate in potato:

<ul>
<: foreach $arch (keys %arches) {
      print "<li> " . $arches{$arch} . "\n";
   } :>
</ul>

<p>Debian GNU/Linux 2.2 sarà disponibile in internet o tramite venditori di
CD, vedi <a href="../../distrib/">questa pagina</a> per maggiorin informazioni
su come ottenere Debian.

<h2><a name="release-notes">Release Notes</a></h2>

<p>Per trovare cosa è nuovo nella Debian 2.2, scegli le ``Release Notes'' per
la tua architettura.

<ul>
<:= &permute_as_list('release-notes/', 'Release Notes'); :>
</ul>

<p>Le Release Notes per architetture che erano già state supportate contengono
anche tutte le informazioni necessarie per chi aggiorna il proprio sistema da
versioni precedenti.

<about_lang_setting>
 
<center>
<table border=2 cellspacing=0 cellpadding=3 bgcolor="#DDDDDD"> 
<tr bgcolor="#44CCCC">
  <th align="left"><strong>Architettura</strong></th>
  <th align="left"><strong>Formato</strong></th>
  <th align="left"><strong>Lingua</strong></th>
</tr>
<: &permute_as_matrix('release-notes', @langsrelnotes); :>
</table>
</center>

<p>Per l'architettura i386 è disponibile un <a
href="http://master.debian.org/~lapeyre/reports/">manuale dettagliato</a>
che descrive i pacchetti che sono cambiati dalle ultime due release.

<h2><a name="new-inst">Nuove installazioni</a></h2>

<p>Le istruzioni per l'installazione e i file da scaricare sono divisi per
architetura:

<ul>
<:= &permute_as_list('install', 'Installation Manual'); :>
</ul>

<about_lang_setting>

<center>
<table border=2 cellspacing=0 cellpadding=3 bgcolor="#DDDDDD"> 
<tr bgcolor="#44CCCC">
  <th align="left"><strong>Architettura</strong></th>
  <th align="left"><strong>Formato</strong></th>
  <th align="left"><strong>Lingua</strong></th>
</tr>
<: &permute_as_matrix('install', @langsinstall); :>
</table>
</center>

<p>Queste pagine web verranno aggiornate con le nuove versioni nel manuale di
installazione per potato non appena saranno disponili.

<P>Uno sforzo particolare è stato fatto per redarre il Manuale di
Installazione Debian, ciononostante alcune parti di esso richiedono un lavoro
ulteriore. Questo è particolarmente vero per i dettagli dell'architettura 
<:= $arches{'arm'} :>, e nell'aggiornare le traduzioni. Se vuoi contribuire
contatta la lista di messaggi <a href="mailto:debian-boot@lists.debian.org">debian-boot@lists.debian.org</a>.
Puoi controllare i sorgenti dei manuali
<a href="http://cvs.debian.org/boot-floppies/">via web</a> o scaricartele
tramite <a
href="http://cvs.debian.org/~checkout~/boot-floppies/README-CVS?tag=HEAD&content-type=text/plain">
CVS</a>.


<h2><a name="errata">Errata</a></h2>

<p>A volte, in caso di problemi particolarmente critici o legati alla
sicurezza, la versione distribuita è aggiornata, In genere questi cambiamenti
sono indicati come 'point release' cioè versioni intermedie.

<p>Poiché potato non è stata ancora distribuita, non ci possono essere
aggiornamenti :-)

#<p>There is no point release for potato at this point. You can find the
#<a href="http://ftp.debian.org/debian/dists/potato/ChangeLog">ChangeLog</a>
#on any Debian archive mirror.
#
#<p>Fixes to the released stable distribution often go through an extended
#testing period before they are accepted into the archive.  However, these
#fixes are available in the
#<a href="ftp://ftp.debian.org/debian/dists/potato-proposed-updates/">
#dists/potato-proposed-updates</a> directory of any Debian archive
#mirror.
#
#If you use <tt>apt</tt> to update your packages, you can install the
#proposed updates by adding the following line to
#<tt>/etc/apt/sources.list</tt>:
#
#<pre>
#  deb http://http.us.debian.org/debian dists/proposed-updates/
#</pre>
#
#After that run <kbd>apt-get update</kbd> followed by <kbd>apt-get
#upgrade</kbd>.


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:"../.doctype"
End:
-->
