#use wml::debian::template title="Debian BTS - pseudo-pacchetti" NOHEADER="true" NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.11"

<H1>Pseudo-pacchetti del Sistema Debian di Tracciamento dei Bug</H1>

<p>Questa pagina elenca i pseudo-pacchetti disponibili per l'impiego
nella riga <code>Package:</code> nei rapporti su bug.

<p>Vedi <a href="Reporting">le istruzioni per riportare un bug</a> per
i dettagli su come specificare una riga <code>Package:</code>.

<hrline>

<:
my $file = "$(ENGLISHDIR)/Bugs/pseudo-packages.description";
open I, "$file" or die "Can't open $file file: $!\n";
while (<I>) { if (/^(\S+)\s+(.*)$/) { my ($foo) = $2; $pseudos{$1} = $foo =~ s/&/&amp;/g; } }
close I;
%pseudos || die "%pseudos is not set -- empty $file file?\n";
foreach $a (sort keys %pseudos) {
  if ($a ne "spam" && $a ne "slink-cd") {
    print "<p><code><a href=\"http://bugs.debian.org/$a\">$a</a></code> &mdash; $pseudos{$a}\n";
  }
}
:>

<hrline>

#use "otherpages.inc"

#use "$(ENGLISHDIR)/Bugs/footer.inc"
