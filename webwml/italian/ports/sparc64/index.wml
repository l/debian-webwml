#use wml::debian::template title="Port su SPARC-64" NOHEADER="yes"
#include "$(ENGLISHDIR)/ports/sparc64/menu.inc"
#use wml::debian::translation-check translation="1.12" maintainer="Luca Monducci"

<h1>Debian per SPARC-64</h1>

<p>&Egrave; possibile eseguire Debian sull'architettura Sun4u (SPARC 64)
utilizzando la distribuzione <a href="../../releases/stable/">stable</a>
rilasciata; è stata realizzata semplicemente usando il port per
l'architettura SPARC con l'aggiunta di un kernel a 64-bit per Sun4u. Il
kernel in esecuzione è a 64-bit mentre tutte le applicazioni sono eseguite
a 32-bit (senza che si noti la differenza). La maggior parte della
distribuzione Debian SPARC è stata compilata su macchine Sun4u.</p>

<p>Per maggiori informazioni, per i problemi e per lo stato vedere le
pagine del port <a href="../sparc/">Debian SPARC</a>.</p>
