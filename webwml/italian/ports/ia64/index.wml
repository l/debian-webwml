#use wml::debian::template title="Port su IA-64" NOHEADER="yes"
#use wml::debian::translation-check translation="1.28" maintainer="Luca Monducci"
#include "$(ENGLISHDIR)/ports/ia64/menu.inc"

<h1>Debian per IA-64</h1>


<h2>Stato</h2>

<p>Adesso IA-64 è una architettura supportata da Debian!</p>

<p>Chiunque voglia contribuire si deve iscrivere alla lista di messaggi
<a href="#mailinglist">debian-ia64</a>.</p>

<p>Presso i normali canali di acquisizione dei supporti di installazione
Debian sono disponibili le immagini dei CD per IA-64. Per alcuni sistemi
HP Itanium 2 è necessario avere un kernel recente per far funzionare
correttamente l'interfaccia Gigabit Ethernet integrata, in particolare
se il sistema è un zx2000 probabilmente si dovrà usare l'immagine del CD
per l'installazione via rete.</p>

<p>Una immagine ISO per creare un CDROM contenete gli strumenti per
installare Debian su IA-64 è disponibile via HTTP da
<url "http://gluck.debian.org/ia64/ia64-net-20030304.iso"> e via FTP
da <url "ftp://gluck.debian.org/ia64/ia64-net-20030304.iso">.</p>

<p>Un'altra opzione, specifica per i sistemi HP Integrity, è usare
l'installatore Debian come parte dell'&ldquo;HP Enablement Kit for
Linux&rdquo;
<a href="http://www.software.hp.com/portal/swdepot/displayProductInfo.do?productNumber=T1502AA">\
scaricabile liberamente</a> dal sito web di HP.</p>

<p>L'installatore supporta il partizionamento sia GPT (consigliato)
che FAT. Se si sceglie di usare il partizionamento FAT assicurarsi di
impostare il tipo di partizione a &ldquo;ef&rdquo; per la partizione
EFI come indicato nella schermata di aiuto che appare prima dell'inizio
del partizionamento. Usare il partizionamento GPT nelle installazioni
su tutti i sistemi nuovi!</p>

<h2>Versioni del BIOS</h2>

<p>Dato che i prodotti IA-64 e i sistemi operativi sono ancora in evoluzione
è possibile trovare delle macchine con versioni del BIOS che non funzionano
con Linux. Una curiosa combinazione di cui abbiamo sentito parlare è il
tentativo di usare un nuovo kernel su sistemi "Lion" con vecchie versioni
del BIOS. Per facilitare la scelta segue una lista delle versioni del BIOS
note per ogni tipo di sistema su cui il port Debian è stato testato.</p>

<ul>
    <li>
	Nei sistemi HP Itanium 2 (rx2600, rx5670, zx2000, zx6000), tutte le
	versioni del firmware vendute ai clienti dovrebbero funzionare.</li>

    <li>
	Lion, noto come HP rx4610, la versione 99b funziona.</li>

    <li>
	BigSur, noto come HP i2000, la versione 130 funziona.</li>
</ul>

<p>&Egrave; possibile scaricare il firmware per i sistemi
<a href="http://www.hp.com">HP</a>, partendo da
<a href="http://welcome.hp.com/country/us/eng/software_drivers.htm">questa
pagina</a>.</p>

<p>Chiunque abbia delle informazioni sulle versioni di BIOS di altri
sistemi IA-64 su cui Debian funziona regolarmente, lo comunichi sulla
lista debian-ia64!</p>

<h2>Contatti</h2>

<p>Gli istigatori del port su IA-64 sono Bdale Garbee e Randolph Chung. Il
miglior modo per porre delle domande è farlo tramite la lista di messaggi.</p>

<h2><a name="mailinglist">Lista di messaggi</a></h2>

<p>Per iscriversi alla lista di questo port occorre inviare un messaggio
contenente la parola &ldquo;subscribe&rdquo; nell'oggetto a
<email "debian-ia64-request@lists.debian.org"> oppure è possibile usare la
<a href="http://lists.debian.org/debian-ia64/">pagina web della lista</a>.
Inoltre è possibile navigare e fare delle ricerche
nell'<a href="http://lists.debian.org/debian-ia64/">archivio della
lista</a>.</p>
