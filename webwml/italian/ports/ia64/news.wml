#use wml::debian::template title="Port su IA-64 -- Ultime notizie" NOHEADER="yes"
#include "$(ENGLISHDIR)/ports/ia64/menu.inc"
#use wml::debian::translation-check translation="1.9" maintainer="Luca Monducci"

<h1>Ultime notizie su Debian per IA-64</h1>

<h3><:=spokendate ("04/03/2003"):></h3>

<p><strong>Aggiornamento dell'immagine del CD con l'installatore</strong></p>

<p>&Egrave; disponibile una nuova immagine del CDROM per l'installazione.
Questa verisone usa il kernel 2.4.19 020821.1, questo kernel contiene
tutti i driver di memorizzazione e di rete necessari all'installazione su
tutti i sistemi ia64 sui cui ha accesso Bdale.
<a href="http://gluck.debian.org/ia64/ia64-net-20030304.iso">HTTP</a>.</p>

<h3><:=spokendate ("06/05/2002"):></h3>

<p><strong>Aggiornamento dell'immagine del CD con l'installatore</strong></p>

<p>L'immagine ISO per creare un CDROM contenente l'insieme di strumenti
per l'installazione di Debian su IA-64 è stato aggiornata con le versioni
del kernel e di "boot floppies" che probabilmente saranno incluse nel
rilascio di Debian 3.0 (woody).
<a href="http://gluck.debian.org/ia64/ia64-net-20020506.iso">HTTP</a>.</p>

<h3><:=spokendate ("28/01/2002"):></h3>

<p><strong>Aggiornamento dell'immagine del CD con l'installatore</strong></p>

<p>L'immagine ISO per creare un CDROM contenente l'insieme di strumenti
per l'installazione di Debian su IA-64 è stato aggiornata nei primi giorni
di Dicembre e ha dimostrato di funzionare correttamente almeno sui sistemi
su cui abbiamo accesso.
<a href="http://gluck.debian.org/ia64/ia64-base-20011202-rc1.iso.gz">\
HTTP</a>.</p>

<h3><:=spokendate ("10/08/2001"):></h3>

<p><strong>Pubblicata l'immagine di un CD con l'installatore</strong></p>

<p>Una immagine ISO per creare un CDROM contenente l'insieme di strumenti
per l'installazione di Debian su IA-64 è disponibile via 
<a href="http://gluck.debian.org/ia64/cd-20010810.iso.gz">HTTP</a>.</p>

<h3><:=spokendate ("06/08/2001"):></h3>

<p><strong>Debian accetta ia64 per il rilascio con Debian 3.0
(woody)!</strong></p>

<p>Con immenso piacere annunciamo l'accettazione di ia64 come una delle
architetture del prossimo rilascio stabile di Debian 3.0, nome in codice
woody. Gli strumenti per l'installazione su ia64 sono nel ramo woody e i
pacchetti adesso in unstable stanno iniziando a essere promossi nel ramo
testing/woody.</p>

<h3><:=spokendate ("24/07/2001"):></h3>

<p><strong>Richiesta ufficiale di rilascio insieme a Debian 3.0
(woody)</strong></p>

<p>Circa il 70% dei pacchetti Debian sono stati compilati e aggiornati
negli archivi, il sistema di compilazione automatica procede senza
intoppi, gli strumenti per l'installazione sono disponibili nell'archivio
e il numero di sistemi funzionanti sta crescendo rapidamente.</p>

<h3><:=spokendate ("16/01/2001"):></h3>

<p><strong>Primo avvio nativo</strong></p>

<p>Debian si è avviata in modo nativo su IA-64 per la prima volta, dopo
lo sviluppo in un ambiente chroot sopra un sistema TurboLinux con una
installazione di base.</p>
