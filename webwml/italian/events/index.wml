#use wml::debian::basic title="Upcoming Attractions"
#use wml::debian::languages
<!--translation 1.1-->

#words and phrases to translate
<define-tag intro>
Questa pagine contiene una lista di avvenimenti.
Inoltre indica il tipo di partecipazione che Debian ha nell'evento (se è
presente).
Se vuoi aiutare alla partecipazione ad un evento, manda un messaggio
al coordinatore dell'evento stesso direttamente. Se non ci sono 
corrdinatori elencati e sei interessato a coordinare l'evento o se vuoi
farci sapere di altri eventi allora manda un messaggio a
&lt;<A HREF="mailto:events@debian.org">events@debian.org</A>&gt;


<p>
Questa pagina è ancora in fase di costruzione, quindi tutti i commenti e 
suggerimeti sono benvenuti!
</define-tag>
<define-tag event_word>Eventi</define-tag>
<define-tag past_words>eventi passati</define-tag>
<define-tag none_word>nessuno</define-tag>

#include "../../english/events/index.include"

<p>
<intro>

#
# This has been redone.  See index.wml.README for details.  Email me if
# you have any difficulties.
#
# - blackie@sfcc.net

<:= get_future_event_list('$(ENGLISHDIR)/events','','','futurefirst') :>

#<:= get_event_list ('1999', '0', '$(ENGLISHDIR)/events' ) :>

#<:= get_event_list ('2000', '0', '$(ENGLISHDIR)/events' ) :>

#<:= get_event_list ('1998', '0', '$(ENGLISHDIR)/events' ) :>

<:= get_past_event_list('$(ENGLISHDIR)/events','','','futurefirst') :>

<:= languages() :>
#use wml::debian::footer
