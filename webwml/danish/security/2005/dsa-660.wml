<define-tag description>manglende kontrol af returværdi</define-tag>
<define-tag moreinfo>
<p>Raphaël Enrici har opdaget at KDE's screensaver kan gå ned under visse lokale
omstændigheder.  Dette kunne udnyttes af en angriber med fysisk adgang til 
maskinen, til at få kontrol over desktop-sessionen.</p>

<p>I den stabile distribution (woody) er dette problem rettet i
version 2.2.2-14.9.</p>

<p>Dette problem er rettet upstream i KDE 3.0.5 og er derfor allerede rettet i
distributionerne unstable (sid) og testing (sarge).</p>

<p>Vi anbefaler at du opgraderer din kscreensaver-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-660.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
