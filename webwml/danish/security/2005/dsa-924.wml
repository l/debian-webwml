<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Kurt Fitzner har opdaget et bufferoverløb i nbd, netværksblok-deviceklienten
og -serveren.  Bufferoverløbet kunne potentielt tillade vilkårlig kode på 
NBD-serveren.</p>

<p>I den gamle stabile distribution (woody) er dette problem rettet i
version 1.2cvs20020320-3.woody.3.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 2.7.3-3sarge1.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din nbd-server-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-924.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
