<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Alexander Gerasiov har opdaget at phpldapadmin, en webbaseret grænseflade
til administering af LDAP-servere, gav alle adgang til LDAP anonymt, også 
selvom dette var slået fra i opsætningen med kommandoen "disable_anon_bind".</p>

<p>Den gamle stabile distribution (woody) er ikke sårbar over for dette
problem.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 0.9.5-3sarge2.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 0.9.6c-5.</p>

<p>Vi anbefaler at du opgraderer din phpldapadmin-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-790.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
