<define-tag description>rettighedsforøgelse</define-tag>
<define-tag moreinfo>
<p>David Watson har opdaget en fejl i mount, som leveres af util-linux og andre
pakker så som loop-aes-utils, der tillader at lokale brugere omgår 
adgangsbegræsninger i filsystemet ved at gen-mounte det som read-only.</p>

<p>I den gamle stabile distribution (woody) er dette problem rettet i
version 2.11n-7woody1.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 2.12p-4sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 2.12p-8.</p>

<p>Vi anbefaler at du opgraderer din util-linux-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-823.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
