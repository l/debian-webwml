#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Eduard Bloch har opdaget en fejl i apt-cacher, et mellemlagringssystem til
Debian-pakker og kildekodefiler.  Fejlen kunne gøre det muligt for 
fjernangribere at udføre vilkårlige kommandoer på værtsmaskinen med 
mellemlageret som brugeren www-data.</p>

<p>Den gamle stabile distribution (woody) indeholder ikke denne pakke.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 0.9.4sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 0.9.10.</p>

<p>Vi anbefaler at du opgraderer din apt-cacher-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-772.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
