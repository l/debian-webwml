<define-tag description>race condition</define-tag>
<define-tag moreinfo>
<p>Imran Ghory har opdaget en "race condition" i bzip2, et bloksorterende 
filkomprimeringsprogram af høj kvalitet.  Ved udpakning af en fil til en mappe,
som en angriber havde adgang til, kunne bunzip2 narres til at opsætte 
filrettighederne på en anden fil, som brugeren havde rettigheder til.</p>

<p>I den stabile distribution (woody) er dette problem rettet i
version 1.0.2-1.woody2</p>

<p>I distributionen testing (sarge) er dette problem rettet i
version 1.0.2-6.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.0.2-6.</p>

<p>Vi anbefaler at du opgraderer dine bzip2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-730.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
