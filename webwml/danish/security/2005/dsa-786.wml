<define-tag description>formatstrengssårbarhed</define-tag>
<define-tag moreinfo>
<p>Ulf Härnhammar fra Debians sikkerhedsauditprojekt har opdaget en 
formatstrengssårbarhed i simpleproxy, en simpel TCP-proxy, som kan udnyttes via
svar fra fjerne HTTP-proxy'er.</p>

<p>Den gamle stabile distribution (woody) er ikke påvirket.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 3.2-3sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 3.2-4.</p>

<p>Vi anbefaler at du opgraderer din simpleproxy-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-786.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
