<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Frank Warmerdam har opdaget et stakbaseret bufferoverløb i libtiff, Tag 
Image File Format-biblioteket til behandling af TIFF-grafikfiler, som kunne 
gøre det muligt at udføre vilkårlig kode via misdannede TIFF-filer.</p>

<p>I den gamle stabile distribution (woody) er dette problem rettet i
version 3.5.5-7.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 3.7.2-3.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 3.7.2-3.</p>

<p>Vi anbefaler at du opgraderer dine libtiff-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-755.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
