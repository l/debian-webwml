<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>En efterforsker hos iDEFENSE har opdaget to problemer i funktioner til 
billedbehandling i PHP, et skriptsprog der afvikles på serveren og indeholder
HTML-kode.  Det ene problem var også i PHP3.  Ved læsning af et JPEG-billede,
kunne PHP snydes til at gå i en uendelig løkke pga. utilstrækkelig kontrol af
inddata.</p>

<p>I den stabile distribution (woody) er dette problem rettet i
version 3.0.18-23.1woody3.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 3.0.18-31.</p>

<p>Vi anbefaler at du opgraderer din php3-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2005/dsa-708.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
