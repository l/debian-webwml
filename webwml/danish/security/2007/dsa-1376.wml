<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>

<p>iKees Huijgen opdagede at under visse omstændigheder i KDM, en 
X-sessionmanager til KDE, kunne narres til at tillade brugerlogin
uden en adgangskode.</p>

<p>I den gamle stabile distribution (sarge) findes problemet ikke.</p>

<p>I den stabile distribution (etch), er dette problem rettet i version
4:3.5.5a.dfsg.1-6etch1.</p>

<p>Vi anbefaler at du opgraderer din kdebase-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1376.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
