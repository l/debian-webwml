<define-tag description>heltalsoverløb</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at et heltalsoverløb BGP-dissektoren i tcpdump, et ydedygtigt
værktøj til netværksovervågning og dataindsamling, kunne føre til udførelse af
vilkårlig kode.</p>

<p>I den gamle stabile distribution (sarge) er dette problem rettet i
version 3.8.3-5sarge3.</p>

<p>I den stabile distribution (etch) er dette problem rettet i
version 3.9.5-2etch1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 3.9.5-3.</p>

<p>Vi anbefaler at du opgraderer din tcpdump-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1353.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
