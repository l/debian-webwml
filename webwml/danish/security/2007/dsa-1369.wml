<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Sumit I. Siddharth opdagede at Gforge, et værktøj til udvikling i fællesskab,
udførte utilstrækkelig kontrol af inddata, hvilket tillod SQL-indsprøjtning.</p>

<p>I den gamle stabile distribution (sarge) er dette problem rettet i
version 3.1-31sarge2.</p>

<p>I den stabile distribution (etch) er dette problem rettet i
version 4.5.14-22etch1.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din gforge-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1369.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
