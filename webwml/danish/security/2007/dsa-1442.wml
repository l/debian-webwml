<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>
Rubert Buchholz opdagede at libsndfile, et bibliotek til læsning og skrivning 
af lydfiler, udførte utilstrækkelig grænsekontrol når FLAC-filer blev 
behandlet, hvilket kunne føre til udførelse af vilkårlig kode.
</p>

<p>
Den gamle stabile distribution (sarge) er ikke påvirket af dette problem.
</p>

<p>
I den stabile distribution (etch), er dette problem rettet i
version 1.0.16-2.
</p>

<p>
Vi anbefaler at du opgraderer dine libsndfile-pakker.
</p>

</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1442.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
