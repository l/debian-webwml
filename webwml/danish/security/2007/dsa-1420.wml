<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>
Bas van Schaik opdagede at agentd-processen i Zabbix, et system til
netværksovervågning, kunne køre brugerleverede kommandoer som gruppe-id'en 
root, og ikke zabbix, hvilket kunne føre til en rettighedsforøgelse.
</p>

<p>
zabbix er ikke indeholdt i den gamle stabile distribution (sarge).
</p>

<p>
I den stabile distribution (etch), er dette problem rettet i version
1:1.1.4-10etch1.
</p>

<p>
Vi anbefaler at du opgraderer dine zabbix-pakker.
</p>

</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1420.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
