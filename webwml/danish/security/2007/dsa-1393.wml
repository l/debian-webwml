<define-tag description>usikker udførelse</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at xfce-terminal, en terminalemulater til xfce-miljøet, ikke
på korrekt vis indkapslede parametre overført til processer startet gennem "Open 
Link".  Dette gjorde det muligt for ondsindede links at udføre vilkårlige 
kommandoer på det lokale system.</p>

<p>I den stabile distribution (etch), er dette problem rettet i version
0.2.5.6rc1-2etch1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version
0.2.6-3.</p>

<p>Vi anbefaler at du opgraderer din xfce4-terminal-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1393.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
