<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at Open Ticket Request System udførte utilstrækkelig 
fornuftighedskontrol på parameteret Subaction, hvilket gjorde det muligt at
indsprøjte vilkårlig webskriptkode.</p>

<p>Den gamle stabile distribution (sarge) indeholder ikke otrs2.</p>

<p>I den stabile distribution (etch) er dette problem rettet i
version 2.0.4p01-18.</p>

<p>Den ustabile distribution (sid) er ikke påvirket af dette problem.</p>

<p>Vi anbefaler at du opgraderer din otrs2-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1298.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
