<define-tag description>mappegennemløb</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at dovecot, en sikker mailserver der understøtter mbox- og
maildir-mailbokse, når opsat til at anvende ikke-systembruger-spools og 
komprimerede mapper, kunne gøre det muligt at gennemløbe mapper i 
mailboks-navne.</p>

<p>I den gamle stabile distribution (sarge), findes dette problem ikke.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 1.0.rc15-2etch1.</p>

<p>I den ustabile distribution (sid) vil problemet snart bllive rettet.</p>

<p>Vi anbefaler at du opgraderer din dovecot-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1359.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
