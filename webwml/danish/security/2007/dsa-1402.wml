<define-tag description>usikre midlertidige filer</define-tag>
<define-tag moreinfo>
<p>Steve Kemp fra Debian Security Audit-projektet opdagede at gforge, et 
samarbejdsudviklingsværktøj, anvendte midlertidige filer på en usikker måde,
hvilket kunne gøre det muligt for lokale brugere at trunkere filer på systemet,
med rettighederne hørende til gforge-brugeren, eller iværksætte lammelsesangreb
(denial of service).</p>

<p>I den gamle stabile distribution (sarge), er dette problem rettet i
version 3.1-31sarge4.</p>

<p>I den stabile distribution (etch), er dette problem rettet i version
4.5.14-22etch3.</p>

<p>Vi anbefaler at du opgraderer din gforge-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1402.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
