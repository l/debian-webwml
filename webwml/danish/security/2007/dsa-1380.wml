<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Kalle Olavi Niemitalo opdagede at elinks, en avanceret webbrowser til 
teksttilstand, sendte HTTP POST-oplysninger i klartekst når der blev anvendt en
HTTPS-proxyserver, hvilket potentielt kunne gøre det muligt af afsløre private
oplysninger.</p>

<p>I den stabile distribution (etch), er dette problem rettet i version
0.11.1-1.2etch1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version
0.11.1-1.5.</p>

<p>Vi anbefaler at du opgraderer din elinks-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1380.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
