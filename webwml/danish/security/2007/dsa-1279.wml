<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at WebCalendar, et PHP-baseret kalenderprogram, udførte
utilstrækkelig fornuftighedskontrol i eksporthåndteringen, hvilket gjode det
muligt at indsprøjte webskripter.</p>

<p>I den gamle stabile distribution (sarge) er dette problem rettet i
version 0.9.45-4sarge7.</p>

<p>Den stabile distribution (etch) indeholder ikke længere WebCalendar-pakker.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.0.5-2.</p>

<p>Vi anbefaler at du opgraderer din webcalendar-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1279.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
