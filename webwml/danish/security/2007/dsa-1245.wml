<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Martin Loewer opdagede at ftp-dæmonen proftpd var sårbar over for 
lammelsesangreb (denial of service), hvis tillægsmodulet til 
Radius-autentifikation var aktiveret.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 1.2.10-15sarge4.</p>

<p>I den kommende stabile distribution (etch) er dette problem
rettet i version 1.2.10+1.3.0rc5-1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.2.10+1.3.0rc5-1.</p>

<p>Vi anbefaler at du opgraderer din proftpd-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1245.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
