<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at WebCalendar, et PHP-baseret kalenderprogram, på 
utilstrækkelig vis beskyttede en intern variabel, hvilket muliggjorde
fjerninkludering af filer.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 0.9.45-4sarge6.</p>

<p>Den kommende stabile distribution (etch) indeholder ikke længere 
webcalendar-pakker.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din webcalendar-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1267.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
