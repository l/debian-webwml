<define-tag description>heltalsoverløb</define-tag>
<define-tag moreinfo>
<p>Colin Percival opdagede et heltalsoverløb i file, et værktøj til 
klassifikering af filtyper, hvilket kunne føre til udførelse af vilkårlig 
kode.</p>

<p>I den gamle stabile distribution (sarge) er dette problem rettet i
version 4.12-1sarge2.</p>

<p>I den stabile distribution (etch) er dette problem rettet i
version 4.17-5etch2.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 4.21-1.</p>

<p>Vi anbefaler at du opgraderer din file-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1343.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
