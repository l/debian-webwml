<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at zoph, et webbaseret system til håndtering af fotografier,
udførte utilstrækkelig kontrol af inddata, hvilket muliggjorde indsprøjtning af
SQL.</p>

<p>I den gamle stabile distribution (sarge) er dette problem rettet i
version 0.3.3-12sarge3.</p>

<p>I den stabile distribution (etch) er dette problem rettet i
version 0.6-2.1etch1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 0.7.0.2-1.</p>

<p>Vi anbefaler at du opgraderer din zoph-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2007/dsa-1389.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
