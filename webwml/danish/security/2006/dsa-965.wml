<define-tag description>null-dereference</define-tag>
<define-tag moreinfo>
<p>Implementeringen af Internet Key Exchange version 1 (IKEv1) i racoon fra
ipsec-tools, IPsec-værktøjer til Linux, prøvede at dereferencere en NULL-pointer
under visse omstændigheder, hvilket gjorde det muligt for fjernangribere at 
forårsage et lammelsesangreb (denial of service).</p>

<p>Den gamle stabile distribution (woody) indeholder ikke ipsec-tools.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 0.5.2-1sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 0.6.3-1.</p>

<p>Vi anbefaler at du opgraderer din racoon-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-965.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
