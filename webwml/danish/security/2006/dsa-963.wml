<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>NISCC rapporterer at MyDNS, en DNS-server som anvender en SQL-database til 
lagring af data, kan snydes ind i en uendelig løkke af en fjernangriber og
dermed forårsage et lammelsesangreb (denial of service).</p>

<p>Den gamle stabile distribution (woody) indeholder ikke mydns-pakker.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 1.0.0-4sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.1.0+pre-3.</p>

<p>Vi anbefaler at du opgraderer din mydns-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-963.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
