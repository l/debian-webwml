<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at webapplikationsservern Zope ikke slog direktivet 
csv_table fra i websider indeholdende ReST-markup, hvilket gjorde det muligt at
afsløre filer der var læsbare af Zope-serveren.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 2.7.5-2sarge2.</p>

<p>Den ustabile distribution (sid) indeholder ikke længere zope2.7, i zope2.8 
er dette problem rettet i version 2.8.8-2.</p>

<p>Vi anbefaler at du opgraderer din Zope-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1176.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
