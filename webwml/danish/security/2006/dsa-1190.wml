<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Oliver Karow har opdaget at WebDBM-frontend'en til MaxDB-databasen udførte
utilstrækkelig kontrol af forespørgsler sendt til den, hvilket kunne føre til
udførelse af vilkårlig kode.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 7.5.00.24-4.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din maxdb-7.5.00-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1190.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
