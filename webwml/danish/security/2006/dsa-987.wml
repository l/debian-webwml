<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Jim Meyering har opdaget flere bufferoverløb i GNU tar, der kunne føre til
udførelse af vilkårlig kode via særligt fremstillede tar-arkiver.</p>

<p>Den gamle stabile distribution (woody) er ikke påvirket af dette problem.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 1.14-2.1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.15.1-3.</p>

<p>Vi anbefaler at du opgraderer din tar-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-987.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
