<define-tag description>usikre midlertidige filer</define-tag>
<define-tag moreinfo>
<p>Den oprindelige bulletin for dette problem indeholdt ikke rettede pakker til
alle understøttede arkitekturer, hvilket rettes med denne opdatering.  Til 
reference er herunder den oprindelige bulletins tekst:</p>

<blockquote>
<p>Marco d'Itri har opdaget at thttpd, en lille, hurtig og sikker webserver,
anvendte usikre midlertidige filer når dens logfiler blev roteret, hvilket
kunne føre til et lammelsesangreb (denial of service) gennem et 
symlinkangreb.</p>
</blockquote>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 2.23beta1-3sarge2.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 2.23beta1-5.</p>

<p>Vi anbefaler at du opgraderer din thttpd-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1205.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
