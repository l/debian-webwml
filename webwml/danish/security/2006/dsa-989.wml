<define-tag description>SQL-indsprøjtning</define-tag>
<define-tag moreinfo>
<p>Neil McBride har opdaget at Zoph, et webbaseret fotohåndteringssystem
udførte utilstrækkelige kontrol af de oplysninger der blev brugt i en 
fotosøgning, hvilket kunne føre til udførelse af SQL-kommandoer via et
SQL-indsprøjtningsangreb.</p>

<p>Den gamle stabile distribution (woody) indeholder ikke zoph-pakker.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 0.3.3-12sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 0.5-1.</p>

<p>Vi anbefaler at du opgraderer din zoph-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-989.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
