<define-tag description>designfejl</define-tag>
<define-tag moreinfo>
<p>En designfejl er opdaget i webapplikationstoolkitet Albatross, som anvender
brugerleverede oplysninger der bruges som en del af udførelsen af en skabelon, 
hvorved der kunne udføres vilkårlig kode.</p>

<p>Den gamle stabile distribution (woody) indeholder ikke albatross-pakker.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 1.20-2.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 1.33-1.</p>

<p>Vi anbefaler at du opgraderer din albatross-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-942.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
