<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Gerald Carter har opdaget at smbd-dæmonen fra Samba, en fri implementering af
protokollen SMB/CIFS, pålagde utilstrækkelige begrænsninger i koden, der 
håndterede delte forbindelser, hvilket kunne udnyttes til at opbruge 
systemhukommelse ved at sende ondsindet fremstillede forespørgsler, førende til
et lammelsesangreb (denial of service).</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 3.0.14a-3sarge2.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din samba-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1110.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
