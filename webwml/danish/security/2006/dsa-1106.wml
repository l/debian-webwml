<define-tag description>programmersingfejl</define-tag>
<define-tag moreinfo>
<p>Marcus Meissner har opdaget at webbind-plugin'en i pppd ikke kontrollerer
om et setuid()-kald har haft succes, når rettighedernes forsøges smidt væk,
hvilket kunne fejle ved nogle PAM-opsætninger.</p>

<p>Den gamle stabile distribution (woody) er ikke påvirket af dette problem.</p>

<p>I den stabile distribution (sarge) er dette problem rettet i
version 2.4.3-20050321+2sarge1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i
version 2.4.4rel-1.</p>

<p>Vi anbefaler at du opgraderer din ppp-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1106.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
