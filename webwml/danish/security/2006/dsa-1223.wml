<define-tag description>fejl ved validering af inddata</define-tag>
<define-tag moreinfo>
<p>Teemu Salmela har opdaget en sårbarhed i GNU tar, der kunne gøre det muligt
for en ondsindet bruger at overskrive vilkårlige filer ved at få offeret til at
forsøge at udpakke en særligt fremstillet tar-fil indeholdende en 
GNUTYPE_NAMES-post med et symbolsk link.</p>

<p>I den stabile distribution (sarge), er dette problem rettet i
version 1.14-2.3.</p>

<p>I den ustabile distribution (sid) og den kommende stabile udgave (etch), 
vil disse problem blive rettet i version 1.16-2.</p>

<p>Vi anbefaler at du opgraderer din tar-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1223.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
