<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Man opdagede at Simple Linux Utility for Resource Management (SLURM), et 
klyngejobhåndterings- og scheduleringssystem, ikke smed supplerende grupper væk. 
Disse grupper kunne være systemgrupper med forøgede rettigheder, hvilket måske 
kunne gøre det muligt for en gyldig SLURM-bruger, at opnå forøgede 
rettigheder.</p>

<p>Den gamle stabile distribution (etch) indeholder ikke en slurm-llnl-pakke.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i
version 1.3.6-1lenny3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.3.15-1.</p>

<p>Vi anbefaler at du opgraderer din slurm-llnl-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1776.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
