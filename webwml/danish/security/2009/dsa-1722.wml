<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Derek Chan opdagede at PAM-modulet til implementeringen af Heimdal Kerberos
tillod geninitialisering af brugeroplysninger, når det blev kørt fra en 
setuid-kontekst, potentielt medførende et lokalt lammelsesangreb (denial of 
service) ved at overskrive oplysningscachefilen eller førende til 
rettighedsforøgelse.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 2.5-1etch1.</p>

<p>I den kommende stabile distribution (lenny), er dette problem
rettet i version 3.10-2.1.</p>

<p>I den ustabile distribution (sid), vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din libpam-heimdal-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1722.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
