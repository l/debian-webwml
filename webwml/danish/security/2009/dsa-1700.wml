<define-tag description>ukorrekt API-anvendelse</define-tag>
<define-tag moreinfo>
<p>Man opdagede at Lasso, et bibliotek til Liberty Alliance- og 
SAML-protokollerne udførte utilstrækkelig kontrol af returværdien fra 
OpenSSL's DSA_verify()-funktion.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 0.6.5-3+etch1.</p>

<p>I den kommende stabile distribution (lenny) og i den ustabile 
distribution (sid), er dette problem rettet i version 2.2.1-2.</p>

<p>Vi anbefaler at du opgraderer din lasso-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1700.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
