<define-tag description>fortolkningskonflikt</define-tag>
<define-tag moreinfo>
<p>Man har opdaget at NTP, en implementering af Network Time Protocol, ikke på
korrekt vis kontrollerede kryptografiske signaturer, hvilket slutteligt kunne 
føre til accept af uautentificerede tidsoplysninger.  (Bemærk at krytografisk
autentifikation af time-servere ofte slet ikke er aktiveret.)</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 4.2.2.p4+dfsg-2etch1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 4.2.4p4+dfsg-8.</p>

<p>I testing-distributionen (lenny) det det snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din ntp-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1702.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
