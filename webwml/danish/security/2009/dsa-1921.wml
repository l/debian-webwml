<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>

<p>Peter Valchev opdagede en fejl i expat, et C-bibliotek til fortolkning af
XML, når der blev fortolket visse UTF-8-sekvenser, hvilket kunne udnyttes til 
at få en applikation, der anvender biblioteket, til at gå ned.</p>

<p>I den gamle stabile distribution (etch), er dette problem rettet i
version 1.95.8-3.4+etch1.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i
version 2.0.1-4+lenny1.</p>

<p>I distributionen testing (squeeze) og i den ustabile distribution
(sid), vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer dine expat-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1921.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
