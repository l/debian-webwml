<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at yaws, en højtydende HTTP 1.1-webserver, var sårbar over for 
et lammelsesangreb (denial of service) gennem en forespørgsel med en stor 
HTTP-header.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i
version 1.77-3+lenny1.</p>

<p>I den gamle stabile distribution (etch), er dette problem rettet i
version 1.65-4etch1.</p>

<p>I distributionen testing (squeeze) og i den ustabile distribution
(sid), er dette problem rettet i version 1.80-1.</p>

<p>Vi anbefaler at du opgraderer din yaws-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1740.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
