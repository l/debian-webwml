<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Jan Lieskovsky opdagede en fejl i expat, et XML-fortolkningsbibliotek i C, 
der når visse UTF-8-sekvenser blev fortolket, kunne udnyttes til at få en 
applikation, der anvender biblioteket, til at gå ned.</p>

<p>I den gamle stabile distribution (etch), er dette problem rettet i
version 1.95.8-3.4+etch2.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i
version 2.0.1-4+lenny2.</p>

<p>I distributionen testing (squeeze) og i den ustabile distribution
(sid), this problem will be i version 2.0.1-6.</p>

<p>Opbygningerne til mipsel-arkitekturen i den gamle stabile distribution er 
ikke klar endnu.  De vil blive frigivet så snart de bliver tilgængelige.</p>

<p>Vi anbefaler at du opgraderer dine expat-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1953.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
