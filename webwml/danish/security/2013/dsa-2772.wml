#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>udførelse af skripter på tværs af websteder</define-tag>
<define-tag moreinfo>
<p>Markus Pieton og Vytautas Paulikas opdagede at den indlejrede video- og 
lydafspiller i håndteringssystemet til webindhold, TYPO3, var sårbar over for 
udførelse af skripter på tværs af websteder.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 4.5.19+dfsg1-5+wheezy1.</p>

<p>I distributionen testing (jessie), er dette problem rettet i
version 4.5.29+dfsg1-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 4.5.29+dfsg1-1.</p>

<p>Vi anbefaler at du opgraderer dine typo3-src-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2013/dsa-2772.data"
