#use wml::debian::translation-check translation="1.2" mindelta="1"
<define-tag description>heapoverløb</define-tag>
<define-tag moreinfo>
<p>Jon Erickson fra iSIGHT Partners Labs opdagede et heapoverløb i 
xml-security-c, en implementering af XML Digital Security-specifikationen.  
Retten der løser 
<a href="https://security-tracker.debian.org/tracker/CVE-2013-2154">\
CVE-2013-2154</a>, indførte mulighed for et heapoverløb i behandlingen af 
misdannede XPointer-udtryk i behandlingskoden vedrørende XML Signature 
Reference, muligvis førende til udførelse af vilkårlig kode.</p>

<p>I den gamle stabile distribution (squeeze), er dette problem rettet i
version 1.5.1-3+squeeze3.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1.6.1-5+deb7u2.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.6.1-7.</p>

<p>Vi anbefaler at du opgraderer dine xml-security-c-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2013/dsa-2717.data"
