#use wml::debian::translation-check translation="1.2" mindelta="1"
<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Bob Nomnomnom rapporterede om en lammelsesangrebssårbarhed (denial of 
service) i IRCD-Hybrid, en Internet Relay Chat-server.  En fjernangriber kunne 
udnytte en fejl i maskevalideringen til at få serveren til at gå ned.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 1:1:7.2.2.dfsg.2-6.2+squeeze1.</p>

<p>I distributionen testing (wheezy), er dette problem rettet i
version 1:7.2.2.dfsg.2-10.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:7.2.2.dfsg.2-10.</p>

<p>Vi anbefaler at du opgraderer dine ircd-hybrid-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2013/dsa-2618.data"
