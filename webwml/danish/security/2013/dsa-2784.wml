#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>anvendelse efter frigivelse</define-tag>
<define-tag moreinfo>
<p>Pedro Ribeiro opdagende en forekomst af anvendelse efter frigivelse i 
håndteringen af ImageText-forespørgsler i Xorg Xserver, hvilket kunne medføre 
lammelsesangreb (denial of service) eller rettighedsforøgelse.</p>

<p>I den gamle stabile distribution (squeeze), er dette problem rettet i
version 1.7.7-17.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1.12.4-6+deb7u1.</p>

<p>I distributionen testing (jessie), er dette problem rettet i
version 1.14.3-4.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.14.3-4.</p>

<p>Vi anbefaler at du opgraderer dine xorg-server-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2013/dsa-2784.data"
