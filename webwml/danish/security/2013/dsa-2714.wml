#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Konstantin Belousov og Alan Cox opdagede at utilstrækkelige 
rettighedskontroller i hukommelseshåndteringen i FreeBSD-kernen, kunne føre til 
rettighedsforøgelse.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 9.0-10+deb70.2.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 9.0-12.</p>

<p>Vi anbefaler at du opgraderer dine kfreebsd-9-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2013/dsa-2714.data"
