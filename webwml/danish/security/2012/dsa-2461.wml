#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Flere sårbarheder er opdaget i SPIP, en maskine til offentliggørelse på 
websteder, som medførte udførelse af skripter på tværs af websteder, 
indsprøjtning af skriptkode samt omgåelse af restriktioner.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
2.1.1-3squeeze3.</p>

<p>I distributionen testing (wheezy), er dette problem rettet i version 
2.1.13-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
2.1.13-1.</p>

<p>Vi anbefaler at du opgraderer dine spip-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2461.data"
