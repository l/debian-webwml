#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>skripting på tværs af servere</define-tag>
<define-tag moreinfo>
<p>Emilio Pinna opdagede en sårbarhed i forbindelse med udførelse af skripter på 
tværs af servere, i siden spellchecker.php hørende til FCKeditor, en populær 
HTML-/DHTML-webeditor.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
1:2.6.6-1squeeze1.</p>

<p>I distributionen testing (wheezy), er dette problem rettet i version 
1:2.6.6-3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
1:2.6.6-3.</p>

<p>Vi anbefaler at du opgraderer dine fckeditor-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2522.data"
