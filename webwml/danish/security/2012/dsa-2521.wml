#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>heltalsoverløb</define-tag>
<define-tag moreinfo>
<p>Jueri Aedla opdagede flere heltalsoverløb i libxml, hvilke kunne føre til 
udførelse af vilkårlig kode eller lammelsesangreb (denial of service).</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
2.7.8.dfsg-2+squeeze5.</p>

<p>I distributionen testing (wheezy) og i den ustabile distribution (sid), er 
dette problem rettet i version 2.8.0+dfsg1-5.</p>

<p>Vi anbefaler at du opgraderer dine libxml2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2521.data"
