#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Et heapbaseret bufferoverløb blev opdaget i bogofilter, en programpakke til 
klassifikation af mail som spam eller ikke-spam.  Fabrikerede mails med ugyldig
base64-data kunne føre til heapkorruption og potentielt udførelse af vilkårlig 
kode.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
1.2.2-2+squeeze1.</p>

<p>I distributionen testing (wheezy) og i den ustabile distribution (sid), er 
dette problem rettet i version 1.2.2+dfsg1-2.</p>

<p>Vi anbefaler at du opgraderer dine bogofilter-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2585.data"
