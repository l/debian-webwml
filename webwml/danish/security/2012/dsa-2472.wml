#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>rettighedsforøgelse</define-tag>
<define-tag moreinfo>
<p>Dave Love opdagede, at brugere som har tilladelse til at submit'e jobs til en
Grid Engine-installation, kunne forøge deres rettigheder til root, fordi miljøet
ikke på korrekt vis var fornuftighedskontrolleret før processer blev 
oprettet.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
6.2u5-1squeeze1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
6.2u5-6.</p>

<p>Vi anbefaler at du opgraderer dine gridengine-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2472.data"
