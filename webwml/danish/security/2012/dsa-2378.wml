#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Flere sårbarheder blev opdaget i FFmpeg, en multimedieafspiller, -server og
-encoder.  Flere inddatavalideringer i decoderne til QDM2-, VP5-, VP6-, VMD- og 
SVQ1-filer kunne føre til udførelse af vilkårlig kode.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 4:0.5.6-3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 4:0.7.3-1 af kildekodepakken libav.</p>

<p>Vi anbefaler at du opgraderer dine ffmpeg-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2378.data"
