#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>ugyldig hukommelsesdeallokering</define-tag>
<define-tag moreinfo>
<p>Man opdagede, at koden til IPv6-understøttelse i Squid, ikke på korrekt vis
håndterede visse DNS-svar, medførende deallokering af en ugyldig pointer og et
dæmonnedbrud.</p>

<p>Pakken squid og den udgave af Squid 3, der leveres med lenny, mangler 
understøttelse af IPv6 og er ikke påvirkede af dette problem.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
3.1.6-1.2+squeeze2.</p>

<p>I distributionen testing (wheezy) og i den ustabile distribution (sid), 
er dette problem rettet i version 3.1.18-1.</p>

<p>Vi anbefaler at du opgraderer dine squid3-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2381.data"
