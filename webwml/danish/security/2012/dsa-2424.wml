#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>udvidelse af eksterne XML-entiteter</define-tag>
<define-tag moreinfo>
<p>Man opdagede, at Perl-modulet XML::Atom ikke deaktiverede eksterne entiteter,
når det fortolkede XML fra kilder, som der potentielt ikke er tillid til.  
Dermed var der måske mulighed for, at angribere kunne få læseadgang til ellers
beskyttede ressourcer, afhængigt af hvordan biblioteket blev benyttet.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
0.37-1+squeeze1.</p>

<p>I distributionen testing (wheezy) og i den ustabile distribution (sid), er 
dette problem rettet i version 0.39-1.</p>

<p>Vi anbefaler at du opgraderer dine libxml-atom-perl-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2424.data"
