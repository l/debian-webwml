#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>manglende rettighedskontroller</define-tag>
<define-tag moreinfo>
<p>Ivano Cristofolini opdagede, at utilstrækkelige sikkerhedskontroller i Sambas 
håndtering af LSA RPC-kald kunne føre til rettighedsforøgelse, ved at få 
rettigheden <q>take ownership</q>.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
3.5.6~dfsg-3squeeze8.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
3.6.5-1.</p>

<p>Vi anbefaler at du opgraderer dine samba-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2463.data"
