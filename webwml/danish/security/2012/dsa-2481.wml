#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>supplerende grupper droppes ikke</define-tag>
<define-tag moreinfo>
<p>Steve Grubb fra Red Hat opdagede, at en patch til arpwatch (som i hvert fald 
distribueres i Red Hats og Debians distributioner), med henblik på at smide 
root-rettigheder væk, mislykkedes og i stedet tilføjedes root-gruppen til listen, 
som dæmonen anvender.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
2.1a15-1.1+squeeze1.</p>

<p>I distributionen testing (wheezy), er dette problem rettet i version 
2.1a15-1.2.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.1a15-1.2.</p>

<p>Vi anbefaler at du opgraderer dine arpwatch-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2481.data"
