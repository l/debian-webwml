#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Nicholas Gregoire og Cris Neckar opdagede flere fejl i forbindelse med 
hukommelseshåndteringen i libxslt, hvilke kunne føre til lammelsesangreb (denial 
of service) eller udførelse af vilkårlig kode, hvis et misdannet dokument blev 
behandlet.</p>

<p>I den stabile distribution (squeeze), er disse problemer rettet i version 
1.1.26-6+squeeze2.</p>

<p>I den ustabile distribution (sid), er disse problemer rettet i version 
1.1.26-14.</p>

<p>Vi anbefaler at du opgraderer dine libxslt-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2555.data"
