#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at libmatroska, et udvidelsebart åben standard-containerformat 
til lyd og video, på ukorrekt vis behandlede EBML-lacing.  Ved at levere 
ondsindet fremstillet inddata, kunne en angriber udnytte fejlen til at 
gennemtvinge en eller anden form for lækage af oplysninger, opbevaret i 
procesheaphukommelsen.</p>

<p>I den gamle stabile distribution (wheezy), er dette problem rettet
i version 1.3.0-2+deb7u1.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 1.4.1-2+deb8u1.</p>

<p>I distributionen testing (stretch) og i den ustabile distribution (sid), er 
dette problem rettet i version 1.4.4-1.</p>

<p>Vi anbefaler at du opgraderer dine libmatroska-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3526.data"
