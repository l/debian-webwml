#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Andreas Cord-Landwehr opdagede at kde4libs, core-bibliotekerne til alle 
KDE 4-applikationer, ikke på korrekt vis hånterede udpakning af arkiver med 
<q>../</q> i filstier.  En fjernangriber kunne drage nytte af fejlen til at 
overskrive filer uden for udpakningsmappen, hvis en bruger blev narret til at 
udpakke et særligt fremstillet arkiv.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 4:4.14.2-5+deb8u1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 4:4.14.22-2.</p>

<p>Vi anbefaler at du opgraderer dine kde4libs-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3643.data"
