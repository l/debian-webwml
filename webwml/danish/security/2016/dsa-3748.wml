#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Gergely Gábor Nagy fra Tresorit opdagede at libcrypto++, et kryptografisk
C++-bibliotek, indeholdt en fejl i flere ASN.1-fortolkningsrutiner.  Dermed var 
det muligt for en fjernangriber at forårsage et lammelsesangreb (denial of 
service).</p>

<p>I den stabile distribution (jessie), er dette problem rettet i version 
5.6.1-6+deb8u3.</p>

<p>I distributionen testing (stretch) og i den ustabile distribution (sid), er 
dette problem rettet i version 5.6.4-5.</p>

<p>Vi anbefaler at du opgraderer dine libcrypto++-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3748.data"
