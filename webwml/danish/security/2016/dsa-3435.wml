#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Blake Burkhart opdagede at Gits helper git-remote-ext på ukorrekt vis 
håndterede rekursive kloner af git-arkiver.  En fjernangriber kunne muligvis 
anvende problemet til at udføre vilkårlig kode, ved at indsprøjte kommandoer 
gennem fabrikerede URL'er.</p>

<p>I den gamle stabile distribution (wheezy), er dette problem rettet
i version 1:1.7.10.4-1+wheezy2.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 1:2.1.4-2.1+deb8u1.</p>

<p>I distributionen testing (stretch), er dette problem rettet
i version 1:2.6.1-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:2.6.1-1.</p>

<p>Vi anbefaler at du opgraderer dine git-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3435.data"
