#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>To sårbarheder i forbindelse med udførelse af skripter på tværs af websteder 
er fundet i Horizon, en webapplikation til kontrollering af en 
OpenStack-sky.</p>

<p>I den stabile distribution (jessie), er disse problemer rettet i version 
2014.1.3-7+deb8u2.</p>

<p>I distributionen testing (stretch), er disse problemer rettet i version 
3:9.0.1-2.</p>

<p>I den ustabile distribution (sid), er disse problemer rettet i version 
3:9.0.1-2.</p>

<p>Vi anbefaler at du opgraderer dine horizon-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3617.data"
