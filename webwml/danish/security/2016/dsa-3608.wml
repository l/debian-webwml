#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Aleksandar Nikolic opdagede at manglende fornuftighedskontrol af inddata i 
RTF-fortolkeren i Libreoffice, kunne medføre udførelse af vilkårlig kode, hvis 
et misdannet dokument blev åbnet.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 1:4.3.3-2+deb8u5.</p>

<p>I distributionen testing (stretch), er dette problem rettet
i version 1:5.1.4~rc1-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:5.1.4~rc1-1.</p>

<p>Vi anbefaler at du opgraderer dine libreoffice-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3608.data"
