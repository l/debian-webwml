#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Jose Duart fra Google Security Team opdagede en fejl i forbindelse med 
dobbelt frigivelse 
(<a href="https://security-tracker.debian.org/tracker/CVE-2014-8137">\
CVE-2014-8137</a>) og en heapbaseret bufferoverløbsfejl 
(<a href="https://security-tracker.debian.org/tracker/CVE-2014-8138">\
CVE-2014-8138</a>) i JasPer, et bibliotek til behandling af JPEG-2000-filer. En 
særligt fremstillet fil kunne medføre, at applikationer som anvender JasPer gik 
ned eller måske kunne udføre vilkårlig kode.</p>

<p>I den stabile distribution (wheezy), er disse problemer rettet i
version 1.900.1-13+deb7u2.</p>

<p>I den kommende stabile distribution (jessie) og i den ustabile distribution 
(sid), vil disse problemer snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer dine jasper-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-3106.data"
