#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Jakub Wilk opdagede at APT, et pakkehåndteringsprogram på højt niveau, ikke 
på korrekt vis udførte autentifikationskontroller vedrørende kildekodepakker 
downloadet ved hjælp af <q>apt-get source</q>.  Det påvirker kun 
anvendelsessituationer, hvor kildekodepakker downloades med denne kommando; det 
påvirker ikke den almindelige installering af og opgradering af 
Debian-pakker.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 0.9.7.9+deb7u2.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.0.4.</p>

<p>Vi anbefaler at du opgraderer dine apt-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-2958.data"
