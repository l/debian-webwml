#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at mailserveren Dovecot var sårbar over for et lammelsesangreb 
(denial of service) mod imap-/pop3-loginprocesserne på grund af ukorrekt 
håndtering af lukning af inaktive SSL-/TLS-forbindelser.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1:2.1.7-7+deb7u1.</p>

<p>I distributionen testing (jessie), er dette problem rettet i
version 1:2.2.13~rc1-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:2.2.13~rc1-1.</p>

<p>Vi anbefaler at du opgraderer dine dovecot-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-2954.data"
