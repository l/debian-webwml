#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at et bufferoverløb i behandlingen af Glyph Bitmap 
Distribution-skrifttyper (BDF), kunne medføre udførelse af vilkårlig kode.</p>

<p>I den gamle stabile distribution (squeeze), er dette problem rettet i
version 1:1.4.1-4.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1:1.4.5-3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:1.4.7-1.</p>

<p>Vi anbefaler at du opgraderer dine libxfont-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-2838.data"
