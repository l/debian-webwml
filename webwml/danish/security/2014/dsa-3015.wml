#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>En heapbaseret overløbssårbarhed blev fundet i den måde Lua, et simpelt 
udvidbart, embedbart programmeringssprog, håndterede varargs-funktioner med 
mange faste parametre kaldt med få argumenter, førende til applikationsnedbrud 
eller potentielt udførelse af vilkårlig kode.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 5.1.5-4+deb7u1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 5.1.5-7.</p>

<p>Vi anbefaler at du opgraderer dine lua5.1-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-3015.data"
