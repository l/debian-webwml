#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Florian Weimer opdagede et bufferoverløb i udisks' kode til fortolkning af 
mount-stien, hvilket kunne medføre en rettighedsforøgelse.</p>

<p>I den gamle stabile distribution (squeeze), er dette problem rettet i
version 1.0.1+git20100614-3squeeze1.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1.0.4-7wheezy1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.0.5-1.</p>

<p>Vi anbefaler at du opgraderer dine udisks-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-2872.data"
