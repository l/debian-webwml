#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Stefano Zacchiroli opdagede en sårbarhed i exuberant-ctags, et værktøj til 
opbygning af tagfilindeks over kildekodedefinitioner:  Visse JavaScript-filer 
medførte at ctags kom i en uendelig løkke, indtil den løb tør for diskplads, 
hvilket slutteligt medførte et lammelsesangreb (denial of service).</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 1:5.9~svn20110310-4+deb7u1.</p>

<p>I distributionen testing (jessie), er dette problem rettet i
version 1:5.9~svn20110310-8.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:5.9~svn20110310-8.</p>

<p>Vi anbefaler at du opgraderer dine exuberant-ctags-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-3042.data"
