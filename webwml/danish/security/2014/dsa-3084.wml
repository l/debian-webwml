#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Dragana Damjanovic opdagede at en autentificeret klient kunne få en 
OpenVPN-server til at gå ned, ved at sende en kontrolpakke indeholdende mindre 
end fire bytes som payload.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 2.2.1-8+deb7u3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.3.4-5.</p>

<p>Vi anbefaler at du opgraderer dine openvpn-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-3084.data"
