<define-tag description>fjernroot-udnyttelse</define-tag>
<define-tag moreinfo>
<p>iSEC Security Research rapporterer at wu-ftpd indeholder en forskudt med 
en-fejl i funktionen fb_realpath, som kunne udnyttes af en bruger, der er logget
på (lokal eller anonym) til at opnå root-rettigheder.  Efter sigende er der et
eksempel på udnyttelse tilgængelig.</p>

<p>I den nuværende stabile distribution (woody) er dette problem rettet
i version 2.6.2-3woody1. </p>

<p>I den ustabile distribution (sid) vil en opdatering snart blive gjort 
tilgængelig.</p>

<p>Vi anbefaler at du omgående opgraderer din wu-ftpd-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-357.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
