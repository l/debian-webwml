<define-tag description>uendelig løkke</define-tag>
<define-tag moreinfo>
<p>Der er opdaget et problem tcpdump, et stærkt værktøj til netværksovervågning 
og datahentning.  En angriber kan sende en særligt fremstillet 
RADIUS-netværkspakke, som får tcpdump til at gå i en uendelig løkke.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
3.6.2-2.4.</p>

<p>Den gamle stabile distribution (potato) lader ikke til at være påvirket af
dette problem.</p>

<p>Den ustabile distribution (sid) er ikke længere påvirket af dette 
problem.</p>

<p>Vi anbefaler at du opgraderer din tcpdump-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-261.data"
#use wml::debian::translation-check translation="1.1"