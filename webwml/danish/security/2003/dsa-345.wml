<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Et nyt bufferoverløb er opdaget i xbl, som adskiller sig fra det, der blev
rettet med <a href="dsa-327">DSA-327</a> (CAN-2003-0451) ved at involvere 
kommandolinie-parameteret <kbd>-display</kbd>.  Denne sårbarhed kunne udnyttes 
af en lokal angriber til at opnå gid 'games'.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
1.0k-3woody2.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i version 
1.0k-6.</p>

<p>Vi anbefaler at du opdaterer din xbl-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-345.data"
#use wml::debian::translation-check translation="1.2"
