<define-tag description>bufferoverløb, lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Bas Wijnen har opdaget at gnocatan-serveren er sårbar overfor flere 
bufferoverløb, der kunne udnyttes til udførelse af vilkårlig kode på
serversystemet.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
0.6.1-5woody2.</p>

<p>Den gamle stabile distribution (potato) indeholder ikke en gnocatan-pakke.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opdaterer din gnocatan-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-315.data"
#use wml::debian::translation-check translation="1.2"