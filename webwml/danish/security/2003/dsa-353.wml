<define-tag description>usikker midlertidig fil</define-tag>
<define-tag moreinfo>
<p>sup, en pakke der anvendes til at vedligeholde samlinger af identiske filer
på forskellige maskiner, foretager ikke de nødvendige sikkerhedsforanstaltninger
ved oprettelse af midlertidige filer.  En lokal angriber kunne udnytte denne
sårbarhed til at overskrive vilkårlige filer med rettighederne hørende til den
bruger, der kører sup.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
1.8-8woody1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i version 
1.8-9.</p>

<p>Vi anbefaler at du opdaterer din sup-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-353.data"
#use wml::debian::translation-check translation="1.1"
