<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Knud Erik Højgaard har opdaget en sårbarhed i moxftp (henholdsvis xftp), en
Athena X-grænseflade til ftp.  Utilstrækkelig grænsekontrol kunne føre til 
udførelse af vilkårlig kode, tilgængelig på en ondsindet ftp-server.  Erik Tews 
har rettet dette.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
2.2-18.1.</p>

<p>I den gamle stabile distribution (potato) er dette problem rettet i version 
2.2-13.1.</p>

<p>I den ustabile distribution (sid) er dette problem rettet i version 
2.2-18.20.</p>

<p>Vi anbefaler at du opgraderer din xftp-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-281.data"
#use wml::debian::translation-check translation="1.1"