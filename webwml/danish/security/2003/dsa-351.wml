<define-tag description>udførsel af skripter på tværs af websteder</define-tag>
<define-tag moreinfo>
<p>Funktionaliteten "transparent session ID" i php4-pakken indkapsler ikke 
brugerleverede data korrekt, før disse indsættes i den genererede HTML-side.
En angriber kunne anvende denne sårbarhed til at udføre indlejrede skripter
indenfor den genererede sides kontekst.</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
4:4.1.2-6woody3.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.
Se Debians <a href="http://bugs.debian.org/200736">fejl nummer 200736</a>.</p>

<p>Vi anbefaler at du opdaterer din php4-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-351.data"
#use wml::debian::translation-check translation="1.2"