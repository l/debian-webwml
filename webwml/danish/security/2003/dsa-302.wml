<define-tag description>rettighedsforøgelse</define-tag>
<define-tag moreinfo>
<p>Joey Hess har opdaget at fuzz, et værktøj til strestestning af programmel,
opretter midlertidige filer uden at fortage passende sikkerhedsforanstaltninger.
Denne fejl kunne give en angriber mulighed for at få rettighederne hørende til
den bruger, som udfører fuzz, bortset fra root (fuzz tillader ikke sig selv at
blive udført som root).</p>

<p>I den stabile distribution (woody) er dette problem rettet i version 
0.6-6woody1.</p>

<p>Den gamle stabile distribution (potato) indeholder ikke en fuzz-pakke.</p>

<p>I den ustabile distribution (sid) vil dette blive snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din fuzz-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-302.data"
#use wml::debian::translation-check translation="1.1"