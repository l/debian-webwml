<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Steve Kemp har opdaget flere bufferoverløb i atari800, en Atari-emulator.  
For at tilgå grafikhardwaren direkte, er et af de påvirkede programmer setuid 
root.  En lokal angriber kunne udnytte denne sårbarhed til at opnå 
root-rettigheder.</p>

<p>I den nuværende stabile distribution (woody) er dette problem rettet
i version 1.2.2-1woody2.</p>

<p>I den ustabile distribution (sid) vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opdaterer din atari800-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-359.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
