<define-tag description>usikker generering af midlertidig fil</define-tag>
<define-tag moreinfo>
<p>Javier Fernandez-Sanguino Pena opdagede at updatejail, en komponent hørende
til chroot-vedligeholdelsesværktÃ¸jet Jailer, oprettede et forudsigeligt 
midlertidigt filnavn, hvilket måske kunne føre til lammelsesangreb (denial of
service) gennem et symlink-angreb.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 0.4-9+etch1.</p>

<p>I den kommande stabile distribution (lenny) og i den unstabile
distribution (sid), er dette problem rettet i version 0.4-10.</p>

<p>Vi anbefaler at du opgraderer din jailer-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1674.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
