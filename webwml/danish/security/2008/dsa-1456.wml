<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>
Daniel B. Cid opdagede at fail2ban, et værktøj til blokering af IP-adresser 
som forårsager loginfejl, var for fri i sin håndtering af SSH-logfiler, hvilket
gjorde det muligt for en angriber af blokere enhver IP-adresse.
</p>

<p>
Den gamle stabile distribution (sarge) indeholder ikke fail2ban.
</p>

<p>
I den stabile distribution (etch), er dette problem rettet i
version 0.7.5-2etch1.
</p>

<p>
I den ustabile distribution (sid), er dette problem rettet i
version 0.8.0-4.
</p>

<p>
Vi anbefaler at du opgraderer din fail2ban-pakke.
</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1456.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
