<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at særligt fremstillede regulære udtryk, der anvendte 
codepoints større end 255 kunne forårsage et bufferoverløb i PCRE-biblioteket
(<a href="http://security-tracker.debian.org/tracker/CVE-2008-0674">\
CVE-2008-0674</a>).</p>

<p>I den gamle stabile distribution (sarge), er dette problem rettet i
version 4.5+7.4-2.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 6.7+7.4-3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version
7.6-1.</p>

<p>Vi anbefaler at du opgraderer din pcre3-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1499.data"
#use wml::debian::translation-check translation="1.5" mindelta="1"
