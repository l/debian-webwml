<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at libfishsound, et simpelt programmeringsinterface der 
indpakker Xiph.Org-audiocodecs, ikke på korrekt vis håndterede negative værdier
i et bestemt headerfelt.  Dette kunne gøre det muligt for ondsindede filer at
udføre vilkårlig kode.</p>

<p>I den stabile distribution (etch), er dette problem rettet i version
0.7.0-2etch1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 0.7.0-2.2.</p>

<p>Vi anbefaler at du opgraderer din libfishsound-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1584.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
