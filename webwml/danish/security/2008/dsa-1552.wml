<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Man opdagede at filmafspilleren MPlayer udførte utilstrækkelig 
fornuftighedskontrol af inddata fra SDP-sessionsdata, hvilket potentielt kunne 
føre til udførelse af vilkårlig kode gennem en misdannet multimediestream.</p>

<p>I den stabile distribution (etch), er dette problem rettet i 
version 1.0~rc1-12etch3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.0~rc2-10.</p>

<p>Vi anbefaler at du opgraderer din mplayer-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1552.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
