<define-tag description>usikker håndtering af midlertidige filer</define-tag>
<define-tag moreinfo>
<p>Dmitry E. Oboukhov opdagede at <q>to-upgrade</q>-pluginen i Feta, en 
simple grænseflade til APT, dpkg og anden af Debians pakkeværktøjer, oprettede
midlertidige filer på usikker vis, hvilket kunne føre til et lokalt
lammelsesangreb (denial of service) gennem symlinkangreb.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 1.4.15+etch1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.4.16+nmu1.</p>

<p>Vi anbefaler at du opgraderer din feta-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1643.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"