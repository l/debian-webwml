<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Alin Rad Pop opdagede at Samba indeholdt en bufferoverløbstilstand, når den 
behandlede visse svar modtaget mens den fungerede som en klient, førende til 
udførelse af vilkårlig kode 
(<a href="http://security-tracker.debian.org/tracker/CVE-2008-1105">CVE-2008-1105</a>).</p>

<p>I den stabile distribution (etch), er dette problem rettet i version 
3.0.24-6etch10.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 3.0.30-1.</p>

<p>Vi anbefaler at du opgraderer dine samba-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1590.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
