<define-tag description>bufferunderløb</define-tag>
<define-tag moreinfo>
<p>Drew Yao opdagede at libTIFF, et bibliotek til håndtering af Tagged Image
File Format, var sårbar på grund af en programmeringsfejl, der gjorde det 
muligt for misdannede tiff-filer at føre til nedbrud eller udførelse af 
vilkårlig kode.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 3.8.2-7+etch1.</p>

<p>I distributionen testing (lenny), er dette problem rettet i
version 3.8.2-10+lenny1.</p>

<p>Den ustabile distribution (sid) vil snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din tiff-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1632.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
