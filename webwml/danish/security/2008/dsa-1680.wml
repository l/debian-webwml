<define-tag description>bufferoverløb, stakforbrug</define-tag>
<define-tag moreinfo>
<p>Moritz Jodeit opdagede at ClamAV, en antivirus-løsning, var ramt af en 
forskud med en-fejl i behandlingen VBA-projektfiler, førende til et heapbaseret
bufferoverløb og potentielt udførelse af vilkårlig kode
(<a href="http://security-tracker.debian.org/tracker/CVE-2008-5050">CVE-2008-5050</a>).</p>

<p>Ilja van Sprundel opdagede at ClamAV indeholdt en lammelsesangrebstilstand 
(denial of service) i behandlingen af JPEG-filer, fordi det ikke begrænsede 
gennemløbsdybden ved behandling af JPEG-thumbnailfiler 
(<a href="http://security-tracker.debian.org/tracker/CVE-2008-5314">CVE-2008-5314</a>).</p>

<p>I den stabile distribution (etch), er disse problemer rettet i
version 0.90.1dfsg-4etch16.</p>

<p>I den ustabile distribution (sid), er disse problemer rettet i
version 0.94.dfsg.2-1.</p>

<p>Distributionen testing (lenny) vil snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer dine clamav-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1680.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"

