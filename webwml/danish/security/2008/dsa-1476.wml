<define-tag description>programmeringsfejl</define-tag>
<define-tag moreinfo>
<p>Marcus Meissner opdagede at lydserveren PulseAudio udførte utilstrækkelige 
kontroller når den smed rettighederne væk, hvilket kunne føre til lokal 
rettighedsforøgelse.</p>

<p>Den gamle stabile distribution (sarge) indeholder ikke pulseaudio.</p>

<p>I den stabile distribution (etch), er dette problem rettet i
version 0.9.5-5etch1.</p>

<p>Vi anbefaler at du opgraderer dine pulseaudio-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2008/dsa-1476.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
