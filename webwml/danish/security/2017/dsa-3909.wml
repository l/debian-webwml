#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Jeffrey Altman, Viktor Duchovni og Nico Williams identificerede en sårbarhed 
i forbindelse med gensidig autentifikationsomgåelse i samba, SMB/CIFS-fil-, 
print- og loginserveren.  Sårbarheden, der også er kendt som Orpheus' Lyre, er 
i komponenten Samba Kerberos Key Distribution Center (KDC-REP), og kunne 
anvendes af en angriber på netværksstien til at udgive sig for at være en 
server.</p>

<p>Flere oplysninger finder man på sårbarhedswebstedet 
(<a href="https://orpheus-lyre.info/">https://orpheus-lyre.info/</a>) og på 
Samba-projektets websted (<a href="https://www.samba.org/samba/security/CVE-2017-11103.html">\
https://www.samba.org/samba/security/CVE-2017-11103.html</a>)</p>

<p>I den gamle stabile distribution (jessie), er dette problem rettet
i version 2:4.2.14+dfsg-0+deb8u7.</p>

<p>I den stabile distribution (stretch), er dette problem rettet i
version 2:4.5.8+dfsg-2+deb9u1.</p>

<p>I distributionen testing (buster), er dette problem rettet
i version 2:4.6.5+dfsg-4.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2:4.6.5+dfsg-4.</p>

<p>Vi anbefaler at du opgraderer dine samba-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3909.data"
