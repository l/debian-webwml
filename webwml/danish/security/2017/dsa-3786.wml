#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Editor-stavekontrolfiler overført til vim-editoren (Vi IMproved), kunne 
medføre et heltalsoverløb i hukommelsesallokering og et deraf følgende 
bufferoverløb, som potentielt kunne medføre udførelse af vilkårlig kode eller 
lammelsesangreb (denial of service).</p>

<p>I den stabile distribution (jessie), er dette problem
rettet i version 2:7.4.488-7+deb8u2.</p>

<p>I den ustabile distribution (sid), er dette problem
rettet i version 2:8.0.0197-2.</p>

<p>Vi anbefaler at du opgraderer dine vim-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3786.data"
