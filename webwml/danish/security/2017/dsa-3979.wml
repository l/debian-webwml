#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at PyJWT, en Python-implementering af JSON Web Token, udførte 
utilstrækkelig validering af nogle offentlig nøgle-typer, hvilket kunne gøre det 
muligt for en fjernangriber at fabrikere JWT'er helt fra begyndelsen.</p>

<p>I den gamle stabile distribution (jessie), er dette problem rettet
i version 0.2.1-1+deb8u2.</p>

<p>I den stabile distribution (stretch), er dette problem rettet i
version 1.4.2-1+deb9u1.</p>

<p>Vi anbefaler at du opgraderer dine pyjwt-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3979.data"
