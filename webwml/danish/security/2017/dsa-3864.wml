#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at en XML-sårbarhed i forbindelse med eksterne entiteter i 
Apache FOP XML-formatering, kunne medføre informationsafsløring.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 1:1.1.dfsg2-1+deb8u1.</p>

<p>I den kommende stabile distribution (stretch), er dette problem
rettet i version 1:2.1-6.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1:2.1-6.</p>

<p>Vi anbefaler at du opgraderer dine fop-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3864.data"
