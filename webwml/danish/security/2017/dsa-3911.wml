#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Felix Wilhelm opdagede at dokumentvisningsprogrammet Evince benyttede tar på 
en usikker måde, når der blev åbnet tar comic book-arkiver (CBT).  Åbning af et 
ondsindet CBT-arkiv kunne medføre udførelse af vilkårlig kode.  Denne opdatering 
deaktiverer fuldstændig CBT-formatet.</p>

<p>I den gamle stabile distribution (jessie), er dette problem rettet
i version 3.14.1-2+deb8u2.</p>

<p>I den stabile distribution (stretch), er dette problem rettet i
version 3.22.1-3+deb9u1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 3.22.1-4.</p>

<p>Vi anbefaler at du opgraderer dine evince-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3911.data"
