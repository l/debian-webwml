#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at manglende fornuftighedskontrol af inddata i Snoopy, en 
PHP-klasse der simulerer en webbrowser, kunne medføre udførelse af vilkårlige 
kommandoer.</p>

<p>I den gamle stabile distribution (wheezy), er dette problem rettet
i version 2.0.0-1~deb7u1.</p>

<p>I den stabile distribution (jessie), this problem was fixed before
the initial release.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.0.0-1.</p>

<p>Vi anbefaler at du opgraderer dine libphp-snoopy-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3248.data"
