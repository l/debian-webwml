#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>En lammelsesangrebssårbarhed (denial of service) er fundet i Shibboleth (et 
forbundet identitetsframework) Service Provider.  Når der blev behandlet visse 
misdannede SAML-meddelelser, genereret af en autentificeret angriber, kunne 
dæmonen gå ned.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 2.4.3+dfsg-5+deb7u1.</p>

<p>I den kommende stabile distribution (jessie), er dette problem
rettet i version 2.5.3+dfsg-2.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.5.3+dfsg-2.</p>

<p>Vi anbefaler at du opgraderer dine shibboleth-sp2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3207.data"
