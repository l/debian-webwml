#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Thorsten Eckel fra Znuny GMBH og Remo Staeuble fra InfoGuard opdagede en 
rettighedsforøgelsessårbarhed i otrs2, Open Ticket Request System.  En angriber 
med gyldige OTRS-loginoplysninger kunne tilgå og ændre ticketdata hørende til 
andre brugere via GenericInterface, hvis en ticketwebservice var opsat og ikke 
yderligere sikret.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i
version 3.1.7+dfsg1-8+deb7u5.</p>

<p>I den kommende stabile distribution (jessie), er dette problem
rettet i version 3.3.9-3.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 3.3.9-3.</p>

<p>Vi anbefaler at du opgraderer dine otrs2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3124.data"
