#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at rebinding af en modtager af en direkte metode-handle, kunne 
gøre det muligt at tilgå en beskyttet metode.</p>

<p>I den gamle stabile distribution (wheezy), er dette problem rettet
i version 7u91-2.6.3-1~deb7u1.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 7u91-2.6.3-1~deb8u1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 7u91-2.6.3-1.</p>

<p>Vi anbefaler at du opgraderer dine openjdk-7-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3401.data"
