#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Man opdagede at cURL, et bibliotek til URL-overførsler, når opsat til at 
anvende en proxyserver med HTTPS-protokollen, som standard sendte de samme 
HTTP-headere, som den sender til målserven, til proxyserveren, og dermed 
muligvis medførende en lækage af følsomme oplysninger.</p>

<p>I den stabile distribution (jessie), er dette problem rettet i
version 7.38.0-4+deb8u2.</p>

<p>I distributionen testing (stretch), vil dette problem blive rettet i 
version 7.42.1-1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 7.42.1-1.</p>

<p>Vi anbefaler at du opgraderer dine curl-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3240.data"
