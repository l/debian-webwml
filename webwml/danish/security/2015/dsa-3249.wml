#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Shadowman131 opdagede at jqueryui, et JavaScript UI-bibliotek til dynamiske 
webapplikationer, ikkek fik fornuftighedskontrolleret sin 
<q>title</q>-valgmulighed.  Dermed havde fjernangribere mulighed for at 
indsprøjte vilkårlig kode gennem udførelse af skripter på tværs af 
websteder.</p>

<p>I den gamle stabile distribution (wheezy), er dette problem rettet
i version 1.8.ooops.21+dfsg-2+deb7u1.</p>

<p>I den stabile distribution (jessie), distributionen testing (stretch) og i 
den ustabile distribution (sid), er dette problem rettet i version 
1.10.1+dfsg-1.</p>

<p>Vi anbefaler at du opgraderer dine jqueryui-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3249.data"
