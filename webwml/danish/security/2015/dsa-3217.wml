#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>Jann Horn opdagede at verifikationen af kildekodeintegritet i dpkg-source 
kunne omgås gennem en særligt fremstillet Debian-kildekodekontrolfil (.dsc).  
Bemærk at fejlen kun påvirker udpakning af lokale Debian-kildekodepakker gennem 
dpkg-source, men ikke installeringen af pakker fra Debians arkiv.</p>

<p>I den stabile distribution (wheezy), er dette problem rettet i version 
1.16.16.  Opdateringen indeholder også ikke-sikkerhedsrelevante ændringer, som 
oprindelig var planlagt til den næste punktopdatering af wheezy.  Se Debians 
changelog for flere oplysninger.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.17.25.</p>

<p>Vi anbefaler at du opgraderer dine dpkg-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3217.data"
