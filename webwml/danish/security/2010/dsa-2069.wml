<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at ZNC, en IRC-bouncer, var sårbar over for lammelsesangreb 
(denial of service) gennem en NULL-pointer-dereference, når der blev bedt om 
trafikstatistik, mens der var en uautoriseret forbindelse.</p>

<p>I den stabile distribution (lenny), er problemet rettet i version 
0.058-2+lenny4.</p>

<p>I distributionen testing (squeeze) og i den ustabile distribution (sid), er 
problemet rettet i version 0.090-2.</p>

<p>Vi anbefaler at du opgraderer dine znc-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2010/dsa-2069.data"
#use wml::debian::translation-check translation="1.2" mindelta="1"
