<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>En er fundet i wireshark, et program til analysering af 
netværksprotokoller.</p>

<p>Man opdagede at ASN.1 BER-dissektoren var sårbar over for et stakoverløb,
hvilket fik applikationen til at gå ned.</p>

<p>I den stabile distribution (lenny), er problemet rettet i version 
1.0.2-3+lenny11.</p>

p>I distributionen testing (squeeze) og i den ustabile distribution (sid), er 
dette problem rettet i version 1.2.11-3.</p>

<p>Vi anbefaler at du opgraderer dine wireshark-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2010/dsa-2127.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
