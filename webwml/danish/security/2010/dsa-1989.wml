<define-tag description>lammelsesangreb</define-tag>
<define-tag moreinfo>
<p>Dan Rosenberg opdagede en racetilstand i FUSE, Filesystem in USErspace (et 
filsystem i brugerrummet).  En lokal angriber, med adgang til at anvende FUSE, 
kunne unmounte vilkårlige steder, førende til et lammelsesangreb (denial of 
service).</p>

<p>I den gamle stabile distribution (etch), er dette problem rettet i
version 2.5.3-4.4+etch1.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i
version 2.7.4-1.1+lenny1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
2.8.1-1.2, og vil om kort tid blive overført til distributionen testing 
(squeeze).</p>

<p>Vi anbefaler at du opgraderer dine fuse-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2010/dsa-1989.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
