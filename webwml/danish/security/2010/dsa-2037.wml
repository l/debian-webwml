<define-tag description>race-tilstand</define-tag>
<define-tag moreinfo>
<p>Sebastian Krahmer opdagede at en race-tilstand i KDE Desktop Environments 
KDM display manager, tillod at en lokal bruger kunne forøge rettigheder til 
root.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i version
4:3.5.9.dfsg.1-6+lenny1.</p>

<p>I den ustabile distribution (sid), vil dette problem snart blive rettet.</p>

<p>Vi anbefaler at du opgraderer din kdm-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2010/dsa-2037.data"
#use wml::debian::translation-check translation="1.1" mindelta="1"
