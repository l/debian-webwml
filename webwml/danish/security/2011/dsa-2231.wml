#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>udførelse af skripter på tværs af websteder</define-tag>
<define-tag moreinfo>
<p>Flere sårbarheder i forbindelse med udførelse af skripter på tværs af 
websteder blev fundet i Open Ticket Request System (OTRS), et 
problemsporingssystem. 
(<a href="http://security-tracker.debian.org/tracker/CVE-2011-1518">CVE-2011-1518</a>)</p>

<p>Desuden indeholder denne opdatering en rettelse af en fejl ved opgradering af
pakken fra lenny til squeeze.</p>

<p>Den gamle stabile distribution (lenny) er ikke påvirket at dette problem.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 2.4.9+dfsg1-3+squeeze1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.4.10+dfsg1-1.</p>

<p>Vi anbefaler at du opgraderer dine otrs2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2231.data"
