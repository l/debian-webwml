#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Witold Baryluk opdagede at MaraDNS, en simpelt sikkerhedsfokuseret Domain 
Name System-server, havde en intern buffer som måske kunne løbe over, når der
blev behandlet forespørgsler med et stort antal etiketter, forårsagende et 
servernedbrud og efterfølgende lammelsesangreb (denial of service).</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i version 
1.3.07.09-2.1.</p>

<p>I den stabile distribution (squeeze) og større er problemet allerede rettet i
version 1.4.03-1.1.</p>

<p>Vi anbefaler at du opgraderer dine maradns-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2196.data"
