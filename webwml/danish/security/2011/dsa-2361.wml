#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at ChaSen, et japansk morfologisk analysesystem, indeholdt et 
bufferoverløb, potentielt førende til udførelse af vilkårlig kode i programmer,
der anvender biblioteket.</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i
version 2.4.4-2+lenny2.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 2.4.4-11+squeeze2.</p>

<p>Vi anbefaler at du opgraderer dine chasen-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2361.data"
