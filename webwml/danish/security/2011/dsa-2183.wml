#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at en regression i forbindelse med et bufferoverløb 
(<a href="http://security-tracker.debian.org/tracker/CVE-2005-3534">CVE-2005-3534</a>) 
i NBD, Network Block Device-serveren, kunne gøre det muligt at udføre vilkårlig
kode på NBD-serveren via en stor forespørgsel.</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i
version 1:2.9.11-3lenny1.</p>

<p>Den stabile distribution (squeeze), distributionen testing (wheezy) og den 
ustabile distribution (sid) er ikke påvirkede.  Problemet blev rettet før 
udgivelsen af squeeze, i version 1:2.9.16-8.</p>

<p>Vi anbefaler at du opgraderer dine nbd-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2183.data"
