#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>manglende kontrol af inddata</define-tag>
<define-tag moreinfo>
<p>Dan Rosenberg opdagede at utilstrækkelig kontrol af inddata i VLC's 
behandling af Matroska/WebM-containere kunne føre til udførelse af vilkårlig 
kode.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i version 
1.1.3-1squeeze3.</p>

<p>Versionen af vlc i den gamle stabile distribution (lenny) er påvirket af 
yderligere problemer, som vil blive løst i en opfølgende bulletin.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
1.1.7-1.</p>

<p>Vi anbefaler at du opgraderer dine vlc-pakker.</p>

</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2159.data"
