#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Chris Evans opdagede at libxml var sårbar over for bufferoverløb, hvilket 
gjorde det muligt, ved hjælp af en fabrikeret XML-inddatafil, potentielt at 
udføre vilkårlig kode.</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i
version 2.6.32.dfsg-5+lenny4.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 2.7.8.dfsg-2+squeeze1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 2.7.8.dfsg-3.</p>

<p>Vi anbefaler at du opgraderer dine libxml2-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2255.data"
