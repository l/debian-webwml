#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>David Wheeler opdagede et bufferoverløb i ldns's kode til fortolkning af 
RR-poster, hvilket kunne føre til udførelse af vilkårlig kode.</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i
version 1.4.0-1+lenny2.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 1.6.6-2+squeeze1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.6.11-1.</p>

<p>Vi anbefaler at du opgraderer dine ldns-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2353.data"
