#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Flere sårbarheder blev fundet i Mantis, et webbaseret fejlsporingssystem:
Utilstrækkelig kontrol af inddata kunne medføre lokal filinkludering og 
udførelse af skripter på tværs af websteder.</p>

<p>I den gamle stabile distribution (lenny), er dette problem rettet i
version 1.1.6+dfsg-2lenny6.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 1.1.8+dfsg-10squeeze1.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 1.2.7-1.</p>

<p>Vi anbefaler at du opgraderer dine mantis-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2308.data"
