#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>flere sårbarheder</define-tag>
<define-tag moreinfo>
<p>Joernchen fra Phenoelit opdagede flere sårbarheder i Redmine, en 
webprojekthåndteringsapplikation:</p>

<ul>

  <li>Indloggede brugere kunne måske have mulighed for at tilgå private 
  data.</li>

  <li>Textile-formateringen gjorde det muligt at udføre skripter på tværs af
  servere, hvorved følsomme data blev blotlagt for en angriber.</li>

  <li>Bazaar-arkivadapteren kunne anvendes fra en fjern maskine til at udføre 
  kommandoer på værtsmaskinen, der kører Redmine.</li>

</ul>

<p>Den gamle stabile distribution (lenny) indeholder ikke redmine-pakker.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 1.0.1-2.</p>

<p>I distributionen testing (wheezy) og i den ustabile distribution (sid),
er dette problem rettet i version 1.0.5-1.</p>

<p>Vi anbefaler at du opgraderer dine redmine-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2261.data"
