#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>bufferoverløb</define-tag>
<define-tag moreinfo>
<p>Man opdagede at PostgreSQL's intarray-contrib-modul ikke på korrekt vis 
håndterede heltal med et stort antal cifre, hvilket førte til et servernedbrud
og potentielt udførelse af vilkårlig kode.</p>

<p>I den stabile distribution (lenny), er dette problem rettet i version 
8.3.14-0lenny1 af pakken postgresql-8.3.</p>

<p>I distributionen testing (squeeze), er dette problem rettet i version 
8.4.7-0squeeze1 af pakken postgresql-8.4.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i version 
8.4.7-1 af pakken postgresql-8.4 og i version 9.0.3-1 af pakken 
postgresql-9.0.</p>

<p>Opdateringen indeholder også pålidelighedsforbedringer; for flere oplysninger
henvises til de respektive changelog-filer.</p>

<p>Vi anbefaler at du opgraderer dine PostgreSQL-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2157.data"
