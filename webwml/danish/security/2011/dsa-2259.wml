#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>autentificeringsomgåelse</define-tag>
<define-tag moreinfo>
<p>Man opdagede at F*EX, en webservice til overførsel meget store filer, ikke 
validererede autentificerings-id'er tilstrækkeligt.  Mens servien på korrekt vis
validerede eksisterende autentificerings-id'er, kunne en angirber, der ikke 
angav en specifik autenficierings-id overhovedet, omgå 
autentificeringsproceduren.</p>

<p>Den gamle stabile distribution (lenny) indeholder ikke fex.</p>

<p>I den stabile distribution (squeeze), er dette problem rettet i
version 20100208+debian1-1+squeeze1.</p>

<p>I distributionen testing (wheezy), vil dette problem snart blive rettet.</p>

<p>I den ustabile distribution (sid), er dette problem rettet i
version 20110610-1.</p>

<p>Vi anbefaler at du opgraderer dine fex-pakker.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2011/dsa-2259.data"
