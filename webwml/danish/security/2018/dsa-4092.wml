#use wml::debian::translation-check translation="1.1" mindelta="1"
<define-tag description>sikkerhedsopdatering</define-tag>
<define-tag moreinfo>
<p>cPanel Security Team opdagede at awstats, et program til analysering af 
logfiler, var sårbar overfor mappegennemløbsangreb.  En uautentificeret 
fjernangriber kunne bruge det som løftestang til at udføre vilkårlig kode.</p>

<p>I den gamle stabile distribution (jessie), er dette problem rettet
i version 7.2+dfsg-1+deb8u1.</p>

<p>I den stabile distribution (stretch), er dette problem rettet i
version 7.6+dfsg-1+deb9u1.</p>

<p>Vi anbefaler at du opgraderer dine awstats-pakker.</p>

<p>For detaljeret sikkerhedsstatus vedrørende awstats, 
se dens sikkerhedssporingsside på: 
<a href="https://security-tracker.debian.org/tracker/awstats">\
https://security-tracker.debian.org/tracker/awstats</a></p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2018/dsa-4092.data"
