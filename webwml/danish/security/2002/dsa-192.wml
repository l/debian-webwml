<define-tag description>udførsel af vilkårlig kode</define-tag>
<define-tag moreinfo>
<p>SuSEs sikkerhedsteam har fundet en sårbarhed i html2ps, et program til
konvertering fra HTML til PostScript, som åbnede filer baseret på 
ukontrollerede inddata, på en usikker måde.  Problemet kan udnyttes når html2ps 
er installeret som et filter i lrpng og angriberen tidligere har fået adgang
til lp-kontoen.</p>

<p>Disse problemer er rettet i version 1.0b3-1.1 i den aktuelle stabile
distribution (woody), i version 1.0b1-8.1 i den gamle stabile distribution 
(potato) og i version 1.0b3-2 i den ustabile distribution (sid).</p>

<p>Vi anbefaler at du opgraderer din html2ps-pakke.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2002/dsa-192.data"
#use wml::debian::translation-check translation="1.1"
