#use wml::debian::template title="Start fra disketter og installér over netværket"
#use wml::debian::translation-check translation="1.5"

<p>Du kan starte Debians installationssystem fra disketter og dernæst fortsætte
med at installere resten af Debian over netværket.</p>

<ul>
  <li>Hent diskette-aftryksfilerne fra kataloget
      <tt>/dists/stable/main/disks-XXXX/</tt> på et af
      <a href="ftplist">Debians ftp-filspejle</a>; hvor du udskifter 
      "<tt>XXXX</tt>" med din computers arkitektur, for eksempel "i386" eller 
      "alpha".
      Du kan enten nøjes med blot at hente de filer du har brug for, som 
      beskrevet i 
      <a href="$(HOME)/releases/stable/i386/ch-install-methods#s-file-descs">\
      installationshåndbogen</a>, eller hente hele træet.
# NB: the location of that section has changed in woody to
# ch-appendix#s-file-descs
  </li>
  <li>Skriv aftrykkene til disketter, jævnfør instruktionerne i 
      <a href="$(HOME)/releases/stable/i386/ch-install-methods#s-install-floppies">\
      installationshåndbogen</a>.
# NB: the anchor within the file has changed in woody, we'll need to
# change the above to #s-create-floppy
  </li>
  <li>Start fra rescue-disketten ved at følge instruktionerne i
      <a href="$(HOME)/releases/stable/i386/ch-rescue-boot#s-features-rescue">\
      installationshåndbogen</a>.
  </li>
  <li>Når installationsprocessen beder dig om filerne, skal du vælge metoden
      "Network" (netværk) for at kunne etablere en forbindelse med et af 
      Debians filspejle, som filerne skal hentes fra.
# NB: the location of that section has changed in woody to
# ch-install-system#s-install-os-netfetch
  </li>
</ul>

<p>Læs <a href="$(HOME)/releases/stable/installmanual">\
installationshåndbogen til din arkitektur</a> for flere oplysninger.</p>

# NB: not sure installing the base system over the network is supported in
# potato, the above wording might indicate otherwise!

# NB2: it might be that the installation manuals for other architectures
# explain differences with dealing with diskettes, someone should probably
# investigate if that is so and place additional links
