#use wml::debian::template title="Debians fejlrapporteringssystem - mailserverens referencekort" NOHEADER=yes NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.17"

<h1>Mailserverens referencekort</h1>

<p>På Internet finder man den komplette dokumentation af mailserveren, i 
filerne <a href="server-request">bug-log-mailserver.txt</a> og 
<a href="server-control">bug-maint-mailcontrol.txt</A> eller ved at sende ordet
<code>help</code> til hver mailserver.</p>

<h2>Sammendrag af tilgængelige kommandoer på 
<code>request@bugs.debian.org</code></h2>

<ul compact>
<li><code>send</code> <var>fejlnummer</var>
<li><code>send-detail</code> <var>fejlnummer</var>
<li><code>index</code> [<code>full</code>]
<li><code>index-summary by-package</code>
<li><code>index-summary by-number</code>
<li><code>index-maint</code>
<li><code>index maint</code> <var>udviklerdelstreng</var>
<li><code>index-packages</code>
<li><code>index packages</code> <var>pakkedelstreng</var>
<li><code>send-unmatched</code> [<code>this</code>|<code>0</code>]
<li><code>send-unmatched</code> <code>last</code>|<code>-1</code>
<li><code>send-unmatched</code> <code>old</code>|<code>-2</code>
<li><code>getinfo</code> <var>filnavn</var> <em>(ftp.debian.org/debian/doc/*)</em>
<li><code>help</code>
<li><code>refcard</code>
<li><code>quit</code>|<code>stop</code>|<code>thank</code>...|<code>--</code>...
<li><code>#</code>... <em>(kommentar)</em>
<li><code>debug</code> <var>niveau</var>
</ul>

<h2>Sammendrag af tilgængelige ekstrakommandoer på kontrolserveren</h2>

<ul compact>
<li><code>reassign</code> <var>fejlnummer</var> <var>pakke</var>
<li><code>severity</code> <var>fejlnummer</var> <var>alvorsgrad</var>
<li><code>reopen</code> <var>fejlnummer</var>
 [ <var>afsenderadresse</var> | <code>=</code> | <code>!</code> ]
<li><code>forwarded</code> <var>fejlnummer</var> <var>adresse</var>
<li><code>notforwarded</code> <var>fejlnummer</var>
<li><code>retitle</code> <var>fejlnummer</var> <var>ny_titel</var>
<li><code>clone</code> <var>fejlnummer</var> [ <var>nye id'er</var> ]
<li><code>merge</code> <var>fejlnummer</var> <var>fejlnummer</var> ...
<li><code>unmerge</code> <var>fejlnummer</var>
<li><code>tags</code> <var>fejlnummer</var>
 [ <code>+</code> | <code>-</code> | <code>=</code> ] <var>mærke</var>
<li><code>close</code> <var>fejlnummer</var>
 <em>(anvendelse frarådes &ndash; du skal særskilt fortælle afsenderen hvorfor)</em>
</ul>

<p><code>reopen</code> med <code>=</code> eller uden afsenderadresse beholder
afsenderen som den oprindelige afsender af rapporten; <code>!</code> sætter den
til dig, personen udfører reopen-kommandoen.</p>

<p>Alvorsgraderne er: <code>critical</code>, <code>grave</code>,
<code>serious</code>, <code>important</code>, <code>normal</code>, 
<code>minor</code> og <code>wishlist</code>.</p>

<p>Tilgængelige mærker er pt. <code>patch</code>, <code>wontfix</code>, 
<code>moreinfo</code>, <code>unreproducible</code>, <code>fixed</code>, 
<code>security</code>, <code>potato</code>, <code>woody</code> og 
<code>sid</code>.</p>

<hr>

#use "otherpages.inc"
#use "$(ENGLISHDIR)/Bugs/footer.inc"
