#use wml::debian::template title="Debian GNU/FreeBSD" BARETITLE="true"
#use wml::debian::translation-check translation="1.5"
#include "$(ENGLISHDIR)/ports/freebsd/menu.inc"

<h1>Debian GNU/FreeBSD</h1>

<p>Pt. er der to forskellige forsøg på at fremstille en Debian-distribution
baseret på FreeBSDs kerne.  Der arbejdes på begge på et eksperimental stade og
vi har ikke helt bestemt os for, hvilken af dem der vil blive den officielle
Debian GNU/FreeBSD.  Se de respektive tilpasningssider for statusoplysninger
om hver tilpasning:</p>

<p>Debian GNU/kFreeBSD <a href="gnu-libc-based">baseret på GNU C-biblioteket 
(Glibc)</a></p>

<p>Debian GNU/FreeBSD <a href="bsd-libc-based">baseret på BSD C-biblioteket 
(libc5)</a>.</p>
