# From: panx <infos@panx.net>

<define-tag pagetitle>Panx Production, France</define-tag>
<define-tag webpage>http://www.gn.apc.org/</define-tag>

#use wml::debian::users

<p>
    We use Debian for web (apache), ftp (wu-ftp), mail (sendmail), icecast 
    servers, firewall/nat, web development (perl), workstation (gnome).
</p>

<p>
    Why?  Because it works! (A great operating system) but also because of the 
    Debian Social Contract!
</p>
