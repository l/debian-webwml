<define-tag moreinfo>
Fixes: ldso-1.8.11 or later
</define-tag>
<define-tag description>Local users may gain root privileges by exploiting a buffer overflow in the dynamic linker (ld.so).</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/undated/1ldso.data'
