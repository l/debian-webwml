<define-tag moreinfo>libpam-smb contains a buffer overflow that can be used to
execute arbitrary commands with root privilege. libpam-smb was not shipped with
Debian 2.1 (slink), but was included in Debian 2.2 (potato).

<p>A fixed version of libpam-smb is available in version 1.1.6-1 for Debian 2.2
(potato). We recommend upgrading your libpam-smb immediately.</define-tag>
<define-tag description>remote root exploit</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2000/20000911.data'
