<define-tag description>security update</define-tag>
<define-tag moreinfo>
<p>Jodie Cunningham discovered multiple vulnerabilities in freexl, a
library to read Microsoft Excel spreadsheets, which might result in
denial of service or the execution of arbitrary code if a malformed Excel
file is opened.</p>

<p>For the stable distribution (wheezy), these problems have been fixed in
version 1.0.0b-1+deb7u1.</p>

<p>For the upcoming stable distribution (jessie), these problems have been
fixed in version 1.0.0g-1+deb8u1.</p>

<p>For the unstable distribution (sid), these problems have been fixed in
version 1.0.0g-1+deb8u1.</p>

<p>We recommend that you upgrade your freexl packages.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3208.data"
# $Id$
