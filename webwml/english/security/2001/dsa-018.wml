<define-tag moreinfo>PkC have found a heap overflow in tinyproxy that could be remotely exploited.  An attacker could gain a shell (user nobody) remotely.

<p>We recommend you upgrade your tinyproxy package immediately.</define-tag>
<define-tag description>remote nobody exploit</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-018.data'
