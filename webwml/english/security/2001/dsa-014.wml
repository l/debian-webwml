<define-tag moreinfo>It was reported recently that splitvt is vulnerable to
numerous buffer overflow attack and a format string attack. An attacker was
able to gain access to the root user id.

<p>We recommend you upgrade your splitvt package immediately.</define-tag>
<define-tag description>buffer overflow and format string attack</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-014.data'
