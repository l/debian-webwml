<define-tag moreinfo>PkC has reported that there is a buffer overflow in
sprintf() in micq versions 0.4.6 and previous, that allows to a remote attacker
able to sniff packets to the ICQ server to execute arbitrary code on the victim
system.

<p>We recommend you upgrade your micq package immediately.</define-tag>
<define-tag description>remote buffer overflow</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-012.data'
