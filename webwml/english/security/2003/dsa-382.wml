<define-tag description>possible remote vulnerability</define-tag>
<define-tag moreinfo>
<p>A bug has been found in OpenSSH's buffer handling where a buffer could
be marked as grown when the actual reallocation failed.</p>

<p>This bug has been fixed in upstream version 3.7. For the Debian stable
distribution this bug has been fixed in version 1:3.4p1-1.1.</p>

<p>Please note that if a machine is setup to install packages from
proposed-updates it will not automatically install this update.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-382.data"
# $Id$
