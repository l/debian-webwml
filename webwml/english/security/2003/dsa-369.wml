<define-tag description>buffer overflow</define-tag>
<define-tag moreinfo>
<p>Steve Kemp discovered a buffer overflow in zblast-svgalib, when saving
the high score file.  This vulnerability could be exploited by a local
user to gain gid 'games', if they can achieve a high score.</p>

<p>For the current stable distribution (woody) this problem has been fixed
in version 1.2pre-5woody2.</p>

<p>For the unstable distribution (sid) this problem is fixed in version
1.2.1-7.</p>

<p>We recommend that you update your zblast package.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-369.data"
# $Id$
