<define-tag moreinfo>We have received reports that the version of mirror as
distributed in Debian GNU/Linux 2.1 could be remotely exploited. When mirroring
a remote site the remote site could use filename-constructions like &quot;
..&quot; that would case mirror to work one level above the target directory
for the mirrored files.</define-tag>
<define-tag description>Incorrect directory name handling in mirror</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1999/19991018.data'
