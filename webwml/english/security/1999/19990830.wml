<define-tag moreinfo>Cron sends mail as root without checking the parameters
passed to sendmail on the command line. This can lead to a root compromise. We
recommend upgrading your cron immediately.</define-tag>
<define-tag description>Root exploit in cron</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1999/19990830.data'
