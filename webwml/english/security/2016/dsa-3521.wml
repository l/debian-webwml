<define-tag description>security update</define-tag>
<define-tag moreinfo>
<p>Lael Cellier discovered two buffer overflow vulnerabilities in git, a
fast, scalable, distributed revision control system, which could be
exploited for remote execution of arbitrary code.</p>

<p>For the oldstable distribution (wheezy), these problems have been fixed
in version 1:1.7.10.4-1+wheezy3.</p>

<p>For the stable distribution (jessie), these problems have been fixed in
version 1:2.1.4-2.1+deb8u2.</p>

<p>For the unstable distribution (sid), these problems have been fixed in
version 1:2.8.0~rc3-1. 
<a href="https://security-tracker.debian.org/tracker/CVE-2016-2315">CVE-2016-2315</a>
was already fixed in version 1:2.7.0-1.</p>

<p>We recommend that you upgrade your git packages.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3521.data"
# $Id$
