#use wml::debian::template title="Schritt 7: Überprüfung des Debian-Verwalters der Konten und Konto-Erstellung" NOHEADER="true"
#include "$(ENGLISHDIR)/devel/join/nm-steps.inc"
#use wml::debian::translation-check translation="1.3"
# $Id$
# Translator: Helge Kreutzmann, <kreutzm@itp.uni-hannover.de>, 2005-09-30

<p>Die Informationen auf dieser Seite, obwohl öffentlich, sind
   primär für zukünftige Debian-Entwickler interessant.</p>

<h2>Schritt 7: Überprüfung des Debian-Verwalters der Konten und Konto-Erstellung</h2>

<p>Der <a href="./newmaint#DAM">Debian-Verwalter der Konten</a> hat die 
   abschließende Entscheidung über die Erstellung des Kontos zu fällen. Falls
   irgendwelche größeren Probleme in der Bewerbung gefunden werden, wird sie
   entweder direkt abgelehnt, oder zurück an die <a 
   href="./newmaint#FrontDesk">Rezeption</a> gegeben, die entscheidet, wie die
   Bewerbung vervollständigt werden kann.
</p>

<p>Es gibt einige Probleme, die immer zu einer Ablehnung führen:</p>
<ul>
 <li>Versäumnis, adäquate Identifikation bereitzustellen</li>
 <li>Versäumnis, unseren Prinzipien und Prozeduren zuzustimmen</li>
 <li>Versäumnis, die Aufgaben zu beenden, denen der Bewerber mit dem 
     Bewerbungsleiter zu erledigen zugestimmt hatte</li>
</ul>
	    
<p>Wenn die <a href="./newmaint#DAM">Debian-Verwalter der Konten</a> mit der
   kompletten Bewerbung zufrieden-gestellt sind, werden die notwendigen Konten
   für den Bewerber erstellt und die debian-private Mailingliste für den 
   Bewerber abonniert.
</p>

<p>Sobald alle abschließenden Details der Mitgliedschaft von den <a
   href="./newmaint#DAM">Debian-Verwalter der Konten</a> beendet wurden wird
   der Bewerber, sein AM und die debian-project-Mailingliste informiert.
</p>

<hr />
#include "$(ENGLISHDIR)/devel/join/nm-steps.inc"
