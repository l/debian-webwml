#use wml::debian::template title="Debian-Installer - Errata"
#use wml::debian::recent_list
#use wml::debian::translation-check translation="1.173"
# $Id$
# Original-Translator: Frank Lichtenheld <frank@lichtenheld.de>, 2003-11-11
# Translator: Helge Kreutzmann <debian@helgefjell.de>, 2005-11-12 ff.
# Updated: Holger Wansing <linux@wansing-online.de>, 2012

<h1 id="squeeze-beta2">Errata für die Wheezy Beta3-Veröffentlichung</h1>

<p>
Dies ist eine Liste der bekannten Probleme in der Wheezy Beta3-Veröffentlichung des
Debian-Installers. Falls Sie Ihr Problem hier nicht aufgeführt finden,
senden Sie uns bitte einen
<a href="$(HOME)/releases/stable/i386/ch05s04.html#submit-bug">Installationsbericht</a>,
der das Problem beschreibt.
</p>

<dl class="gloss">
  <dt>CD1 ohne Netzwerkspiegel nicht ausreichend, um eine vollständige
      Desktop-Umgebung zu installieren</dt>
  <dd>
      Die Standard-Umgebung (GNOME) scheint nicht mehr unter diesem Problem
      zu leiden.</dd>

  <dt>Netzwerk-Probleme, wenn ein Arbeitsplatzsystem über eine drahtlose
      Funkverbindung installiert wird</dt>
  <dd>
      Wird ein Arbeitsplatzsystem über eine drahtlose Funkverbindung installiert,
      funktioniert die network-manager-Komponente auf dem neu installierten
      System möglicherweise nicht direkt ohne Nachhilfe
      (<a href="http://bugs.debian.org/682608">#682608</a>). Dies Problem kann
      eventuell umgangen werden, indem die Zeilen für wlan0 in der Datei
      /etc/network/interfaces auskommentiert werden.</dd>

  <dt>Einrichtung des Netzwerks schlägt beim Start des neu installieren Systems
      offenbar fehl, wenn die GNOME-Arbeitsplatzumgebung installiert wird</dt>

  <dd>
      Network-manager deaktiviert in /etc/network/interfaces die Konfiguration
      der Netzwerkschnittstelle eth0. Die Implementierung des Skripts, das die
      Anpassungen durchführt, ist sehr blauäugig und kann nicht mit den neuen
      privacy-Erweiterungen umgehen, die von netcfg gesetzt werden. Wenn
      network-manager während der Installation installiert wurde, weil z.B.
      die GNOME-Arbeitsplatzumgebung ausgewählt war, müssen Sie in
      /etc/network/interfaces die Zeile mit dem Inhalt <q>privext 2</q>
      auskommentieren, um die Warnungen loszuwerden, die von ifupdown während
      des Boot-Prozesses ausgegeben werden. Das Netzwerk wird trotzdem
      korrekt von network-manager eingerichtet.
  </dd>

 <!-- leaving this in for possible future use...
     <dt>i386: mehr als 32&nbsp;MB an Speicher werden zur Installation benötigt</dt>
     <dd>
        Die für eine erfolgreiche Installation auf i386 benötigte Minimalmenge an
        Speicher beträgt 48&nbsp;MB, anstatt der bisherigen 32&nbsp;MB. Wir hoffen, die
        Anforderungen später wieder auf 32&nbsp;MB zu reduzieren. Die
        Speicheranforderungen können sich auch für andere Architekturen geändert
        haben.
         </dd>

  <dt>i386: mehrere Probleme</dt>
  <dd>
     In der i386-Portierung gibt es in dieser Veröffentlichung mehrere bekannte
     Probleme:
     <ul>
       <li>Aufgrund gestiegener Größe des Kernels sind wir nicht in der
     Lage, Installations-Images für Installationen von Diskette bereitzustellen.
       </li>
    </ul>
 </dd>
  <dt>Partitionen mit mehr als 16 TiB werden von Ext4 nicht unterstützt</dt>
  <dd>
     Das Werkzeug zum Erzeugen von Ext4-Dateisystemen erlaubt nicht die Erstellung
     von Dateisystemen mit einer Größe von über 16 TiB.
 </dd>

  <dt>S390: Nichtunterstützte Funktionalitäten</dt>
  <dd>
     <ul>
        <li>Die Unterstützung für DASD DIAG-Disziplinen ist derzeit nicht
            verfügbar.</li>
      </ul>
 </dd>

  <dt>CDs mit enthaltener nicht-freier Firmware funktionieren nicht</dt>
  <dd>
     <ul>
        <li>CDs, die nicht-freie Firmware enthalten, funktionieren derzeit
            nicht, da die CD vmlinux für den Linux-Kernel 2.6.32 mitbringt,
            aber udebs für Linux-Kernel 3.2.0.</li>
     </ul>
  </dd>

-->

</dl>
