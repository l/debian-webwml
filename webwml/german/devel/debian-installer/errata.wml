#use wml::debian::template title="Debian-Installer-Errata"
#use wml::debian::recent_list
#use wml::debian::translation-check translation="1.130"
# $Id$
# Original-Translator: Frank Lichtenheld <frank@lichtenheld.de>, 2003-11-11
# Translator: Helge Kreutzmann <debian@helgefjell.de>, 2005-11-12 ff.

<h1 id="squeeze-alpha1">Errata für Squeeze-Veröffentlichung Alpha1</h1>

<p>
Dies ist eine Liste der bekannten Probleme in der Squeeze-Alpha1-Veröffentlichung des
Debian-Installers. Falls Sie Ihr Problem hier nicht aufgeführt finden,
senden Sie uns bitte einen <a href="report-template">\
Installationsbericht</a>, der das Problem beschreibt.
</p>

<dl class="gloss">
  <dt>Selbst-Aufbau von RAID-Verbünden kann im Rettungsmodus Daten beschädigen</dt>
  <dd>
     Der Rettungsmodus sollte beim Einsatz von Software-RAID auf dem zu 
     rettenden System mit großer Vorsicht verwandt werden, da die Skripte des
     Rettungsmodus RAID-Verbünde automatisch aufbauen. Dies könnte Daten
     beschädigen, falls ungültige oder veraltete RAID-Superblöcke vorhanden
     sind.
 </dd>
  <dt>Fehlerhafte Router können zu Netzproblemen führen</dt>
  <dd>
      Falls bei Ihnen während der Installation Netzprobleme auftreten, könnte
      dies an Routern irgendwo zwischen Ihnen und dem Debian-Spiegel liegen, die
      nicht korrekt mit <q>Window Scaling</q> umgehen können. Lesen Sie hierzu
      <a href="http://bugs.debian.org/401435">#401435</a> und diesen <a 
      href="http://kerneltrap.org/node/6723">Kerneltrap-Artikel</a> für weitere
      Details.<br />
      Sie können dieses Problem umschiffen, indem Sie TCP-Window-Scaling 
      deaktivieren. Öffnen Sie eine Shell und geben Sie den folgenden Befehl
      ein:<br />
      <tt>echo 0 &gt; /proc/sys/net/ipv4/tcp_window_scaling</tt><br />
      Im installierten System sollten Sie das TCP-Window-Scaling wohl besser
      nicht komplett deaktivieren. Der folgende Befehl setzt einen Bereich
      zum Lesen und Schreiben, der mit fast allen Routern funktionieren 
      sollte:<br />
      <tt>echo 4096 65536 65536 &gt;/proc/sys/net/ipv4/tcp_rmem</tt><br />
      <tt>echo 4096 65536 65536 &gt;/proc/sys/net/ipv4/tcp_wmem</tt>

  <dt>Ländernamen auf Englisch nicht korrekt sortiert</dt>
  <dd>
     Während des Auswahl des Spiegels werden die Ländernamen nach Ländercode
     und nicht nach Ländernamen sortiert. Dadurch kann es schwierig werden,
     einige Länder in der Liste zu finden. Dies betrifft nur Installationen
     auf Englisch.
 </dd>

 <!-- leaving this in for possible future use...
     <dt>i386: mehr als 32&nbsp;MB an Speicher werden zur Installation benötigt</dt>
     <dd>
        Die für eine erfolgreiche Installation auf i386 benötigte Minimalmenge an
        Speicher beträgt 48&nbsp;MB, anstatt der bisherigen 32&nbsp;MB. Wir hoffen, die
        Anforderungen später wieder auf 32&nbsp;MB zu reduzieren. Die
        Speicheranforderungen können sich auch für andere Architekturen geändert
        haben.
         </dd>
-->
  <dt>Einige USB-Tastaturen könnten nicht funktionieren</dt>
  <dd>
     Einige USB-Tastaturen benötigen zur korrekten Unterstützung spezielle 
     Kernelmodule. Diese fehlen im aktuellen Installer. Lesen Sie hierzu <a 
     href="http://bugs.debian.org/561036">#561036</a>, <a 
     href="http://bugs.debian.org/567431">#567431</a>. In den täglich gebauten
     Images ist dies bereits behoben.
 </dd>

  <dt>i386: mehrere Probleme</dt>
  <dd>
     In der i386-Portierung gibt es in dieser Veröffentlichung mehrere bekannte
     Probleme:
     <ul>
       <li>Aufgrund gestiegener Größe des Kernels sind wir nicht in der
     Lage, Installations-Images für Installationen von Diskette bereitzustellen.
       </li>
    </ul>
 </dd>
  <dt>Partitionen mit mehr als 16 TiB werden von Ext4 nicht unterstützt</dt>
  <dd>
     Das Dateisystemerstellwerkzeug von Ext4 erlauben nicht die Erstellung
     von Dateisystemen mit einer Größe von über 16 TiB.
 </dd>
  <dt>Armel: Root auf RAID Boot-Problem</dt>
  <dd>
     Bitte seien Sie darauf vorbereitet, dass es ein Problem gibt, falls Sie
     auswählen, Root auf RAID zu setzen, da nach der Installation von Debian 
     Ihre Maschine nicht starten wird. Glücklicherweise können Sie das Problem
     leicht umgehen: Wenn der Installer Ihnen mitteilt, dass er fertig sei und
     Sie fragt, ob Sie das System neu starten möchten, wählen Sie <q>Zurück</q>
     und öffnen eine Shell im Menü. Verwenden Sie dann Nano, um die Datei
     <tt>/target/usr/share/initramfs-tools/hooks/flash_kernel_set_root</tt> zu
     bearbeiten. Entfernen Sie dort die Prüfung, die sicherstellt, dass das
     Root-Gerät existiert. Geben Sie <q>exit</q> ein, wählen Sie <q>Das System
     bootbar machen</q> und drücken Sie <q>okay</q> bei der <q>Installation
     abgeschlossen</q>-Nachricht.
 </dd>

  <dt>S390: Nichtunterstützte Funktionalitäten</dt>
  <dd>
     <ul>
        <li>Die Unterstützung für DASD DIAG-Disziplinen ist derzeit nicht
            verfügbar.</li>
      </ul>
 </dd>
  <dt>Sparc: Silo-Bootloader-Installationsproblem</dt>
  <dd>
     Der Silo-Bootloader wird nicht korrekt im Zielsystem installiert. Daher
     wird ein Systemneustart fehlschlagen. Lesen Sie <a 
     href="http://bugs.debian.org/565639">#565639</a> für Details. Korrigiert in
     den täglich gebauten Images.
 </dd>
  <dt>kFreeBSD-Images nicht verfügbar</dt>
  <dd>
     Die Unterstützung für kFreeBSD wird noch in den Installer integriert und 
     ist in dieser Veröffentlichung noch nicht verfügbar. Es wird daran
     gearbeitet, dies für kommende Veröffentlichungen zu korrigieren.
 </dd>
</dl>
