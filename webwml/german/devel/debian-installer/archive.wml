#use wml::debian::template title="Ihr apt-fähiges d-i Archiv aufsetzen" NOHEADER="true"
#use wml::debian::recent_list
#use wml::debian::translation-check translation="1.4"
# $Id$
# Translation: Gerfried Fuchs <alfie@debian.org> 2003-09-21

<h1>apt-ftparchive aufsetzen, um udebs zu verarbeiten</h1>

<p>apt-ftparchive benötigt ein paar Anpassungen, damit es udebs ordentlich
behandelt. Ich verwendet etwa folgendes:</p>

<pre>
Dir {
    ArchiveDir "/home/tfheen/public_html/d-i";
    OverrideDir "indices";
    CacheDir "indices";
};

TreeDefault {
        Directory "pool/";
};

BinDirectory "pool/main" {
     Packages "dists/sid/main/debian-installer/binary-i386/Packages";
     BinOverride "override";
     ExtraOverride "override.extra";
};

Default {
        Packages {
                 Extensions ".udeb";
        };
};
</pre>

<p>Sie müssen natürlich die Pfade und ähnliches für sich selbst anpassen. Um
die Packages-Datei tatsächlich zu erstellen:</p>

<ol> 
  <li>Stellen Sie sicher, dass alle Pfad-Komponenten vorhanden sind
    (<code>mkdir -p dists/sid/main/debian-installer/binary-i386</code>)</li>
  <li>Verwenden Sie einen Befehl wie <code>apt-ftparchive generate
    config</code> (falls Sie die obige Konfiguration in eine Datei namens
    <kbd>config</kbd> stellen)</li>
</ol>

<hrline>

<address><a href="mailto:tfheen@debian.org">Tollef Fog Heen</a></address>
