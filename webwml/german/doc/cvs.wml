#use wml::debian::ddp title="DDP CVS-Zugang"
#use wml::debian::translation-check translation="1.11"
# Translator: Martin Schulze <joey@debian.org>
# $Id$

<p>Das Debian-Dokumentations-Projekt speichert seine Webseiten und
viele der Handbuchtexte auf dem CVS-Server von Debian auf
<b>cvs.debian.org</b>.</p>

<p>Nur Mitglieder des Debian-Dokumentations-Projekt können die Dateien
auf dem CVS-Server bearbeiten.  Andere können auf diese Dateien nur
lesend zugreifen.</p>

<p>Sie können <a href="http://cvs.debian.org/ddp/manuals.sgml/?cvsroot=debian-doc">CVSWeb</a>
benutzen, um auf die Dateien einzeln zuzugreifen und die Änderungen zu
sehen.</p>

<p>Zum Download eines kompletten Handbuchs ist der direkte Zugriff auf
den CVS-Server die bessere Wahl.  Sie benötigen dazu das Paket
<tt>cvs</tt> auf Ihrem eigenen Rechner.</p>

<h2>Anonyme Verbindung zum CVS-Server (nur lesend)</h2>

<p>Zum Verbinden geben Sie folgenden Befehl ein:</p>

<p style="margin-left: 2em">
  <code>cvs -d :pserver:anonymous@cvs.debian.org:/cvs/debian-doc login</code>
</p>

<p>und einfach Enter drücken, wenn Sie nach einem Paßwort gefragt werden.</p>

<p>Sie können anschließend die normalen CVS-Befehle verwenden (siehe
auch die Infoseite von CVS), um Dateien auszuchecken, Sie können die
Dateien jedoch nicht ändern und zurückschieben.</p>

<p>Um die Dateien das erste Mal zu laden, erstellen Sie ein neues
Verzeichnis und wechseln Sie in dieses, das alles enthalten soll.
Zwei Verzeichnisse, <strong>CVS</strong> und <strong>ddp</strong>,
werden in diesem erzeugt werden.</p>

<p>Verwenden Sie diesen Befehl, um alle Dateien herunterzuladen:</p>

<p style="margin-left: 2em">
  <code>cvs -d :pserver:anonymous@cvs.debian.org:/cvs/debian-doc -z3 checkout ddp/manuals.sgml</code>
</p>

<p>Das wird die Verzeichnisse rekursiv erzeugen und entsprechend
behandeln.</p>

<p>Um Ihre lokale Kopie mit allen Änderungen zu aktualisieren, die von
den Entwicklern gemacht wurden, begeben Sie sich in das Verzeichnis
<strong>ddp/manuals.sgml</strong> und führen den folgenden Befehl aus:</p>

<p style="margin-left: 2em">
  <code>cvs -z3 update -d -P</code>
</p>

<p>Wenn Sie mit mit der Sitzung fertig sind, benutzen Sie diesen
Befehl, um sich vom Server abzumelden:</p>

<p style="margin-left: 2em">
  <code>cvs logout</code>
</p>

<h2>Zugang zum CVS-Server mit Update-Privilegien</h2>

<p>Bevor Sie auf den CVS-Server mit dieser Methode zugreifen können,
muß Ihnen zuerst Schreibzugriff gewährt worden sein.  Bitte lesen Sie,
wie Sie diesen <a href="#obtaining">anfordern</a> können.</p>

<p>Zum Verbinden geben Sie diesen Befehl ein:</p>

<p style="margin-left: 2em">
  <code>cvs -d :pserver:userid@cvs.debian.org:/cvs/debian-doc login</code>
</p>

<p>Geben Sie das Paßwort ein, das Sie mit dem Koordinator ausgehandelt
haben.  Im obigen Befehl ersetzen Sie <i>userid</i> mit Ihrem eigenen
Benutzernamen auf dem CVS-Server.  Dieser muß nicht notwendigerweise
der gleiche sein wie Ihr Login auf <b>cvs.debian.org</b>, falls Sie
eines haben.</p>

<p>Sie können nun die normalen CVS-Befehle verwenden (siehe auch die
Infoseite von CVS), um Dateien auszuchecken und Änderungen zu übergeben.</p>

<p>Um die Dateien das erste Mal zu laden, erstellen Sie ein neues
Verzeichnis und wechseln Sie in dieses, das alles enthalten soll.
Zwei Verzeichnisse, <strong>CVS</strong> und <strong>ddp</strong>,
werden in diesem erzeugt werden.</p>

<p>Verwenden Sie diesen Befehl, um alle Dateien herunterzuladen:</p>

<p style="margin-left: 2em">
  <code>cvs -d :pserver:userid@cvs.debian.org:/cvs/debian-doc -z3 checkout ddp/manuals.sgml</code>
</p>

<p>Das wird die Verzeichnisse rekursiv erzeugen und entsprechend
behandeln.</p>

<p>Um Ihre lokale Kopie mit allen Änderungen zu aktualisieren, die von
den Entwicklern gemacht wurden, begeben Sie sich in das Verzeichnis
<strong>ddp/manuals.sgml</strong> und führen den folgenden Befehl aus:</p>

<p style="margin-left: 2em">
  <code>cvs -z3 update -d -P</code>
</p>

<p>Wenn Sie mit mit der Sitzung fertig sind, benutzen Sie diesen
Befehl, um sich vom Server abzumelden:</p>

<p style="margin-left: 2em">
  <code>cvs logout</code>
</p>

<h2>Zugang zum CVS-Server mit Update-Privilegien und SSH</h2>

<p>Diese Methode ist nur für registrierte Debian-Entwickler verfügbar
und ist dem pserver von oben vorzuziehen.  Wenn Sie kürzlich ein
registrierter Debian-Entwickler geworden sind, teilen Sie das bitte
dem Koordinator mit, damit er den pserver-Zugang für Sie löschen kann.</p>

<p>Um mit SSH Zugang zu erhalten, teilen Sie zuerst CVS mit, daß Sie
es nutzen möchten:</p>

<p style="margin-left: 2em">
  <code>export CVS_RSH=ssh</code>
</p>

<p>Anschließend erstellen Sie ein neues Verzeichnis und wechseln Sie
in dieses, das alles enthalten soll, und geben Sie folgenden Befehl ein:</p>

<p style="margin-left: 2em">
  <code>cvs -d :ext:<i>userid</i>@cvs.debian.org:/cvs/debian-doc -z3 checkout <i>module</i></code>
</p>

<p>Ersetzen Sie im obigen Befehl <i>userid</i> mit Ihrem eigenen
Benutzername auf <b>cvs.debian.org</b>.  Ersetzen Sie <i>module</i>
mit dem Namen des Moduls, das Sie auschecken möchten, üblicherweise
ist das <tt>ddp/manuals.sgml</tt>.  Sie werden anschließend nach Ihrem
Paßwort gefragt.  Verwechseln Sie es nicht mit dem auf Ihrem eigenen
Rechner.</p>

<p>Zwei Verzeichnisse, <strong>CVS</strong> und <strong>ddp</strong>,
werden in diesem erzeugt werden.  CVS wird die Module
(bzw. Verzeichnisse) rekursiv erzeugen und entsprechend behandeln.</p>

<p>Sie können nun die normalen CVS-Befehle verwenden (siehe auch die
Infoseite von CVS), um Dateien auszuchecken und Änderungen zu übergeben.</p>

<p>Um Ihre lokale Kopie mit allen Änderungen zu aktualisieren, die von
den Entwicklern gemacht wurden, begeben Sie sich in das Verzeichnis
<strong>ddp/manuals.sgml</strong> und führen den folgenden Befehl aus:</p>

<p style="margin-left: 2em">
  <code>cvs -z3 update -d -P</code>
</p>


<h2><a name="obtaining">Update-Privilegien erhalten</a></h2>

<p>Update-Privilegien kann jede Person erhalten, die sich am Schreiben von
Handbüchern beteiligen möchte.</p>

<p>Um Update-Privilegien zu erhalten, schreiben Sie eine Anfrage (in
Englisch!) an den
<a href="mailto:aph@debian.org">Dokumentations-Koordinator</a>, zusammen
mit dem Benutzernamen, den Sie auf dem pserver verwenden möchten und
dem verschlüsselten Paßwort, das von Ihnen mit folgendem Befehl
generiert wurde:</p>

<p style="margin-left: 2em">
  <code>makepasswd --crypt --clear=<i>Ihr_Paßwort</i></code>
</p>

<p>(<tt>makepasswd</tt> ist ein Debian-Paket.)</p>

<p>Wenn Sie kein Paßwort mitschicken, wird eines für Sie generiert, es
wird jedoch nicht zu leicht merken sein.  Um Ihr Paßwort zu ändern,
schreiben Sie dem Koordinator erneut eine Mail und schicken Sie das
neue verschlüsselte Paßtwort mit.</p>

<hr/>

<h1>Organisation der Dateien</h1>

<p>Diese Abbildung zeigt die Struktur des CVS-Verzeichnisbaumes:

<p><img src="dirtree.jpg" align="middle" alt="DDP CVS-Verzeichnis-Baum"/></p>

<p>Bitte beachten Sie, daß die DDP-Website im webwml-Modul des
CVS-Archives enthalten ist, die mit dem Rest der Debian-Webseiten
integriert werden soll.</p>

<hr/>

<h1>Automatische Update-Mechanismen</h1>

<p>Die veröffentlichten Webseiten der Handbücher werden von einem
Cronjob auf www.debian.org jeden Tag um 7:00, 15:00 und 23:00 Uhr
(Pazifik-Zeit) generiert.</p>

<p>Das Skript führt <kbd>cvs update -d -P</kbd> und anschließend
<kbd>make -C manuals.sgml clean publish</kbd> aus, beides im Verzeichnis
<code>ddp</code>.</p>

<p>Das Makefile ist so eingerichtet, daß Dateien in jedem
Unterverzeichnis neu generiert werden (das gepflegte Dokumentation
enthält), und daß es Dateien aus jedem Unterverzeichnis im Verzeichnis
<code>doc/manuals/</code> der Website installiert.  Die Makefiles der
Unterverzeichnisse sind Abkömmlinge von
<code>ddp/manuals.sgml/standard.makefile</code>.</p>

# <!-- Created: Mon Jul  6 19:58:09 BST 1998 -->
