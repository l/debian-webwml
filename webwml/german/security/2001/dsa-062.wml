<define-tag description>Puffer-Überlauf</define-tag>
<define-tag moreinfo>
Samuel Dralet meldete auf bugtraq, daß version 2.6.2 von rxvt (eine
VT102 Terminal Emulation für X) einen Puffer-Überlauf in der
<code>tt_printf()</code> Funktion hat. Ein lokaler Benutzer kann dies, indem
er rxvt veranlaßt eine spezielle Zeichenkette auszugeben, die diese
Funktion benutzt, zum Beipiel durch benutzen der <kbd>-T</kbd> oder 
<kbd>-name</kbd> Kommandozeilenoption. Diese Zeichenkette erzeugt 
einen Stappelüberlauf und enthält Befehle, die rxvt ausführt.

<p>Seit rxvt sgid utmp installiert ist, kann ein Angreifer dies benutzen
um umtp zu werden, was ihm erlaubt, die umtp Datei zu verändern.

<p>Dies wurde in Version 2.6.2-2.1 repariert, und wird empfehlen, daß
Sie Ihr rxvt Packet aktualisieren.
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2001/dsa-062.data"
#use wml::debian::translation-check translation="1.1"
# Translator: Noel Koethe, noel@koethe.net, 2001-06-17
