#use wml::debian::template title="Debian BTS - Fehler berichten" NOHEADER=yes NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.18"

<H1>Wie werden Fehler in Debian berichtet?</H1>

<P>Schicken Sie eine E-Mail an 
<A href="mailto:submit@bugs.debian.org"><code>submit@bugs.debian.org</code></A>,
beachten Sie dabei die weiter unten angeführten Richtlinien.  Bitte
schreiben Sie die Mail auf Englisch, da die meisten Entwickler Deutsch
nicht verstehen (gebrochenes Englisch ist kein Problem).  Debian ist
eine internationale Distribution mit Mitarbeiten aus vielen Ländern,
so daß die verwendete Sprache meistens Englisch ist.</P>

<P>Vermeiden Sie es bitte, mehrere, in keiner Beziehnung zueinander
stehende Fehler - besonders keine, die in verschiedenen Paketen vorkommen,
- in einer einzigen Nachricht zu verschicken. Senden Sie Ihren Fehlerbericht
bitte an keine Mailinglisten oder Empfänger außer
<code>submit@bugs</code> (Einzelheiten über die richtige Vorgehensweise
finden Sie weiter <A href="#xcc">unten</A>).</P>

<P>Listen der zum jetzigen Zeitpunkt noch nicht behobenen Fehler finden
Sie im <A href="./">WWW</A> oder <A href="Access">hier</A> - lesen Sie
weitere Documente, um Details zu erhalten.

<P>Sie müssen einen Pseudo-Header an den Anfang Ihrer Nachricht setzen,
der die Zeilen <CODE>Package:</CODE><VAR>Name des fehlerhaften
Paketes</VAR> und <CODE>Version:</CODE><VAR>Versionsnummer des
fehlerhaften Paketes</VAR> enthält. (Die Felder der Pseudo-Header sollten
ganz am Anfang ihrer jeweiligen Zeile stehen. Die Fehlerdatenbank versteht 
gegenwärtig MIME oder PGP E-Mails noch nicht richtig, so daß es
vorkommen kann, daß die Pseudo-Header in solchen E-Mails nicht erkannt
werden.) Den Paketnamen/-versionsnummer finden Sie mittels <code>dpkg
--search</code> und <code>dpkg --list</code> heraus; mehr dazu
in <code>dpkg --help</code>.


<em><A href="#details">Weitere Anforderungen</A></em>.</P>

<P>Es gibt einige Pseudo-Pakete, die man in der
<code>Package</code>-Zeile des Pseudo-Headers verwenden kann, wenn man
einen Bug meldet, der nicht in einem der Softwarepakte von Debian
vorkommt. Die Liste mit ihren Namen finden Sie 
<A HREF="pseudo-packages">hier</A>.</P>

<H2><A name="example">Beispiel eines Fehlerberichts</A></H2>

<P>Ein Fehlerbericht mit einem Header sieht ungefähr so aus:
<PRE>
  To: submit@bugs.debian.org
  From: diligent@testing.linux.org
  Subject: Hello says `goodbye'

  <A name="pseudoheader">Package: hello</A>
  Version: 1.3-16

  When I invoke `hello' without arguments from an ordinary shell
  prompt it prints `goodbye', rather than the expected `hello, world'.
  Here is a transcript:

  $ hello
  goodbye
  $ /usr/bin/hello
  goodbye
  $

  I suggest that the output string, in hello.c, be corrected.

  I am using Debian GNU/Linux 2.2, kernel 2.2.17-pre-patch-13
  and libc6 2.1.3-10.
</PRE>

<h2><A name="details">Bitte fügen Sie in Ihren Bericht noch folgendes
ein:</A></h2>

<ul>
<li>Den <em>exakten</em> und <em>vollständigen</em> Text aller
    Fehlermeldungen die ausgegeben oder protokolliert wurden. Das ist sehr
    wichtig!
<li>Was Sie genau getippt oder gemacht haben um den Fehler zu
    demonstrieren.
<li>Eine Beschreibung des fehlerhaften Verhaltens der Software: welches
    Verhalten haben Sie genau erwartet und was haben Sie in der Tat
    beobachtet. Eine Kopie einer Beispielsitzung ist ein guter Weg so etwas
    vorzuzeigen.
<li>Korrekturvorschlag oder sogar ein Patch, wenn Sie einen haben.
<li>Details der Konfiguration des Programms, das den(die) Problem(e) verursacht.
    Fügen Sie den kompletten Text der Konfigurationsdateien mit ein.
<li>Die Version beliebiger Pakete von denen das fehlerhafte Paket
    abhängt.
<li>Welche Kernelversion Sie benutzen (<code>uname -a</code>), Ihre
    Shared C Bibliothek (<code>ls -l /lib/libc.so.6</code> oder <code>dpkg -s
    libc6 | grep ^Version</code>) und, falls angebracht, beliebige andere 
    Details über Ihr Debian System. Wenn Sie zum Beispiel ein Problem mit
    einem Perl-Script hatten, dann sollten Sie die Version der
    'perl'-Binärdatei mitliefern (<code>perl -v</code> oder
    <code>dpkg -s perl-5.005 | grep ^Version:</code>).
<li>Relevante Einzelheiten über die Hardware in Ihrem System. Wenn Sie
    ein Problem mit einem Gerätetreiber melden, dann listen Sie bitte
    <em>alle</em> Hardwarekomponenten Ihres Systems auf, da solche Probleme
    oft von IRQ und E/A Adreßkonflikten herrühren.
</ul>

<P>Berichten Sie über alle Einzelheiten, die Ihnen wichtig erscheinen,
Sie laufen kaum Gefahr Ihren Fehlerbericht wegen zu viel Information zu lang
werden zu lassen. Wenn sie klein sind, dann fügen Sie in Ihren Bericht auch
den Inhalt der Dateien ein, mit denen Sie das Problem reproduzieren konnten
(kodieren Sie sie notfalls mit UUEncode falls sie merkwürdige Zeichen
enthalten etc).</P>

<P>Wie in jeder anderen E-Mail auch, sollten Sie eine saubere,
beschreibende <code>Subject</code>-Zeile in Ihrem Mail-Header haben.
Diese Betreffzeile wird später in der Fehlerdatendank als der usprüngliche
Titel des von Ihnen übermittelten Programmfehlers fungieren, also
versuchen Sie sie bitte so informativ wie nur möglich zu machen!</P>


<H2><A name="xcc">Versenden von Kopien der Fehlerberichte an andere
Adressen</A></H2>

<P>Manchmal ist es nötig eine Kopie des Fehlerberichts an eine andere
Adresse außer <code>debian-bugs-dist</code> und der Adresse des
Paketbetreuers zu verschicken (normalerweise sind das die zwei Adressen an
die ein Fehlerbericht verschickt wird).</P>

<P>Das können Sie tun, indem Sie Ihren Fehlerbericht als "Kopie an" (CC) 
an die anderen Adressen verschicken. In diesem Fall würden die anderen 
Kopien jedoch in ihrem <code>Reply-To</code>-Feld und in der
<code>Subject</code>-Zeile keine Nummer des Fehlerberichts haben.
Wenn die Empfänger auf den Fehlerbericht antworten, werden sie
wahrscheinlich den <code>submit@bugs.debian.org</code> Eintrag in ihrem
Header lassen und somit die Nachricht zu einem neuen Fehlerbericht
machen. Das führt zu vielen doppelt-eingereichten Berichten.</P>

<P>Um es <EM>richtig</EM> zu machen sollte man den <code>X-Debbugs-CC</code>
Header verwenden. Fügen sie diese Zeile zum Mail-Header Ihrer Nachricht
(nicht zum Pseudo-Header mit dem <code>Package</code>-Feld) hinzu:</P>
<PRE>
 X-Debbugs-CC: other-list@cosmic.edu
</PRE>
<P>Das veranlasst die Fehlerdatenbank dazu, eine Kopie Ihres
Fehlerberichts an die Adresse(n) in der <code>X-Debbugs-CC</code>-Zeile
sowie an <code>debian-bugs-dist</code> zu senden.</P>

<P>Dieses Feature kann oft sinnvoll mit <code>quiet</code> kombiniert
werden - Einzelheiten dazu finden Sie weiter unten.</P>

<H2><A name="severities">Dringlichkeitsstufen</A></H2>

<P>Ob es sich im Fehlerbericht um einen besonders schweren Fehler oder
lediglich um ein Feature-Request handelt, können Sie beim Abschicken
Ihres Berichts in der Dringlichkeitsstufe festlegen. Das ist allerdings
nicht unbedingt notwendig, der Entwickler wird die passende
Dringlichkeitsstufe festlegen, wenn Sie es nicht tun.

<P>Um eine Dringlichkeitsstufe festzulegen, fügen Sie die
<code>Severity: <var>Dringlichkeit</var></code>-Zeile zusammen mit
<code>Package</code> und <code>Version</code> in den Pseudo-Header Ihrer
Nachricht ein. Die verfügbaren Dringlichkeitsstufen sind in der 
<A href="Developer#severities">Entwicklerdokumentation</A> ausführlich
beschrieben.</P>


<H2>Keine Weiterleitung an die Mailingliste bei kleineren Fehlern</H2>

<P>Wenn ein Fehlerbericht eher unbedeutend ist (zum Beispiel ein
Tippfehler in der Dokumentation oder ein unbedeutendes Build Problem)
oder wenn Sie mehrere Berichte auf einmal haben, dann schicken sie Sie
an <code>maintonly@bugs</code> oder <code>quiet@bugs</code>.
<code>maintonly</code> wird den Bericht dann an den Paketbetreuer
weiterleiten (vorausgesetzt, Sie haben die richtige
<code>Package</code>-Zeile im Pseudo-Header geliefert und der Betreuer
ist der Fehlerdatenbank bekannt); <code>quiet</code> wird den Bericht zwar
an niemanden weiterleiten, ihn jedoch als Fehler vermerken (sinnvoll wenn
Sie zum Beispiel mehrere ähnliche Fehler melden und nur eine
Zusammenfassung posten wollen).</P>

<P>Wenn Sie das tun, wird die Fehlerdatenbank das Feld <code>Reply-To</code>
jeder weitergeleiteten Nachricht so setzen, daß diese Nachrichten genauso
bearbeitet werden wie der Originalbericht.</P>

<H2>Fehlerberichte bei unbekannten Paketen</H2>

<P>Wenn die Fehlerdatenbank den Betreuer des betroffenen Pakets nicht
kennt, dann wird sie den Bericht an <code>debian-bugs-dist</code>
weiterleiten, auch wenn der <code>maintonly</code>-Tag verwendet wurde.

<P>Wenn Sie etwas an <code>maintonly@bugs</code> oder
<var>nnn</var><code>-maintonly@bugs</code> schicken, dann sollten Sie
sicherstellen, daß der Bericht das richtige Paket betrifft, indem Sie
beim Verschicken des Originalfehlerberichts das Feld <code>Package</code>
korrekt angeben, oder indem Sie <A href="server-control">den
<code>control@bugs</code>-Dienst</A> nutzen und damit den Bericht passend
zuordnen, falls es nicht bereits passiert ist.</P>

<h2>Benutzen von <code>dpkg</code>, um den Paketnamen und die Version
herauszufinden</h2>

<P>Wenn Sie über einen Fehler in einem Kommando berichten möchten, dann
finden Sie mit <code>dpkg --search</code> heraus, zu welchem Paket
dieses Kommando gehört. Die Versionsnummer des installierten Paketes
ermitteln sie mit <code>dpkg --list</code> oder <code>dpkg
--status</code>.</P>

<P>Zum Beispiel:
<pre>
$ which apt-get
/usr/bin/apt-get
$ type apt-get
apt-get is /usr/bin/apt-get
$ dpkg --search /usr/bin/apt-get
apt: /usr/bin/apt-get
$ dpkg --list apt
Desired=Unknown/Install/Remove/Purge/Hold
| Status=Not/Installed/Config-files/Unpacked/Failed-config/Half-installed
|/ Err?=(none)/Hold/Reinst-required/X=both-problems (Status,Err: uppercase=bad)
||/ Name           Version        Description
+++-==============-==============-============================================
ii  apt            0.3.19         Advanced front-end for dpkg
$ dpkg --status apt
Package: apt
Status: install ok installed
Priority: standard
Section: base
Installed-Size: 1391
Maintainer: APT Development Team &lt;deity@lists.debian.org&gt;
Version: 0.3.19
Replaces: deity, libapt-pkg-doc (&lt;&lt; 0.3.7), libapt-pkg-dev (&lt;&lt; 0.3.7)
Provides: libapt-pkg2.7
Depends: libapt-pkg2.7, libc6 (&gt;= 2.1.2), libstdc++2.10
Suggests: dpkg-dev
Conflicts: deity
Description: Advanced front-end for dpkg
 This is Debian's next generation front-end for the dpkg package manager.
 It provides the apt-get utility and APT dselect method that provides a
 simpler, safer way to install and upgrade packages.
 .
 APT features complete installation ordering, multiple source capability
 and several other unique features, see the Users Guide in
 /usr/doc/apt/guide.text.gz

</pre>

<hr>

#use "otherpages.inc"

#use "footer.inc"
