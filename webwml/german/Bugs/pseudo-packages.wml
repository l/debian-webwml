#use wml::debian::template title="Debian BTS - Pseudo-Pakete" NOHEADER="true" NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.8"
# Translator: Noel Köthe, noel@koethe.net, 2001-07-18

<H1>Debian Fehlerdatenbank Pseudo-Pakete</H1>

<p>Diese Seite listet die Pseudo-Pakete auf, die man in der
<code>Package:</code> Zeile von Fehlerberichten benutzen kann.

<p>Schauen Sie sich die <a href="Reporting">Anleitung um Fehler zu melden</a>
für Details, wie man eine <code>Package:</code> Zeile spzifiziert, an.

<hrline>

<:
my $file = "$(ENGLISHDIR)/Bugs/pseudo-packages.description";
open I, "$file" or die "Can't open $file file: $!\n";
while (<I>) { if (/^(\S+)\s+(.*)$/) { $pseudos{$1} = $2; } }
close I;
%pseudos || die "%pseudos is not set -- empty $file file?\n";
foreach $a (sort keys %pseudos) {
  if ($a ne "spam" && $a ne "slink-cd") {
    print "<p><code>$a</code> &mdash; $pseudos{$a}\n";
  }
}
:>

<hrline>

#use "otherpages.inc"

#use "footer.inc"
