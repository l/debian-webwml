#use wml::debian::languages

<perl>
my %langs_reverse = ();
foreach $lang (keys %langs) {
  my $foo = $langs{$lang};
  $langs_reverse{$foo} = $lang;
};

if ("$(TEAM)" ne "") {
  (my $l = "$(TEAM)") =~ s/_\S+$//;
  $teampolang = ucfirst $langs_reverse{'$(TEAM)'};
  $header = "<tr><td>Package<td>Score<td>Details<td>PO files\n";
} else {
  $header = "<tr><td>Pakete<td>Bewertung<td>Details<td>po-Dateien\n"; # <td>Maintainer<td>Kommentare\n";
}
</perl>

#use wml::debian::template title="PO-Dateien in Debian für jede Sprache"
#use wml::debian::translation-check translation="1.4"
# $Id$

<: print "<h2>$teampolang</h2>\n" if ("$(TEAM)" ne ""); :>

<if "$(TEAM)" "
<p>Sie können sie herunterladen, übersetzen und als Fehlerbericht dem
Paket-Betreuer zuschicken.
<BR>
<BR>
" "
<p>Hier ist eine Liste aller Sprachen, für die po-Dateien existieren.
Klicken Sie auf Ihre Sprache, um Details zu erhalten.
<BR>
<BR>
">

#include "$(ENGLISHDIR)/international/l10n/scripts/team-po.pl"
