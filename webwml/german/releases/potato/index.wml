#use wml::debian::template title="Debian Release-Informationen f&uuml;r ``potato''"

<p>

Der Kodename f&uuml;r den n&auml;chsten Release von Debian ist
"potato". Es ist sehr wahrscheinlich, dass die Versionsnummer "2.2"
sein wird, aber das ist noch nicht sicher.

<p>

Dieser Release wird derzeit noch als "instabil" (<em>unstable</em>)
betrachtet. Das bedeutet, dass die Distribution <em>nicht</em> richtig
funktionieren wird.

<p>

Debian hat die Regel, dass es kein festes Datum f&uuml;r den
n&auml;chsten Release gibt, also gibt es auch noch kein Datum f&uuml;r
den Release von "potato".

<h2>Gr&ouml;&szlig;te &Auml;nderungen bei "potato"</h2>

Die gr&ouml;&szlig;ten &Auml;nderungen in potato, soweit schon jetzt
bekannt, sind die folgenden:

<ul>

  <li> Linux-Kernel 2.2 (F&uuml;r Architekturen mit Linux-Kernel).

  <li> GNU libc 2.1 basiert (aufw&auml;rtskompatibel mit GNU libc 2.0).

  <li> Die <a href="http://egcs.cygnus.com/">egcs</a>-Version von
    <tt>gcc</tt>, auch bekannt als <tt>egcc</tt>, wird der
    Standardkompiler f&uuml;r C sein.

</ul>

Es ist ausserdem sehr wahrscheinlich, dass potato auch f&uuml;r
die PowerPC und ARM-Architektur ver&ouml;ffentlicht wird.

<p>

Es wird vermutlich noch viele weitere Ver&auml;nderungen in diesem
Release geben, die aber bis jetzt noch nicht bekannt sind.

