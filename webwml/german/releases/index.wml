#use wml::debian::template title="Debian Releases"

<!--
  Translator       : Thimo Neubauer <thimo@debian.org>
  $Id$
  -->

      <h2>Releases nach Status</h2>

<p>

Die folgenden Releases werden in der Reihenfolge ihres Status
pr&auml;sentiert. Diese Links werden immer funktionieren, nachdem
Debian immer mindestens zwei Releases in Arbeit hat: "stable" (stabil)
und "unstable" (instabil).

<dl>
	<dt><a href="stable/">stable</a>
	<dd>
Die stabile Distribution ist die letzte, offiziell freigegebene
Distribution von Debian. Es ist die Produktionsversion von Debian. Die
aktuelle stabile Distribution ist <em>slink</em>.
</dd>
	<dt><a href="unstable/">unstable</a>
	<dd>
Die instabile Distribution ist der Ort, an dem die aktive Entwicklung
von Debian stattfindet. Diese Distribution wird im wesentlichen nur
von den Entwicklern und Leuten, die an vorderster Front leben wollen,
benutzt. Die aktuelle instabile Distribution ist <em>potato</em>.

<: if ( -d 'frozen' ) { _:>
	<dt><a href="frozen/">frozen</a>
	<dd>
Die eingefrorene Distribution ist ein Kanidat zur
Ver&ouml;ffentlichung als n&auml;chste stabile Version. Zur Zeit gibt
es eine eingefrorene Distribution; normalerweise gibt es nur die
stabile und instabile Distribution. Die aktuelle eingefrorene
Distribution ist
<em>
<: print readlink('frozen'); # FYI, this doesn't work, I had it above too :>
</em>
<: } :>

      </dl>

<h2>Releases nach Namen</h2>
<p>

Die folgenden Releases sind nur ein anderer Weg, um an die
verschiedenen Versionen der Distribution zu gelangen.
<ul>
<: if ( -d 'hamm' ) { _:>
	<li><a href="hamm/">Debian 2.0 (hamm)</a>
<: } :>
<: if ( -d 'slink' ) { _:>
	<li><a href="slink/">Debian 2.1 (slink)</a>
<: } :>
<: if ( -d 'potato' ) { _:>
	<li><a href="potato/">Debian 2.2 (potato)</a>
<: } :>
</ul>


<!-- Keep this comment at the end of the file
Local variables:
mode: html
sgml-indent-data:nil
sgml-doctype:".doctype"
End:
-->
