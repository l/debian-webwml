#use wml::debian::template title="Debian ``woody'' Release Informationen"
#use wml::debian::translation-check translation="1.9"
# Translation for 1.7: Gerfried Fuchs <alfie@debian.org>

<p>Der Codename des nächsten großen Releases nach
<a href="../potato/">potato</a> ist ``woody''.  Er wird
die Versionsnummer ``3.0'' erhalten.

<p>Diese Distribution befindet sich zur Zeit in der ``<a href="<a href="http://ftp-master.debian.org/testing/">">testing</a>'' Phase.  Das
bedeutet, dass Programme <em>nicht funktionieren</em> könnten, wenn Sie
woody verwenden.  Von jedem, der woody verwendet, wird erwartet dass er über
die Unbeständigkeiten informiert ist, üblicherweise durch das Verfolgen der
Entwickler-bezogenen Mailing-Listen; die testing-Distribution sollte stabiler
sein als ``unstable'', aber Sie sollten trotzdem vorsichtig sein.

<p>Wie gewohnt sind <strong>keine</strong> speziellen Release-Ziele definiert,
wie auch <strong>keine</strong> Zeitrichtlinien festgesetzt werden. Um es kurz
zu fassen, "Debian veröffentlicht wenn es Zeit ist".

<p>Erwartete große Aktualisierungen, wenn woody veröffentlicht werden wird,
beinhalten Linux Kernel 2.4, GLibC 2.2, GCC 3.0, XFree86 4.0 und Perl 5.6. Es
ist ebenfalls wahrscheinlich dass das <code>debconf</code> Paket von allen
Paketen verwendet werden wird, die eine Benutzereingabe benötigen.

<p>Falls Sie APT verwenden können Sie die folgenden Zeilen in Ihre
<code>/etc/apt/sources.list</code> Datei einfügen um Zugriff auf die Pakete
in testing zu erhalten:

<pre>
  deb http://http.us.debian.org/debian testing main contrib non-free
  deb http://non-us.debian.org/debian-non-US testing/non-US main non-free
</pre>

<p>Lesen Sie bitte die <code>apt-get</code>(8) und die
<code>sources.list</code>(5) Manual-Seiten für ausführlichere Informationen.
