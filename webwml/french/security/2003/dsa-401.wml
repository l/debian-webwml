#use wml::debian::translation-check translation="1.1" maintainer="DFS Task Force"
<define-tag description>Attaque sur les formats de chaînes de caractères</define-tag>
<define-tag moreinfo>
<p>L'équipe de sécurité de SuSE a découvert plusieurs failles de sécurité 
exploitables sur les formats de chaînes de caractères dans <i>hylafax</i>, un 
un système client/serveur de gestion des fax flexible. Elles pouvaient permettre
d'exécuter n'importe quel code en tant que <i>root</i> sur le serveur de 
fax.</p>

<p>Pour la distribution stable (<em>Woody</em>), ce problème a été corrigé
dans la version&nbsp;4.1.1-1.3.</p>

<p>Pour la distribution instable (<em>Sid</em>), ce problème a été corrigé
dans la version&nbsp;4.1.8-1.</p>

<p>Nous vous recommandons de mettre à jour votre paquet hylafax.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-401.data"
