#use wml::debian::translation-check translation="1.1" maintainer="Pierre Machard"
<define-tag description>Erreurs de débordements mathématiques</define-tag>
<define-tag moreinfo>
<p>Al Viro et Alan Cox ont découvert plusieurs erreurs de débordements 
mathématiques dans NetPBM, un ensemble d'outils de conversion graphique. Ces
programmes ne sont pas installés avec le <i>setuid root</i>, mais on les 
installe souvent pour préparer les données qui doivent être traitées. Ces 
failles peuvent permettre à des assaillants à distance de provoquer un déni de 
service ou d'exécuter une commande arbitraire.</p>

<p>Pour la distribution stable (<em>Woody</em>), ce problème a été corrigé dans
la version&nbsp;9.20-8.2.</p>

<p>L'ancienne distribution stable (<em>Potato</em>) ne semble pas affectée par
ce problème.</p>

<p>Pour la distribution instable (<em>Sid</em>), ce problème a été corrigé dans la
version&nbsp;9.20-9.</p>

<p>Nous vous recommandons de mettre à jour votre paquet netpbm.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-263.data"
