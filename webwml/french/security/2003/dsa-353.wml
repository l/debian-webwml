#use wml::debian::translation-check translation="1.1" maintainer="DFS Task Force"
<define-tag description>Fichier temporaire non sécurisé</define-tag>
<define-tag moreinfo>
<p><i>sup</i>, un paquet utilisé pour maintenir des collections de fichiers 
dans des versions identiques sur plusieurs machines, ne prend pas les
précautions de sécurité appropriées lors de la création de fichiers 
temporaires.  Un attaquant local pouvait exploité cette faille de sécurité 
pour écraser n'importe quel fichier avec les privilèges de l'utilisateur 
exécutant <i>sup</i>.</p>

<p>Pour la distribution stable (<em>Woody</em>), ce problème a été corrigé dans 
la version&nbsp;1.8-8woody1.</p>

<p>Pour la distribution instable (<em>Sid</em>), ce problème a été corrigé dans
la version&nbsp;1.8-9.</p>

<p>Nous vous recommandons de mettre à jour votre paquet sup.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-353.data"
