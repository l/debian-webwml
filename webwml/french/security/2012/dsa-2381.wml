#use wml::debian::translation-check translation="1.1" maintainer="David Prévot"
<define-tag description>Désallocation de mémoire non valable</define-tag>
<define-tag moreinfo>
<p>
Le code de prise en charge d'IPv6 de Squid ne traite pas
correctement certaines réponses DNS, avec pour conséquence une
désallocation de pointeur non valable et un plantage du démon.
</p>

<p>
Le paquet squid et la version de Squid 3 embarquées avec Lenny n'ont pas
de prise en charge d'IPv6 et ne sont pas concernées par ce problème.
</p>

<p>Pour la distribution stable (Squeeze), ce problème a été corrigé dans la version 3.1.6-1.2+squeeze2.</p>

<p>Pour la distribution testing (Wheezy) et la distribution unstable (Sid), ce problème a été corrigé dans la version 3.1.18-1.</p>

<p>Nous vous recommandons de mettre à jour vos paquets squid3.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2381.data"
# $Id$
