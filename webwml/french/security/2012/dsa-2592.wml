#use wml::debian::translation-check translation="1.2" maintainer="David Prévot"
<define-tag description>Erreur de programmation</define-tag>
<define-tag moreinfo>
<p>
Marko Myllynen a découvert qu'ELinks, un navigateur
puissant en mode texte, délègue incorrectement les
autorisations de l'utilisateur lors d'une négociation GSS.
</p>

<p>
Pour la distribution stable (Squeeze), ce problème a été corrigé dans la version 0.12~pre5-2+squeeze1.

La version initiale de XULRunner dans Squeeze devait être mise à jour
et la version actuellement dans l'archive est incompatible avec ELinks.

Ainsi, la prise en charge de JavaScript a due être désactivée (seul un petit
sous-ensemble de fonctionnalités typiques était pris en charge de toute façon).

Elle sera sans doute réactivée lors d'une future mise à jour mineure.
</p>

<p>Pour la distribution testing (Wheezy), ce problème a été corrigé dans la version 0.12~pre5-9.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 0.12~pre5-9.</p>

<p>Nous vous recommandons de mettre à jour vos paquets elinks.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2012/dsa-2592.data"
# $Id$
