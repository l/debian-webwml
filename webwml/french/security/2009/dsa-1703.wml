#use wml::debian::translation-check translation="1.1" maintainer="Jean-Edouard Babin"
<define-tag description>conflit d'interprétation</define-tag>
<define-tag moreinfo>
<p>Il a été découvert que BIND, une implémentation du protocole DNS,
ne vérifie pas correctement le résultat d'une fonction de OpenSSL qui est utilisée
pour vérifier les signatures DSA.
Ceci peut conduire à l'enregistrement d'entrées DNS non signées dans des zone
protégées par DNSSEC.</p>

<p>Pour la distribution stable (Etch), ce problème a été corrigé dans la
version 9.3.4-2etch4.</p>

<p>Pour la distribution unstable (Sid) et la distribution testing (Lenny), ce
problème sera corrigé prochainement.</p>

<p>Nous vous recommandons de mettre à jour vos paquets BIND.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2009/dsa-1703.data"
# $Id$
