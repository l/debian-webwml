#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p>Alex Rousskov de « The Measurement Factory » a découvert que Squid3, un
cache de serveur mandataire web riche en fonctionnalités, ne traite pas
correctement les erreurs pour certaines réponses HTTP malformées. Un
serveur HTTP distant peut exploiter ce défaut pour provoquer un déni de
service (échec d'assertion et arrêt du démon).</p>

<p>Pour la distribution oldstable (Wheezy), ce problème a été corrigé dans la version 3.1.20-2.2+deb7u4.</p>

<p>Pour la distribution stable (Jessie), ce problème a été corrigé dans la version 3.4.8-6+deb8u2.</p>

<p>Pour la distribution testing (Stretch), ce problème a été corrigé dans la version 3.5.15-1.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 3.5.15-1.</p>

<p>Nous vous recommandons de mettre à jour vos paquets squid3.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2016/dsa-3522.data"
# $Id$
