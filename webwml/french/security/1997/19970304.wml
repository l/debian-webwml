#use wml::debian::translation-check translation="1.6" maintainer="DFS Task Force"
<define-tag moreinfo>
<p>SuperProbe n'est pas installé avec un <i>setuid root</i> sur Debian.

<p>Rapporteur original du rapport&nbsp;:&nbsp;&lt;solar@ideal.ru&gt;

<p>SuperProbe est un programme fourni avec XFree86 (il permet de déterminer
le type de matériel vidéo installé sur la machine), installé avec un <i>setuid
root</i> sur la plupart des distributions Linux.

<p>Le débordement que j'utilise se trouve dans la fonction TestChip [...]

</define-tag>
<define-tag description>SuperProbe (composant de XFree86) contient un certain nombre de débordements de tampons</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1997/19970304.data'
