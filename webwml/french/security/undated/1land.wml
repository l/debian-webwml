<define-tag description>Attaque par capture d'IP</define-tag>
<define-tag moreinfo>
Des paquets SYN capturés sont envoyés à la machine A, en utilisant l'adresse 
de A comme destination et comme source et le même port libre pour les deux 
cibles. Ceci bloque la machine.
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/undated/1land.data'
#use wml::debian::translation-check translation="1.7" maintainer="DFS Task Force"
