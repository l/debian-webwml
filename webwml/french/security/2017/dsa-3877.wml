#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p>Il a été découverte que Tor, un système de communication anonyme à faible
latence, contient un défaut dans le code du service caché lors de la
réception d'une cellule BEGIN_DIR dans un circuit de « rendez-vous » d'un
service caché. Un attaquant distant peut tirer avantage de ce défaut pour
provoquer le plantage d'un service caché avec un échec d'assertion
(TROVE-2017-005).</p>

<p>Pour la distribution stable (Jessie), ce problème a été corrigé dans la version 0.2.5.14-1.</p>

<p>Pour les distributions stable à venir (Stretch) et unstable (Sid), ces
problèmes ont été corrigés dans la version 0.2.9.11-1~deb9u1.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 0.2.9.11-1.</p>

<p>Nous vous recommandons de mettre à jour vos paquets tor.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2017/dsa-3877.data"
# $Id$
