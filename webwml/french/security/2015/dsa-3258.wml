#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p> La correction pour
<a href="https://security-tracker.debian.org/tracker/CVE-2013-4422">\
CVE-2013-4422</a> dans quassel, un client IRC distribué, était incomplète.
Cela pourrait permettre à des attaquants distants d'injecter des requêtes SQL
après la reconnexion d'une base de données (par exemple quand le moteur du
serveur PostgreSQL est redémarré).</p>

<p>Pour la distribution stable (Jessie), ce problème a été corrigé dans la version 1:0.10.0-2.3+deb8u1.</p>

<p>Pour la distribution testing (Stretch), ce problème a été corrigé dans la version 1:0.10.0-2.4.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 1:0.10.0-2.4.</p>

<p>Nous vous recommandons de mettre à jour vos paquets quassel.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3258.data"
# $Id$
