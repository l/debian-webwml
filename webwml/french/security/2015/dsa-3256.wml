#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p>Hanno Boeck a découvert un défaut de dépassement de tas dans la manière
dont Libtasn1, une bibliothèque pour gérer les structures ASN.1, décodait
certaines entrée encodée avec DER. Une entrée encodée avec DER contrefaite
pour l'occasion pourrait provoquer le plantage d'une application utilisant la
bibliothèque Libtasn1, ou éventuellement exécuter du code arbitraire.</p>

<p>Pour la distribution stable (Jessie), ce problème a été corrigé dans la version 4.2-3+deb8u1.</p>

<p>Pour la distribution testing (Stretch), ce problème a été corrigé dans la version 4.4-3.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 4.4-3.</p>

<p>Nous vous recommandons de mettre à jour vos paquets libtasn1-6.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3256.data"
# $Id$
