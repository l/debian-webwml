#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p>Tomek Rabczak de NCC Group a découvert un défaut dans la méthode
normalize_params() de Rack, une interface modulaire de serveur web en Ruby. Un
attaquant distant peut utiliser ce défaut avec des requêtes contrefaites pour
l'occasion pour provoquer un <q>SystemStackError</q> et éventuellement une
condition de déni de service pour le service.</p>

<p>Pour la distribution oldstable (Wheezy), ce problème a été corrigé dans la version 1.4.1-2.1+deb7u1.</p>

<p>Pour la distribution stable (Jessie), ce problème a été corrigé dans la version 1.5.2-3+deb8u1.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 1.5.2-4.</p>

<p>Nous vous recommandons de mettre à jour vos paquets ruby-rack.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2015/dsa-3322.data"
# $Id$
