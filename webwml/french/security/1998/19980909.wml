#use wml::debian::translation-check translation="1.7" maintainer="DFS Task Force"
<define-tag moreinfo>
<p>Nous avons reçu des rapports montrant que le shell <i>bash</i> a des
problèmes avec les noms de chemins très longs. Lorsqu'il rencontre un très
long chemin, <i>bash</i> échoue dans la vérification du résultat de
<i>getcwd()</i> et ce peut importe l'endroit, ce qui pourrait être exploité.

<p>Nous vous recommandons la mise à jour immédiate de votre paquet <i>bash</i>.
</define-tag>
<define-tag description>Problème avec les noms de chemins très longs</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980909.data'
