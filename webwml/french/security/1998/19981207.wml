#use wml::debian::translation-check translation="1.7" maintainer="DFS Task Force"
<define-tag moreinfo>
<p>Nous avons trouvé que le paquet <i>fte</i> fourni dans nos archives 
<i>slink (frozen)</i> et <i>potato (unstable)</i>  n'enlève pas ses privilèges
<i>root</i> après une initialisation du périphérique de console virtuelle.
Cela autorise les utilisateurs à écrire et lire les fichiers avec les
privilèges <i>root</i> ainsi que d'exécuter les programmes en tant que
<i>root</i>.</p>

<p>Un nouveau paquet (version&nbsp;0.46b-4.1) a été mis en place pour
résoudre ce problème.</p>

<p>Nous vous recommandons la mise à jour immédiate de votre paquet
<i>fte</i>.</define-tag>
<define-tag description>Pas de suppression de ses privilèges <i>root</i></define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19981207.data'
