#use wml::debian::translation-check translation="1.7" maintainer="DFS Task Force"
<define-tag moreinfo>
Il a été rapporté que quelques distributions de Linux sont affectées par une
vulnérabilité dans <i>samba</i>. Il y a deux problèmes&nbsp;:
<ul>
 <li>Le programme <i>wsmbconf</i> (qui est toujours une application
     prototype sans prétention pour la version générale) est non sécurisée. 
     Debian ne distribue pas ce programme&nbsp;;
 <li>Quelques distributions utilisent un répertoire ouvert en écriture à
     tous sans aucun bit t.
     Nous utilisons <i>/var/tmp</i> qui ne souffre pas de ce problème.
</ul>

<p>Il n'y a pas besoin de mettre à jour votre paquet <i>samba</i>.
</define-tag>
<define-tag description>Fichiers temporaires non sûrs</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19981118.data'
