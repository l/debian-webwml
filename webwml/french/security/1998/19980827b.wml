#use wml::debian::translation-check translation="1.6" maintainer="DFS Task Force"
<define-tag moreinfo>
<p>Nous avons reçu un rapport comme quoi un utilisateur peut exécuter des
commandes aléatoire à partir d'un fichier <i>.plan</i> ou <i>.project</i>.
Bien que par défaut l'option qui permet cela n'est pas activée, le système
devient vulnérable si l'administrateur système l'a activé.

<p>Nous recommandons la mise à jour immédiate de votre paquet <i>cfingerd</i>.
</define-tag>
<define-tag description>Accès potentiel permettant des exploitations locales de <i>root</i></define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980827b.data'
