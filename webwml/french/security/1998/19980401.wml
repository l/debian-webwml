#use wml::debian::translation-check translation="1.6" maintainer="DFS Task Force"
<define-tag moreinfo>
Les anciennes versions de <i>bind</i> sont vulnérables à un débordement de
tampon pendant une demande de requête inversée, autorisant potentiellement
les accès systèmes en tant que <i>root</i>. Des vulnérabilités
additionnelles permettent un déni de service sur le service <i>bind</i>.

<p>Voir l'avertissement&nbsp;CA-98.05 du CERT.
</define-tag>
<define-tag description>Un dépassement de capacité crée un exploit distant <i>root</i> potentiel, ainsi qu'un déni de service</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980401.data'
