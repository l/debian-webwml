<define-tag moreinfo>Bill Nottingham a rapporté un problème dans 
les fonctions d'encapsulation du lecteur de nouvelles slrn. Une en-tête 
trop longue dans un message pouvait faire dépasser un tampon et donc 
d'exécuter n'importe quel code encodé dans le message.

<p>La configuration par défaut n'active pas l'encapsulation mais il est 
facile de le faire en changeant la configuration ou en pressant la 
touche W en visualisant un message.

<p>Ceci a été corrigé dans la version&nbsp;0.9.6.2-9potato1 et nous vous
recommandons de mettre à jour immédiatement votre paquet slrn.</define-tag>

<define-tag description>Dépassement de tampon</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-040.data'
#use wml::debian::translation-check translation="1.3" maintainer="DFS Task Force"
