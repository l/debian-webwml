<define-tag moreinfo>Avec des versions moins récentes de <i>jazip</i>, 
un utilisateur pouvait avoir l'accès <i>root</i> pour les membres du groupe 
<i>floppy</i> sur la machine locale. L'interface ne faisant plus appel au 
privilège de <i>root</i>, cette faille de sécurité est corrigée. Le programme 
tronque la variable DISPLAY si elle dépasse les 256 caractères. Ceci peut 
entraîner un dépassement de tampon (à l'intérieur de xforms).

<p>Nous vous recommandons de mettre à jour votre paquet jazip 
immédiatement.</define-tag> 
<define-tag description>Dépassement de tampon</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-017.data'
#use wml::debian::translation-check translation="1.3" maintainer="DFS Task Force"
