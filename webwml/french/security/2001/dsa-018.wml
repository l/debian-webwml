<define-tag moreinfo>PkC a découvert un débordement mémoire dans 
<i>tinyproxy</i> qui est exploitable à distance. Un attaquant peut obtenir 
un interpréteur de commandes en tant que <i>nobody</i> à distance.

<p>Nous vous recommandons de mettre à jour votre paquet tinyproxy 
immédiatement.</define-tag>
<define-tag description>Faille de sécurité concernant <i>nobody</i> exploitable à distance</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-018.data'
#use wml::debian::translation-check translation="1.2" maintainer="DFS Task Force"
