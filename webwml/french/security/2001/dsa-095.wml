#use wml::debian::translation-check translation="1.2" maintainer="Pierre Machard"
<define-tag description>Root vulnérable en local</define-tag>
<define-tag moreinfo>
Le paquet «&nbsp;gpm&nbsp;» contient le programme <code>gpm-root</code>, qui peut être exécuté pour
créer des menus activables au moyen de la souris dans la console.
Parmi les autres problèmes, le programme <code>gpm-root</code>, contient un 
format vulnérable de chaîne qui permet à un assaillant d'obtenir les privilèges de root.

<p>Ceci a été corrigé dans la version&nbsp;1.17.8-18.1, et nous vous 
recommandons de mettre à jour le paquet&nbsp;1.17.8-18 immédiatement.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2001/dsa-095.data"
