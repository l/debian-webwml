<define-tag description>Script du responsable non opérationnel</define-tag>
<define-tag moreinfo>Les versions du paquet <i>sash</i> antérieures à 3.4-4 
ne clônaient pas proprement /etc/shadow, le laissant lisible pour n'importe 
qui.

<p>Ce paquet n'existe que dans la version stable, donc les utilisateurs de 
celle instable, ne verront pas de correction.

<p>Nous vous recommandons de mettre à jour votre paquet sash 
immédiatement.</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-015.data'
#use wml::debian::translation-check translation="1.3" maintainer="DFS Task Force"
