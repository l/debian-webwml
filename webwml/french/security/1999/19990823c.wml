<define-tag moreinfo>Toutes les versions antérieures de <i>trn</i> 
utilisaient un nom inscrit dans le code pour un fichier de 
/tmp servant de zone de stockage temporaire. Un lien symbolique 
malin permettrait à un utilisateur de modifier un fichier dont 
il n'était pas le propriétaire, et ce sans laisser de trace.</define-tag>
<define-tag description>Problème lors de la création de fichiers dans /tmp</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1999/19990823c.data'
#use wml::debian::translation-check translation="1.5" maintainer="DFS Task Force"
