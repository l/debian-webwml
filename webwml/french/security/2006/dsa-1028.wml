#use wml::debian::translation-check translation="1.2" maintainer="Simon Paillard"
<define-tag description>Erreur de programmation</define-tag>
<define-tag moreinfo>
<p>Kjetil Kjernsmo a découvert un bogue dans libimager-perl, un module Perl
pour générer des images 24&nbsp;bits, qui pouvait provoquer une erreur de
segmentation lors de la manipulation d'images JPEG à 4 canaux.</p>

<p>L'ancienne distribution stable (<em>Woody</em>) ne contient pas ce
paquet.</p>

<p>Pour l'actuelle distribution stable (<em>Sarge</em>), ce problème a été
corrigé dans la version&nbsp;0.44-1sarge1.</p>

<p>Pour la distribution instable (<em>Sid</em>), ce problème a été corrigé dans
la version&nbsp;0.50-1.</p>

<p>Nous vous recommandons de mettre à jour votre paquet libimager-perl.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-1028.data"
