#use wml::debian::translation-check translation="1.1" maintainer="Jean-Pierre Giraud"
<define-tag description>Mise à jour de sécurité</define-tag>
<define-tag moreinfo>
<p>Oscar Reparaz a découvert une vulnérabilité de contournement
d'authentification dans iodine, un outil pour créer un tunnel de données IPv4
à travers un serveur de DNS. Un attaquant distant pourrait, en exploitant ce
défaut, faire en sorte que le serveur accepte le reste de la configuration et 
également du traffic réseau.</p>

<p>Pour la distribution stable (Wheezy), ce problème a été corrigé dans la version 0.6.0~rc1-12+deb7u1.</p>

<p>Pour la distribution testing (Jessie), ce problème a été corrigé dans la version 0.6.0~rc1-19.</p>

<p>Pour la distribution unstable (Sid), ce problème a été corrigé dans la version 0.6.0~rc1-19.</p>

<p>Nous vous recommandons de mettre à jour vos paquets iodine.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2014/dsa-2964.data"
# $Id$
