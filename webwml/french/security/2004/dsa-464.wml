#use wml::debian::translation-check translation="1.2" maintainer="DFS Task Force"
<define-tag description>Gestion d'image défaillante</define-tag>
<define-tag moreinfo>
<p>Thomas Kristensen a découvert une faille de sécurité dans <i>gdk-pixbuf</i> 
(paquet binaire libgdk-pixbuf2), la bibliothèque d'image pour Gtk GdkPixBuf, 
qui peut causer le plantage de l'application. Pour exploiter ce problème,
un attaquant distant pouvait envoyer un fichier BMP soigneusement conçu 
via courriel, qui ferait planter par exemple Evolution mais pas que cette
application.</p>

<p>Pour la distribution stable (<em>Woody</em>), ce problème a été corrigé
dans la version&nbsp;0.17.0-2woody1.</p>

<p>Pour la distribution instable (<em>Sid</em>), ce problème sera
corrigé dans la version&nbsp;0.22.0-3.</p>

<p>Nous vous recommandons de mettre à jour votre paquet libgdk-pixbuf2.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2004/dsa-464.data"
