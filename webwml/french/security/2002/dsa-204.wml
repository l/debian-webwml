<define-tag description>Exécution de code arbitraire</define-tag>
<define-tag moreinfo>
<p>L'équipe KDE a 
<a href="http://www.kde.org/info/security/advisory-20021111-1.txt">\
découvert</a> une faille de sécurité dans les fonctionnalités de plusieurs 
protocoles réseau par KIO. L'implémentation des protocoles de <i>rlogin</i>
et de <i>telnet</i> permet d'exécuter des commandes arbitraires 
sur la machine vulnérable en utilisant le compte de la victime via 
une URL malveillante dans une page HTML, un courriel au format HTML ou 
dans toute autre application faisant appel à KIO.</p>

<p>Ce problème a été corrigé en désactivant <i>rlogin</i> et <i>telnet</i> 
dans la version&nbsp;2.2.2-13.woody.5 pour l'actuelle distribution stable 
(<em>Woody</em>). L'ancienne distribution stable (<em>Potato</em>) n'est pas 
affectée vu qu'elle ne contient pas KDE. Une correction pour le paquet
dans la distribution instable (<em>Sid</em>) n'est pas encore disponible.</p>

<p>Nous vous recommandons la mise à jour de votre paquet kdelibs3 
immédiatement.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2002/dsa-204.data"
# $Id$
#use wml::debian::translation-check translation="1.3" maintainer="DFS Task Force"

