<define-tag description>Dépassement de tampon</define-tag>
<define-tag moreinfo>
<p>fc a trouvé un dépassement de tampon dans le code concernant l'expansion 
de variables utilisée par sudo pour son prompt. Vu que sudo est nécessairement 
installé avec comme suid celui de root, un utilisateur local peut 
l'exploiter pour obtenir un accès en tant que root.</p>

<p>Ceci est corrigé dans la version&nbsp;1.6.2-2.2 pour la distribution stable
de Debian et la version&nbsp;1.6.6-1 pour celle testing/unstable.
Nous vous recommandons de mettre à jour le paquet sudo le plus rapidement 
possible.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2002/dsa-128.data"
# $Id$
#use wml::debian::translation-check translation="1.1" maintainer="DFS Task Force"
