#use wml::debian::template title="l10n チームの共同作業"
#use wml::debian::translation-check translation="1.1"

<h1>疑似 URL</h1>

<p>
debian-l10n-* メーリングリストを観察しているプログラムは
subject ヘッダの疑似 URL を解釈します。
疑似 URL は以下の形式でなければなりません。
</p>
<div class="center"><code>[&lt;状態&gt;]
&lt;種類&gt;://&lt;パッケージ&gt;/&lt;ファイル&gt;</code></div>

<p>
<i>状態</i>は以下のうちの一つでなければなりません:
TAF、MAJ、ITT、RFR、LCFC、BTS#&lt;バグ番号&gt;、DONE、HOLD。
</p>

  <ul>
    <li>TAF (<i>Travail &Atilde; Faire</i>、やるべき作業)
      <ul>
	<li>作業が必要な文書があることを示します。</li>
      </ul>
    </li>
    <li>MAJ (<i>Mise &Atilde; Jour</i>、更新)
      <ul>
	<li>更新が必要な文書があることを示します。
	    その作業は前回の翻訳者による予約となります。</li>
      </ul>
    </li>
    <li>ITT (Intent To Translate、翻訳の意志)
      <ul>
	<li>翻訳作業を予定していることを示します。
	    作業が重なるのを避けるのに使われます。</li>
      </ul>
    </li>
    <li>RFR (Request For Review、レビュー要求)
      <ul>
	<li>最初の翻訳草稿が添付されます。
	    メーリングリストにいる他の人には誤りがないかの確認と返信が要求されます
	    (特に指摘事項がない場合はメーリングリスト外が望ましい)。</li>
	<li>大きな変更があった場合は更なる RFR が続くこともあります。</li>
      </ul>
    </li>
    <li>ITR (Intent To Review、レビューの意志)
      <ul>
	<li>保留しているレビューがある場合に LCFC が送られるのを避けるのに使われます。</li>
	<li>(翻訳が大きいものや週末まで時間が取れない等で)
	    レビューが数日以内に終わらないことが予見される場合に主に使われます。</li>
	<li>メール本文ではレビュー可能時期の予測を示しておきましょう。</li>
        <li>ITR での疑似 URL はプログラムには無視されることに注意してください。</li>
      </ul>
    </li>
    <li>LCFC (Last Chance For Comment、コメント締め切り)
      <ul>
	<li>翻訳がレビュー過程での変更を盛り込んで完成し、
	    まもなく適切な場所に送られることを示します。</li>
	<li>ITR がなく、最後の
	    RFR に続く議論が終わって数日が過ぎている場合に発信できます。</li>
	<li>レビューが全くない状態で発信すべきではありません。</li>
      </ul>
    </li>
    <li>BTS#&lt;バグ番号&gt; (バグ追跡システム)
      <ul>
	<li>翻訳を BTS に提出してからバグ番号を登録するのに使われます。</li>
	<li>プログラムは未解決のバグ報告が修正済みや終了になっていないか
	    定期的に確認しています。</li>
      </ul>
    </li>
    <li>DONE
      <ul>
	<li>翻訳が採用され、スレッドを閉じるのに使われます。
	    BTS には送られていない場合に有用。</li>
      </ul>
    </li>
    <li>HOLD
      <ul>
	<li>翻訳を保留するのに使われます。例えば更なる変更が予定され、
	    現時点で更新しても無駄になりそうな場合。</li>
      </ul>
    </li>
  </ul>

<p>
<i>種類</i>は文書の種類を示すもので、
po-debconf、debian-installer、po、po4a、wml 等何でも良い。</p>

<p>
<i>パッケージ</i>はその文書がどのパッケージから来ているものなのか、
パッケージ名を示します。Debian ウェブサイトの WML ファイルの場合は
<i>www.debian.org</i> を使うか、指定しないでください。
</p>

<p>
<i>ファイル</i>は文書のファイル名で、その文書を一意に識別するため、
ファイルへのパスといった他の情報を含めることも出来ます。
これは通常 <i>lc</i>.po、<i>lc</i> は言語コード
(例えば: ドイツなら de、ブラジル人のポルトガル語なら pt_BR) のようになります。
</p>

<p>
<i>ファイル</i>の構造は選択した種類、そして当然言語に依存します。
原則としてこれは単なる識別子ですが、
ウェブページのこのセクションで翻訳の状態を追跡するため、
以下の形式に沿うことが強く推奨されます。
</p>

<ul>
<li><code>po-debconf://パッケージ名/lc.po</code></li>
<li><code>po://パッケージ名/ソースパッケージ中のパス/lc.po</code></li>
<li><code>debian-installer://パッケージ名/ソースパッケージ中のパス/lc.po</code></li>
<li><code>wml://CVS 中のパスに使われている言語名</code></li>
<li><code>po4a://パッケージ名/ソースパッケージ中のパス/lc.po</code></li>
</ul>

<p>
BTS の状態はやや特別で、翻訳を BTS
に提出すると、未解決のバグ報告が閉じられたかどうかを確認することにより、
バグ番号を登録すれば l10n-bot がその状態を追跡できます。
したがって、例えば debian-l10n-spanish メーリングリストではこのようになります:
</p>
<div class="center"><code>[BTS#123456] po-debconf://cups/es.po</code></div>

<p>
大量のパッケージを翻訳する予定なら、全て同時に ITT することもできます。
例 (debian-l10n-danish メーリングリストの場合):
</p>
<div class="center"><code>[ITT] po-debconf://{cups,courier,apache2}/da.po</code></div>
<p>
つまり、パッケージをブレース「{}」の間にコンマで区切って列挙します。
余計な空白を入れないように!
</p>
