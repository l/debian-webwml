#use wml::debian::template title="Debian-Installer 正誤表"
#use wml::debian::recent_list
#use wml::debian::translation-check translation="1.175"

<h1 id="squeeze-beta2">Wheezy リリース Beta3 の正誤表</h1>

<p>
これは Debian Installer の wheezy リリース Beta3 における既知の問題のリストです。
ここに挙げていない問題を見つけた場合、問題を記述した<a
href="$(HOME)/releases/stable/i386/ch05s04.html#submit-bug">インストールレポート</a>を送ってください。
</p>

<dl class="gloss">

	<dt>完全なデスクトップ環境をインストールするには、ネットワークミラーのない
	CD1 だけの状態では不足です。</dt>
	<dd>
	デフォルトの環境 (GNOME) ではもうこの問題には遭わないようです。
	</dd>
	
	<dt>無線の接続環境経由でデスクトップをインストールする場合のネットワークの問題。</dt>
	<dd>
	無線の接続環境を使ってデスクトップをインストールする際、
	インストール中のシステムで network-manager
	コンポーネントがそのままでは動作しないことがあります
	(<a href="http://bugs.debian.org/682608">#682608</a>)。
	対処法は /etc/network/interfaces ファイルの wlan0
	についての行をコメントアウトすることです。
	</dd>

	<dt>デスクトップ環境をインストールしているときにネットワークが
	post-install で明らかに失敗している。</dt>
	<dd>
	network-manager は /etc/network/interfaces にある eth0
	ネットワークインターフェイスの設定を無効化します。
	調整を行うスクリプトの実装は非常に初歩的で、netcfg
	によりセットされるプライバシー拡張のオプションに対応していません。
	したがって、例えば GNOME デスクトップ環境や KDE
	デスクトップ環境を選択したことでインストールの過程で
	network-manager をインストールする場合、/etc/network/interface
	post-install 中の「privext 2」をコメントアウトして、ブートプロセスで
	ifupdown が発する警告を出さないようにする必要があります。
	そうしてもネットワークは network-manager により正常に設定できます。
	</dd>

<!-- leaving this in for possible future use...
	<dt>i386: more than 32 mb of memory is needed to install</dt>
	<dd>
	The minimum amount of memory needed to successfully install on i386
	is 48 mb, instead of the previous 32 mb. We hope to reduce the
	requirements back to 32 mb later. Memory requirements may have
	also changed for other architectures.
	</dd>
-->

</dl>
