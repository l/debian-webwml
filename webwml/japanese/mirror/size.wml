#use wml::debian::template title="ミラーサイズ"
#use wml::debian::translation-check translation="1.30"

<h2>Debian アーカイブの大きさは?</h2>

# psql -U nobody projectb
# projectb=> select sum(files.size) from files,binaries,architecture
#            where architecture.id=binaries.architecture and
#            architecture.arch_string='i386' and files.id=binaries.file;
# projectb=> select architecture.arch_string as Architecture, sum(files.size)
#            from files,binaries,architecture where
#            architecture.id=binaries.architecture and files.id=binaries.file
#            group by architecture.arch_string;

# however, this might not work completely reliably, it will report smaller
# figures. dunno exactly why, elmo didn't have a good answer.


<p>このページにあげられている数値は、
2006 年 12 月現在のアーカイブのものです。</p>

<dl>
<dt><strong>全体</strong></dt>
  <dd>約 205 GB</dd>

<dt><strong>アーキテクチャ非依存のパッケージ</strong></dt>
  <dd>約 16.5 GB</dd>

<dt><strong>リリースされたアーキテクチャ</strong> (旧安定版、安定版、テスト版、不安定版)</dt>
  <dd>約 11.5 GB から 15.3 GB の範囲</dd>

<dt><strong>リリースされていないアーキテクチャ</strong></dt>
  <dd>1.6 GB から 11 GB のどこかだが増加中</dd>

<dt><strong>ソース</strong></dt>
  <dd>約 27 GB</dd>
</dl>

<p>アーカイブは常に増加し続けていることに注意してください。
テスト版はリリース時期になると特に増加するでしょう。
また、特定のディストリビューションを除外してミラーのサイズを減少させることは推奨されていません。
除外するなら特定のアーキテクチャ、それでも不十分ならソースを除外してください。</p>

<h3>Debian CD アーカイブの大きさは?</h3>

<p>CD アーカイブはミラーによって全く違ってきます &mdash; Jigdo
ファイルはアーキテクチャごとに約 50 MB ですが完全な
DVD/CD はそれぞれ約 9 GB、さらに 'update' CD イメージ、Bittorrent
ファイル等の容量が追加されます。</p>

<p>CD アーカイブのミラー作業についての詳細は、<a
href="../CD/mirroring/">Debian CD ミラー</a>のページを参照してください。</p>

<h3>Debian non-US アーカイブの大きさは?</h3>

<p>non-US アーカイブは全体で約 1.1 GB です。リリースされた
アーキテクチャごとに 60 - 110 MB で、リリースされていないアーキテクチャ
はそれらより少ないです。</p>

<p>(non-US アーカイブは 2005 年から非推奨となっていることに注意してください。)</p>

<h2>ウェブページの大きさは?</h2>

<p>私たちのウェブページのうちミラーされる部分 (www.debian.org)
は、約 2 GB です。</p>
