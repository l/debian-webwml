#use wml::debian::cdimage title="Debian CD イメージの rsync ミラー" BARETITLE=true
#use wml::debian::translation-check translation="1.4"

<p>以下に示すのは Debian CD イメージの
<a href="http://rsync.samba.org/">rsync</a> ミラーです。
一部のミラーは完全に最新の状態というわけではないかもしれません。このリストは
Debian の <em>公式</em> イメージを置くサイトのみを掲げています。
</p>

<p><strong>注記:</strong> これらのサイトは必ずしもイメージを FTP や HTTP
プロトコルで提供しているわけではありません - 試さないでください。
これらのサイトは rsync プロトコルでイメージを提供しています -
これはこのサイトの目的ではずっと効率がよいためです。もし HTTP/FTP
のダウンロードサイトを探していると言うことなら、
<a href="../http-ftp/">HTTP/FTP ミラーの一覧</a> をご覧ください。</p>

<p>もし多数のサイトから "Access Denied"
というメッセージを受け取るということなら、あなたはたぶん rsync (873)
ポートを遮断したファイアウォールの中にいるのでしょう。
ファイアウォールの設定を変更するよう管理者に問い合わせてください。</p>

#use wml::debian::countries
#include "$(ENGLISHDIR)/CD/mirroring/cdimage_mirrors.list"
