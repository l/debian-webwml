<define-tag description>安全でない一時ファイルの扱い、mod_rewrite 脆弱性</define-tag>
<define-tag moreinfo>WireX は htdigest と htpasswd でいくつかの場合に
一時ファイルが安全でないやり方で作成されていることを発見しました。
どちらのプログラムも setuid や setgid を使っていないので影響は最小限
にとどまります。Apache グループは mod_rewrite 中の脆弱性で、リモート
から任意のコードを Web サーバ上で実行できるセキュリティバグの修正
を公開しています。</define-tag>

#use wml::debian::translation-check translation="1.2"

# do not modify the following line
#include '$(ENGLISHDIR)/security/2001/dsa-021.data'
