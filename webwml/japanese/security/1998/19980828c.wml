#use wml::debian::translation-check translation="1.10"
<define-tag description>サービス拒否の脆弱性</define-tag>
<define-tag moreinfo>
<p>Dag-Erling Coidan Smørgrav さんにより、Debian GNU/Linux 2.0
で配布されている apache には同一のヘッダが繰り返された場合に
O(n²) のメモリを消費する可能性があり、
サービス拒否悪用の脆弱性があるという報告を受け取っています。</p>

<p>直ちに apache パッケージをアップグレードすることを勧めます。</p>
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980828c.data'
