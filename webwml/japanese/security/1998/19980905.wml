#use wml::debian::translation-check translation="1.5"
<define-tag description>nslookup および dig でバッファオーバフローが可能</define-tag>
<define-tag moreinfo>
<p>現在の Linux ディストリビューションに収録されている nslookup および dig
ユーティリティでバッファオーバフローが可能だという報告を受け取っています。</p>

<p>直ちに bind パッケージをアップグレードすることを勧めます。</p>
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980905.data'
