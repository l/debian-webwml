#use wml::debian::cdimage title="Lista dos servidores rsync espelho ('mirror') para as imagens de CD Debian" BARETITLE=true
#use wml::debian::translation-check translation="1.4"

<p>Estes são os endereços dos servidores <a
href="http://rsync.samba.org/">rsync</a> espelho ("mirror") para a
Imagem de CD Debian.
Note que alguns servidores podem não estar completamente atualizados.
Esta lista contem apenas aqueles sites que trazem imagens Debian
<em>oficiais</em>.</p>

<p><strong>Nota:</strong> Estes sites <em>não</em> oferecem
necessariamente imagens  FTP ou HTTP - não adianta tentar! Ao invés
disto, eles usam o protocolo rsync, que é muito mais eficiente para os
nossos propósitos. Se estiver procurando por links para baixar via
HTTP/FTP, veja a  <a href="../http-ftp/">lista dos servidores
espelho HTTP/FTP</a>.</p>

<p>Se receber a mensagem "Access Denied" de vários sites, provavelmente
é porque você está atrás de um firewall que está bloqueando a porta
rsync (873). Peça a seu administrador que reconfigure o firewall.</p>

#use wml::debian::countries
#include "$(ENGLISHDIR)/CD/mirroring/cdimage_mirrors.list"
