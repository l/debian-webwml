#use wml::debian::template title="Versões/Lançamentos Debian"
#include "$(ENGLISHDIR)/releases/info"
#use wml::debian::translation-check translation="1.44" maintainer="Felipe Augusto van de Wiel (faw)"

<p>O Debian sempre tem pelo menos três versões em manutenção ativa:
&ldquo;stable&rdquo; (estável), &ldquo;testing&rdquo; e
&ldquo;unstable&rdquo; (instável).

<dl>
<dt><a href="stable/">stable</a> (estável)</dt>
  <dd>A distribuição &ldquo;stable&rdquo; (estável) contém a última versão
  oficialmente lançada do Debian.
<p>
  Esta é a versão de produção do Debian, a que nós primeiramente
  recomendamos que seja usada.
<p>
  A atual distribuição &ldquo;stable&rdquo; do Debian GNU/Linux é a versão
  <current_release>, codinome <em><current_release_name></em>.  Ela foi
  lançada em <current_release_date>.
  </dd>

<dt><a href="testing/">testing</a></dt>
  <dd>A distribuição &ldquo;testing&rdquo; contém pacotes que ainda não foram
  aceitos na versão &ldquo;stable&rdquo;, mas estão na fila para tanto. A
  principal vantagem de usar esta distribuição é que ela possui versões
  mais recentes de software.
<p>
  Veja o <a href="$(DOC)/FAQ/">Debian FAQ</a> para mais informações sobre
  <a href="$(DOC)/FAQ/ch-ftparchives#s-testing">o que é
  &ldquo;testing&rdquo;</a> e
  <a href="$(DOC)/FAQ/ch-ftparchives#s-frozen">como ela se torna
  &ldquo;stable&rdquo;</a>.
<p>
  A atual distribuição &ldquo;testing&rdquo; é <em><current_testing_name></em>.
  </dd>

<dt><a href="unstable/">unstable</a> (instável)</dt>
  <dd>A distribuição &ldquo;unstable&rdquo; é onde o desenvolvimento ativo do
  Debian ocorre. Geralmente, esta distribuição é executada por desenvolvedores
  e por aqueles que gostam de viver no limite.
<p>
  A distribuição &ldquo;unstable&rdquo; é chamada <em>sid</em>.
  </dd>
</dl>

<H2>Índice dos lançamentos</H2>

<ul>
 <if-stable-release release="etch">
  <li><a href="lenny/">A próxima versão do Debian tem o codinome
    &lsquo;lenny&rsquo;</a>
      -- nenhuma data de lançamento foi definida
  <li><a href="etch/">Debian GNU/Linux 4.0 (&lsquo;etch&rsquo;)</a>
      -- versão estável atual
  <li><a href="sarge/">Debian GNU/Linux 3.1 (&lsquo;sarge&rsquo;)</a>
      -- versão estável obsoleta
 </if-stable-release>     
 <if-stable-release release="sarge">
  <li><a href="etch/">A próxima versão do Debian tem o codinome
    &lsquo;etch&rsquo;</a>
      -- nenhuma data de lançamento foi definida
  <li><a href="sarge/">Debian GNU/Linux 3.1 (&lsquo;sarge&rsquo;)</a>
      -- versão estável atual
 </if-stable-release>     
  <li><a href="woody/">Debian GNU/Linux 3.0 (&lsquo;woody&rsquo;)</a>
      -- versão estável obsoleta
  <li><a href="potato/">Debian GNU/Linux 2.2 (&lsquo;potato&rsquo;)</a>
      -- versão estável obsoleta
  <li><a href="slink/">Debian GNU/Linux 2.1 (&lsquo;slink&rsquo;)</a> 
      -- versão estável obsoleta
  <li><a href="hamm/">Debian GNU/Linux 2.0 (&lsquo;hamm&rsquo;)</a>
      -- versão estável obsoleta
</ul>

<p>As páginas web para as versões obsoletas do Debian são mantidas
intactas, mas as distribuições propriamente ditas só podem ser encontradas
num <a href="$(HOME)/distrib/archive">repositório</a> separado.

<p>Veja o <a href="$(HOME)/doc/FAQ/">Debian FAQ</a> para uma explicação de
<a href="$(HOME)/doc/FAQ/ch-ftparchives#s-sourceforcodenames">onde
todos estes codinomes vêm</a>.

<h2>Integridade dos dados nas versões</h2>

<p>A integridade dos dados é garantida por um arquivo <code>Release</code>
assinado digitalmente. Para garantir que todos os arquivos na versão
pertençam a ela, checksums MD5 de todos os arquivos <code>Packages</code>
são copiados dentro do arquivo <code>Release</code>.</p>

<p>Assinaturas digitais para este arquivo são armazenadas no arquivo
<code>Release.gpg</code>, usando a versão atual da chave de assinatura
do repositório.</p>
