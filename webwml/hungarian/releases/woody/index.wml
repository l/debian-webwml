#use wml::debian::template title="Információk a Debian &quot;woody&quot; verziójáról" BARETITLE="true"
#use wml::debian::translation-check translation="1.7"

# translated by Hajnalka Hegedûs <heha@inf.elte.hu>

<p>

A Debian <a href="../potato/">potato</a> után következõ verziójának neve 
"woody". A verziószáma feltehetõen "2.3" vagy "3.0" lesz.

<p>Ez a disztribúció jelenleg tesztelési fázisban van. Ez azt jelenti, hogy
egyes dolgok <em>összeomolhatnak</em>, ha ezt a verziót használod. Aki woodyt
futtat, annak figyelemmel kell kísérnie a változásokat a fejlesztõi listákon;
a teszt disztribúció stabilabb, mint az unstable, ennek ellenére óvatosnak
kell lenned.

<p>Mint általában, <strong>most sem</strong> állítottunk fel külön kiadási
célokat, és <strong>nem</strong> határoztunk meg elõre semmilyen kiadási
dátumot. Leegyszerûsítve: "A Debian akkor jelenik meg, amikor eljön az
ideje".

<p>A woody megjelenésével nagyobb verziófrissítések várhatóak, mint például
2.4-es kernel, GLibC 2.2, GCC 3.0, XFree86 4.0 és Perl 5.6. Szintén
valószínû, hogy a <code>debconf</code> csomagot fogja használni minden olyan
csomag, amely a telepítõscript futtatása közben információkat kér a
felhasználótól.

<p>Ha APT-et használsz, a teszt disztribúció csomagjainak eléréséhez az
alábbi sorokat helyezd el a <code>/etc/apt/sources.list</code> fájlba:

<pre>
  deb http://http.us.debian.org/debian testing main contrib non-free
  deb http://non-us.debian.org/debian-non-US testing/non-US main non-free
</pre>

<p>Bõvebb információkért olvasd el az <code>apt-get</code>(8) és a
<code>sources.list</code>(5) kézikönyvoldalakat.


# Local variables:
# mode sgml
# sgml-indent-datanil
# sgml-doctype"../.doctype
# End:
