#use wml::debian::template title="Rozmiar serwerów lustrzanych Debiana"
#use wml::debian::translation-check translation="1.22"

<h2>Ile miejsca zajmuj± dane umieszczone na FTP Debiana?</h2>

# psql -U nobody projectb
# projectb=> select sum(files.size) from files,binaries,architecture
#            where architecture.id=binaries.architecture and
#            architecture.arch_string='i386' and files.id=binaries.file;
# projectb=> select architecture.arch_string as Architecture, sum(files.size)
#            from files,binaries,architecture where
#            architecture.id=binaries.architecture and files.id=binaries.file
#            group by architecture.arch_string;

# however, this might not work completely reliably, it will report smaller
# figures. dunno exactly why, elmo didn't have a good answer.

<p>Liczby na tej stronie ukazuj± stan z 21 kwietnia 2004.

<dl>
<dt><strong>ca³o¶æ</strong></dt>
  <dd>oko³o 100 GB.</dd>

<dt><strong>wydane architektury (stabilna, testowa i niestabilna)</strong></dt>
  <dd>od 5.5 GB do 8 GB (oko³o), pakiety niezale¿ne od architektury równie¿
 		zajmuj± oko³o 8GB.</dd>

<dt><strong>architektury niewydane</strong></dt>
  <dd>przeciêtnie 0.6 GB ale ro¶nie</dd>
</dl>

<p>Zauwa¿, ¿e archiwum ci±gle ro¶nie; zw³aszcza wersja testowa jako wersja rozwijana.
poza tym nie polecamy zmniejszania rozmiaru serwera lustrzanego
przez wy³±czanie pewnych dystrybucji, architektur lub nawet ¼róde³.

<h3>Ile miejsca zajmuje wersja non-US?</h3>

<p>Ca³e archiwum non-US zajmuje oko³o 1.1 GB. Ka¿da wydana architektura zajmuje
oko³o 60-110 MB, trochê mniej miejsca przypada na ka¿d± z niewydanych architektur.

<h2>Ile zajmuj± strony Debiana?</h2>

<p>Czê¶æ serwera lustrzanego naszej strony (www.debian.org) ma oko³o 1.1 GB.</p>
