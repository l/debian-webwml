#use wml::debian::translation-check translation="1.1"
<define-tag description>przepe³nienia buforów</define-tag>
<define-tag moreinfo>
<p>"infamous41md" oraz Chris Evans odkryli kilka przepe³nieñ buforów opartych na stosach w pakiecie xpdf, nar¿edziu do obs³ugi plików PDF (Portable Document Format), które s± tak¿e obecne w tetex-bin, binarnych plikach dla teTeX, które mog± prowadziæ do zablokowania systemu poprzez rozbicie siê aplikacji, b±d¼ te¿ do uruchomienia dowolnego kodu.</p>

<p>Dla starej stabilnej dystrybucji (woody) b³edy te zosta³y
poprawione w wersji 1.0.7+20011202-7.7.</p>

<p>Dla stabilnej dystrybucji (sarge) b³êdy te zostan± poprawione
w wersji 2.0.2-30sarge4.</p>

<p>Dla niestabilnej dystrybucji (sid) b³êdy te zosta³y poprawione w
wersji 0.4.3-2 of poppler against which tetex-bin links.</p>

<p>Rekomendujemy, by¶ uaktualni³ swój pakiet tetex-bin.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2006/dsa-937.data"
# $Id$
