#use wml::debian::translation-check translation="1.2"
<define-tag description>B³±d przy rozbiorze (parsing) ASN.1 </define-tag>
<define-tag moreinfo>
<p>Steve Henson z zespo³u j±dra OpenSSL zidentyfikowa³ i opracowa³ ³atê
dla kilku podatno¶ci w kodzie OpenSSL ASN1, które zosta³y odkryte po uruchomieniu
zestawu testów przez British National Infrastructure Security Coordination Centre (NISCC).</p>
</p>

<p>Poza tym zidentyfikowano b³ad w protokole OpenSSL SSL/TLS który powoduje
¿e OpenSSL analizuje certyfikat klienta z SSL/TLS gdy ten powinien byæ odrzucony
jako b³±d w protokole.
</p>

<p>Projekt Common Vulnerabilities and Exposures identyfikuje nastêpuj±ce problemy:
</p>

<ul>

<li><a href="http://cve.mitre.org/cgi-bin/cvename.cgi?name=CAN-2003-0543">CAN-2003-0543</a>:

<p>Przepe³nienie typu "Integer overflow" w OpenSSL umo¿liwiaj±ce atakuj±cemu z zewn±trz
spowodowanie odmowy us³ugi (za³amanie) poprzez certyfikat SSL klienta z okre¶lon± warto¶ci±
ASN.1.</p>

<li><a href="http://cve.mitre.org/cgi-bin/cvename.cgi?name=CAN-2003-0544">CAN-2003-0544</a>:

<p>OpenSSL nieprawid³owo ¶ledzi ilo¶æ znaków w okre¶lonym wej¶ciu ASN.1, co pozwala
atakuj±cemu z zewn±trz spowodowanie odmowy us³ugi (za³amanie) poprzez certyfikat klienta SSL,
powoduj±c czytanie poza koñcem bufora gdy u¿ywany jest d³ugi format.
</p>

<li><a href="http://cve.mitre.org/cgi-bin/cvename.cgi?name=CAN-2003-0545">CAN-2003-0545</a>:

<p>Podatno¶æ "Double-Free" pozwalaj±ca atakuj±cemu z zewn±trz spowodowaæ
odmowê us³ugi (za³amanie) oraz potencjalne uruchomienie arbitralnego kodu poprzez
certyfikat klienta SSL z okre¶lonym nieprawid³owym kodowaniem ASN.1. Ta dziura
istnia³a tylko w OpenSSL 0.9.7 a tutaj jest wymieniona tylko jako nawi±zanie.
</p>

</ul>

<p>W dystrybucji stabilnej (woody) problem ten zosta³ poprawiony w wersji openssl095
0.9.5a-6.woody.3.</p>

<p>Pakietu tego nie ma w dystrybucji niestabilnej (sid) oraz testowej (sarge).
</p>

<p>Zalecamy aktualizacjê swoich pakietów libssl095a i zrestartowanie us³ugi z t±
bibliotek±. Debian nie dostarcza ¿adnych pakietów które s± po³±czone z t± bibliotek±.
</p>

<p>Nastêpuj±ce polecenie (dziêki uprzejmo¶ci Raya Dassena) wy¶wietla listê
nazw uruchomionych procesów które mapuj± libssl095 w ich przestrzeni adresowej:
</p>

<pre>
    find /proc -name maps -exec egrep -l 'libssl095' {} /dev/null \; \
    | sed -e 's/[^0-9]//g' | xargs --no-run-if-empty ps --no-headers -p | \
    sed -e 's/^\+//' -e 's/ \+/ /g' | cut -d ' ' -f 5 | sort | uniq
</pre>

<p>Powiniene¶ zrestartowaæ zale¿ne us³ugi.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-394.data"
