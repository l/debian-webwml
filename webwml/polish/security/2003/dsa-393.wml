#use wml::debian::translation-check translation="1.2"
<define-tag description>odmowa us³ugi</define-tag>
<define-tag moreinfo>
<p>Dr. Stephen Henson (<email "steve@openssl.org">), u¿ywaj±c pakietu testuj±cego
dostarczanego przez NISCC (<url "http://www.niscc.gov.uk/">), odkry³ kilka b³edów
w kodzie OpenSSL ASN.1. W po³±czeniu z b³êdem powoduj±cym ¿e OpenSSL
parsuje certyfikat klienta nawet wtedy gdy nie powinien tego robiæ, b³êdy te
mog± spowodowaæ odmowê us³ugi (DoS) systemu u¿ywaj±cego kodu OpenSSL, w zale¿no¶ci
jak ten kod jest u¿ywany. Na przyk³ad choæ apache-ssl i ssh u¿ywaj± bibliotek OpenSSL
nie powinny byæ podatne na to zagro¿enie. Niemniej jednak inne aplikacje u¿ywaj±ce SSL
mog± byæ na te b³edy podatne tak wiêc zaleca siê aktualizacje OpenSSL.
</p>

<p>W aktualnej stabilnej dystrybucji (woody) powy¿sze problemy zosta³y wyeliminowane
w wersji 0.9.6c-2.woody.4.</p>

<p>W niestabilnej dystrybucji (sid) powy¿sze problemy zosta³y wyeliminowane
w wersji 0.9.7c-1.</p>

<p>Zalecamy aktualizacjê pakietu openssl. Aby aktualizacja odnios³a skutek, nale¿y pamiêtaæ
o restarcie us³ug u¿ywaj±cych biblioteki libssl.
</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-393.data"
