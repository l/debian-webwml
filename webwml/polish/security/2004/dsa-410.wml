#use wml::debian::translation-check translation="1.1"
<define-tag description>przepe³nienie bufora</define-tag>
<define-tag moreinfo>
<p>Odkryto naruszenie bezpieczeñstwa w libnids, bibliotece u¿ywanej przy analizie ruchu sieci IP,
z pomoc± którego odpowiednio zmodyfikowany datagram TCP mo¿e spowodowaæ
uszkodzenie w pamiêci i potencjalne wywo³anie w³asnego kodu z
przywielejami u¿ytkownika uruchamiaj±cego program u¿ywaj±cy libnids
(taki jak dsniff).</p>

<p>W stabilnej dystrybucji (woody) powy¿szy problem zosta³ wyeliminowany w wersji
1.16-3woody1.</p>

<p>W dystrybucji niestabilnej (sid) problem ten bêdzie nied³ugo rozwi±zany.</p>

<p>Zalecamy aktualizacjê pakietu libnids.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2004/dsa-410.data"
