#use wml::debian::translation-check translation="1.5"
<define-tag description>przepe³nienie bufora</define-tag>
<define-tag moreinfo>

<p>Ta porada bezpieczeñstwa poprawia DSA 458-1, która powodowa³a b³êdy
segmentacji w gethostbyaddr z wej¶ciem ró¿nym od localhost. Ta aktualizacja
wy³±cza obs³ugê IPv6 na wszystkich architekturach.</p>

<p>Oryginalna porada brzmia³a:</p>

<blockquote>

<p>Sebastian Schmidt odkry³ b³±d przepe³nienia bufora w 
funkcji getaddrinfo Pythona, który mo¿e pozwoliæ adresowi IPv6,
podanego przez zdalnego atakuj±cego poprzez DNS na nadpisanie pamiêci na stosie.</p>

<p>B³±d ten istnieje w pythonie 2.2 i 2.2.1 i tylko wtedy gdy wy³±czona jest 
obs³uga IPv6. Pakiet python2.2 w Debianie woody uwzglêdnia te zastrze¿enia
(pakiet 'python' ich nie uwzglêdnia).</p>

</blockquote>

<p>W dystrybucji stabilnej (woody) powy¿szy b³±d zosta³ naprawiony
w wersji 2.2.1-4.5.</p>

<p>Dystrybucja testowa i niestabilna (sarge i sid) nie s± nara¿one 
na ten b³±d.</p>

<p>Zalecamy aktualizacjê pakietu python2.2.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2004/dsa-458.data"
