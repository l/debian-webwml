#use wml::debian::translation-check translation="1.2"
<define-tag description>zagro¿enie zewnêtrzne</define-tag>
<define-tag moreinfo>
<p>Zarz±dca konta (account management) CVS pserver (u¿ywany przy przydzielaniu
dostêpu do repezytorium CVS osobom z zewn±trz) u¿ywa pliku <kbd>CVSROOT/passwd</kbd>
w ka¿dym repezytorium który zawiera konta i ich informacje autentyfikacyjne, a tak¿e
nazwê lokalnego konta uniksowego u¿ywanego przy wykorzystywaniu konta pserver.
Poniewa¿ CVS nie wykonuje sprawdzania jakie zosta³o wyszczególnione konto uniksowe,
ka¿dy kto mo¿e modyfikowaæ <kbd>CVSROOT/passwd</kbd> mo¿e te¿ uzyskaæ
dostêp do wszystkich lokalnych u¿ytkowników na serwerze CVS w³±cznie z kontem root.</p>

<p>Problem zosta³ rozwi±zany w zewnêtrznej wersji 1.11.11 poprzez uniemo¿liwenie
rootowi na dzia³anie na pserver. W przypadku Debiana problem rozwi±zano w wersji
1.11.1p1debian-9 na dwa ró¿ne sposoby:</p>

<ul>
<li>pserver nie pozwala ju¿ na u¿ywanie konta root przy dostêpie do repezytoriów</li>

<li>Wprowadzono nowy <kbd>/etc/cvs-repouid</kbd> który mo¿e byæ u¿ywany
przez administratora systemu do przes³oniêcia konta uniksowego u¿ywanego przy dostêpie
do repezytorium. Wiêcej informacji o tej zmianie mo¿na uzyskaæ pod tym adresem:
<url "http://www.wiggy.net/code/cvs-repouid/"></li>
</ul>

<p>Dodatkowo CVS pserver mia³ b³±d w module parsowania ¿±dañ, który
móg³ byæ u¿yty do stworzenia plików i katalogów poza repezytorium. Zosta³o to wyeliminowane
w zewnêtrznej wersji 1.11.11, a w Debianie w wersji 1.11.1p1debian-9.</p>

<p>Na koniec, zmieniono umask u¿ywany dla &ldquo;cvs init&rdquo; i
&ldquo;cvs-makerepos&rdquo; aby zabezpieczyæ repezytoria przed mo¿liwo¶ci±
stworzenia ich z prawami zapisu grupy.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2004/dsa-422.data"
