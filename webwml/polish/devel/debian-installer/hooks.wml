#use wml::debian::template title="Dodatki do instalatora"
#use wml::debian::translation-check translation="1.3"

    <p>To jest lista dostêpnych modu³ów i rozszerzeñ do instalatora.</p>

    <dl>

      <dt>/etc/rcS.d/S[0-9][0-9]* [rootskel]</dt>

      <dd>Je¶li jest to wykonywalny program, zostanie uruchomiony podczas
       uruchamiania instalatora, zanim g³ówne menu zostanie wy¶wietlone.</dd>

      <dt>/cdrom/.disk/udeb_include [anna]<br>/cdrom/.disk/udeb_exclude</dt>

      <dd>Lista pakietów (udeb) do zainstalowania (b±d¼ nie) w ramdysku
       instalatora.</dd>

      <dt>/etc/autopartkit.d/*.table [autopartkit]</dt>

      <dd>Instrukcje partycjonowania dla autopartkit.  Wyboru dokonuje siê
       u¿ywaj±c debconfa.</dd>
      
      <dt>/cdrom/.disk/base_include [base-installer]<br>/cdrom/.disk/base_exclude</dt>
      
      <dd>Zawarto¶æ tych plików jest traktowana jaki lista pakietow do
       zainstalowania (b±d¼ nie) w miejscu przeznaczenia.</dd>

      <dt>/usr/lib/prebaseconfig.d/* [prebaseconfig]</dt>

      <dd>Pliki z tego katalogu s± wykonywane po kolei na koñcu
       instalacji.</dd>

      <dt>/var/lib/partconf/fstab.d/* [partconf?]</dt>

      <dd>partconf i mkfstab odczytuj± wszystkie pliki w katalogu i dodaj±
liniê (zmodyfikowan± lub nie) do wygenerowanego /target/etc/fstab</dd>

      <dt>/usr/share/partitioner/${ARCH}.sh [partitioner?]<br>/usr/share/partitioner/common.sh</dt>
      <dd>partitioner najpierw próbuje wywo³aæ skrypt dla aktualnej
       architektury, a je¶li skrypt nie istnieje uruchomi common.sh z wyborem
       dysku twardego jako argument. Skrypt wtedy mo¿e zrobiæ cokolwiek jest
       potrzebne do przepartycjonowania dysku twardego na danej architekturze 
       (np. uruchomiæ fdisk (tekst) lub cfdisk (slang))</dd>

      <dt>/var/lib/partconf/block.d/* [partconf?]</dt>

      <dd>partconf uruchamia dla ka¿dej partycji wszystkie skrypty z tego
       katalogu, wiêc niektóre pliki udeb mog± umie¶ciæ tutaj skrypty, 
       a nastêpnie zablokowaæ wyj¶cie pewnych partycji; bardzo u¿yteczne 
       dla obs³ugi raid/lvm.
	<br>(w³±czenie planowane przez Thorstena Sautera )
      </dd>

      <dt>apt-install [rootskel]</dt>

      <dd>Program u¿ywany w skryptach postinst by pytaæ, które pakiety maj±
       byæ zainstalowane w systemie.</dd>

    </dl>
