#use wml::debian::template title="U¿ywanie WML"
#use wml::debian::translation-check translation="1.10"

<p>
  WML jest metajêzykiem dla stron internetowych. Oznacza to, ¿e
  pobiera on pliki .wml, przetwarza wszystko, co siê w nich znajduje
  (mo¿e to byæ wszystko od prostego HTML do kodu Perla!) i zwraca na
  wyj¶ciu np. plik .html lub .php.
</p>

<p>
  Dokumentacja WML nie jest najprostsza do nauki. Jest ona generalnie
  do¶æ znacznych rozmiarów, aczkolwiek zanim zaczniesz rozumieæ, jak
  dzia³a WML (a jest on do¶æ potê¿ny), najlepiej uczyæ siê na
  przyk³adach. Powiniene¶ zainteresowaæ siê szablonami u¿ywanymi przez
  strony Debiana. Znajdziesz je na 
  <a href="http://cvs.debian.org/webwml/english/template/debian/?cvsroot=webwml">\
  webwml/english/template/debian/</a>.
</p>

<p>
  Zak³adamy tutaj, ¿e masz ju¿ zainstalowany WML na Twoim
  komputerze. Jest on dostêpny jako 
  <a href="http://packages.debian.org/wml">pakiet Debiana</a>.
</p>

<h2>Edytowanie ¼róde³ WML</h2>

<p>
  Jedn± rzecz, któr± bêd± mia³y wszystkie pliki .wml jest jedna lub
  wiêcej linii zaczynaj±cych siê od <code>#use</code>. Nie wolno Ci
  zmieniaæ lub t³umaczyæ sk³adni tych linii, tylko wyra¿enia zawarte w
  cudzys³owiach, po s³owach takich jak: <code>title=</code> (które
  na przyk³ad zmienia tag <code>&lt;title&gt;</code> w wyj¶ciowym pliku).
</p>

<p>
  Zamiast sekcji nag³ówkowej wiêkszo¶æ naszych stron .wml zawiera
  tylko prosty HTML. Je¶li napotkasz tagi takie
  jak <code>&lt;define-tag&gt;</code> lub <code>&lt;: ... :&gt;</code>
  b±d¼ ostro¿ny, gdy¿ separuj± one kod, który jest przetwarzany
  specjalnie przez WML.
</p>

<h2>Budowanie stron internetowych Debiana</h2>

<p>
  Po prostu wydaj komendê <kbd>make</kbd> w katologu
  webwml/&lt;lang&gt;/. Przygotowali¶my pliki makefile, które wywo³uj±
  <kbd>wml</kbd> ze wszystkimi potrzebnymi argumentami.
</p>

<p>
  Je¶li wpiszesz w konsoli <kbd>make install</kbd>, wtedy pliki HTML
  trafi± do katalogu: ../../www/.
</p>

<h2>Dodatkowe funkcje WML, których u¿ywamy</h2>

<p>
  Jedn± z funkcji WML, któr± powszechnie u¿ywamy, jest Perl. Strony
  Debiana nie s± dynamicznie generowane. Perl zrobi wszystko to, co
  zechcesz, w trakcie kompilowania stron WML. Dwa dobre przyk³ady
  jak u¿ywamy Perla na stronach to: tworzenie listy najnowszych
  wiadomo¶ci na stronie g³ównej oraz generowanie odno¶ników do t³umaczeñ
  na koñcu ka¿dej strony.
</p>

# TODO: add the basic stuff from webwml/english/po/README here

<p> 
  Aby móc przebudowywaæ szablony z naszej strony, potrzebny
  jest <code>wml</code> w wersji &gt;= 2.0.6. Natomiast aby
  przebudowaæ szablon <code>gettext</code> dla t³umaczeñ z
  angielskiego, potrzebujesz pakietu <code>mp4h</code> w wersji &gt;=
  1.3.0
</p>

<h2>Specyficzne zagadnienia odno¶nie WML</h2>

<p>
  Jêzyki z multibajtowymi kodowaniami znaków wymagaj± specjalnego
  pre- i post- przetwarzania plików .wml, aby zachowaæ poprawne
  kodowanie. Mo¿na to zrobiæ zmieniaj±c odpowiednio zmienne
  <code>WMLPROLOG</code> oraz <code>WMLEPILOG</code> w
  webwml/&lt;lang&gt;/Make.lang. Zale¿nie od tego, jak dzia³a Twój
  program <code>WMLEPILOG</code>, prawdopodobnie bêdziesz musia³
  równie¿ zmieniæ warto¶æ <code>WMLOUTFILE</code>.
  <br>
    Zobacz, dla przyk³adu, t³umaczenia japoñskie lub chiñskie.
</p>
