#use wml::debian::template title="System ¶ledzenia b³êdów - zg³aszanie b³êdów" NOHEADER=yes NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.44"

<h1>Jak zg³osiæ b³±d w Debianie</h1>

<h2>Wa¿ne rzeczy, na które trzeba zwróciæ uwagê <strong>przed</strong> wys³aniem zg³oszenia</h2>

<p>Prosimy nie zg³aszaæ kilku niezwi±zanych ze sob± b³êdów - zw³aszcza b³êdów w
ró¿nych pakietach - w pojedynczym zg³oszeniu. Wysy³anie ich w postaci
osobnych zg³oszeñ znacznie u³atwia nam ¿ycie.</p>

<p>Przed zg³oszeniem nale¿y sprawdziæ, czy dany b³±d nie zosta³ ju¿ zg³oszony
przez kogo¶ innego. Listy otwartych zg³oszeñ b³êdów s± dostêpne na <a
href="./">stronach WWW</a> i <a href="Access">w innych miejscach</a> - wiêcej
informacji znajdziesz w pozosta³ych dokumentach opisuj±cych system ¶ledzenia
b³êdów.  Mo¿esz dodaæ swój komentarz do istniej±cego zg³oszenia o numerze #<var>&lt;numer&gt;</var>
wysy³aj±c list na adres <var>&lt;numer&gt;</var>@bugs.debian.org</p>

<p>Je¶li nie potrafisz okre¶liæ w którym pakiecie znajduje siê b³±d, wy¶lij list
z pro¶b± o radê na <a href="mailto:debian-user@lists.debian.org">listê
dyskusyjn± u¿ytkowników Debiana</a> (lub jej <a
href="mailto:debian-user-polish@lists.debian.org">polskojêzyczny
odpowiednik</a>). Je¶li problem nie jest zwi±zany z jednym pakietem, ale z jak±¶
us³ug± Debiana, mo¿esz zg³osiæ b³±d w jednym z <a
href="pseudo-packages">pseudo-pakietów</a> lub wys³aæ list na któr±¶ z <a
href="../MailingLists/">list dyskusyjnych</a>.</p>

<p>Je¶li chcesz wys³aæ kopiê swojego zg³oszenia do dodatkowych adresatów (na
przyk³ad na jak±¶ listê dyskusyjn±), nie u¿ywaj zwyk³ych nag³ówków pocztowych,
ale <a href="#xcc">innej metody, opisanej ni¿ej</a>.</p>


<h2>Wysy³anie zg³oszenia b³êdu przy pomocy specjalnego narzêdzia</h2>

<p>Program <code><a
href="http://packages.debian.org/stable/utils/reportbug">reportbug</a></code>
potrafi znacznie u³atwiæ wype³nianie zg³oszeñ o b³êdach przeprowadzaj±c
przez ca³y proces krok po kroku.

Narzêdzie <kbd>querybts</kbd>, dostêpne w tym samym pakiecie 
<a
href="http://packages.debian.org/stable/utils/reportbug">reportbug</a>,
dostarcza wygodny tekstowy interfejs do systemu ¶ledzenia b³êdów.</p>


<p>U¿ytkownicy Emacs-a mog± równie¿ u¿ywaæ polecenia debian-bug zawartego w <code><a href="http://packages.debian.org/stable/utils/debbugs-el">pakiecie debbugs-el</a></code>. Gdy wywo³ane z <kbd> M-x debian-bug</kbd>, zapyta o wszystkie potrzebne informacje podobnie jak <code> reportbug</code>.</p>

<h2>Wysy³anie zg³oszenia b³êdu poczt± elektroniczn±</h2>

<p>Wy¶lij list na adres
<a href="mailto:submit@bugs.debian.org"><code>submit@bugs.debian.org</code></a>,
zwracaj±c uwagê na poni¿sze wskazówki.</p>

<p>Oczywi¶cie jak ka¿dy inny, list ten powinien zawieraæ odpowiedni nag³ówek
<code>Subject</code>. Podany przez Ciebie temat bêdzie pocz±tkowym tytu³em
zg³oszenia b³êdu, wiêc niech niesie ze sob± u¿yteczn± informacjê!</p>

<p>Na pocz±tku tre¶ci wiadomo¶ci powinien znajdowaæ siê <a name="pseudoheader">pseudo-nag³ówek</a>. Znaczy
to, ¿e pierwsza linia tre¶ci wiadomo¶ci powinna wygl±daæ nastêpuj±co:

<pre>
Package: &lt;co¶&gt;
</pre>

<p>W miejsce <code>&lt;co¶&gt;</code> wstaw nazwê pakietu, w którym
znalaz³e¶/znalaz³a¶ b³±d.

<p>Druga linia wiadomo¶ci powinna wygl±daæ nastêpuj±co:

<pre>
Version: &lt;co¶&gt;
</pre>

<p>W miejsce <code>&lt;co¶&gt;</code> wstaw wersjê pakietu.

<p>Nale¿y podaæ poprawn± liniê <code>Package</code> w pseudo-nag³ówku, aby
system ¶ledzenia b³êdów móg³ dostarczyæ wiadomo¶æ do opiekuna danego pakietu.
Mo¿esz zobaczyæ <a href="#findpkgver">przyk³ad</a> ilustruj±cy jak odnale¼æ
nazwê odpowiedniego pakietu.

<p>Pseudo-nag³ówki powinny zaczynaæ siê na pocz±tku linii.</p>

<p>W zg³oszeniu prosimy zamie¶ciæ:

<ul>
<li><em>Dok³adne</em> i <em>kompletne</em> komunikaty b³êdów zarówno
wypisywanych na ekranie jak i zapisywanych do plików dziennika (logi). Jest to
bardzo wa¿ne!
<li>Dok³adny opis tego co wpisa³e¶/³a¶ lub zrobi³a¶, co spowodowa³o problem.
<li>Opis niepoprawnego zachowania: czego dok³adnie siê spodziewa³e¶/³a¶, a co
zaobserwowa³e¶/³a¶. Dobrym sposobem na pokazanie tego jest zamieszczenie zapisu
przyk³adowej sesji.
<li>Sugerowana poprawka, lub nawet ³ata, je¶li tak± posiadasz.
<li>Szczegó³y konfiguracji programu który sprawia problem. Zamie¶æ kompletn±
tre¶æ jego plików konfiguracyjnych.
<li>Wersje wszystkich pakietów, których wymaga pakiet z b³êdem.
<li>Wersje u¿ywanego przez Ciebie j±dra (wpisz <code>uname -a</code>),
biblioteki wspó³dzielonej jêzyka C (wpisz <code>ls -l /lib/libc.so.6</code> lub
<code>dpkg -s libc6 | grep ^Version</code>), oraz inne szczegó³y na temat
Twojego systemu Debian, je¶li wydaj± siê one stosowne. Je¶li masz na przyk³ad
problem z b³êdnym skryptem jêzyka Perl, dobrze bêdzie podaæ wersjê programu
interpretera ,,perl'' (wpisz <code>perl -v</code> lub <code>dpkg -s perl | grep
^Version:</code>).
<li>Istotne szczegó³y na temat sprzêtu w Twoim systemie. Je¶li zg³aszasz problem
ze sterownikiem, zamie¶æ listê <em>wszystkich</em> urz±dzeñ, poniewa¿ problemy
czêsto sprawiaj± konflikty przerwañ lub adresów wej¶cia/wyj¶cia.
</ul>

<p>Zamie¶æ wszystkie szczegó³y, które wydaj± siê zwi±zane z dan± spraw± - bardzo
ma³o prawdopodobne jest, ¿e Twoje zg³oszenie bêdzie za d³ugie z powodu zbyt
du¿ej ilo¶ci informacji. Je¶li pliki, których mo¿na by u¿yæ w celu powtórzenia
b³êdu s± niewielkie (do kilkunastu kilobajtów), zamie¶æ je te¿ (zakoduj je przy
pomocy uuencode je¶li zawieraj± rzadko wystêpuj±ce znaki itp.).</p>

<p>Przeczytaj dokument 
<a href="http://www.chiark.greenend.org.uk/~sgtatham/bugs.html">
Jak zg³aszaæ b³êdy efektywnie</a> by dowiedzieæ siê wiêcej na temat pomocy
deweloperom w rozwi±zywaniu problemów.</p>

<h2><A name="example">Przyk³ad</a></h2>

<p>Zg³oszenie b³êdu, razem z nag³ówkami pocztowymi powinno wygl±daæ mniej wiêcej
tak:

<pre>
  To: submit@bugs.debian.org
  From: pilny@tester.debiana.org
  Subject: Hello mówi `goodbye'

  Package: hello
  Version: 1.3-16

  Kiedy uruchamiam `hello' bez argumentów, ze zwyk³ej sesji shella, wypisuje
  ,,goodbye'' zamiast spodziewanego ,,hello, world''.
  Oto zapis sesji:

  $ hello
  goodbye
  $ /usr/bin/hello
  goodbye
  $

  Sugerujê poprawienie wypisywanego napisu w hello.c.

  U¿ywam Debiana GNU/Linux 2.2, j±dra 2.2.17-pre-patch-13
  i libc6 2.1.3-10.
</pre>

<p><strong>Uwaga:</strong> je¶li tylko to mo¿liwe, postaraj siê napisaæ
zg³oszenie b³êdu w jêzyku angielskim (chyba ¿e wiesz, ¿e opiekun pakietu zna
jêzyk polski). Je¶li nie bêdziesz w stanie tego zrobiæ, popro¶ o pomoc na
przyk³ad na li¶cie <a
href="mailto:debian-user-polish@lists.debian.org">debian-user-polish@lists.debian.org</a>.
Je¶li wy¶lesz zg³oszenie w jêzyku, którego nie zna opiekun pakietu, to i tak on
bêdzie musia³ poprosiæ kogo¶ o przet³umaczenie.</p>

<h2><a name="xcc">Wysy³anie kopii zg³oszeñ b³êdów na inne adresy</a></h2>

<p>Czasem konieczne jest wys³anie kopii zg³oszenia b³êdu gdzie¶ indziej ni¿ na
listê <code>debian-bugs-dist</code> i do opiekuna pakietu (zg³oszenie zwykle tam
w³a¶nie jest przekazywane).</p>

<p>Mo¿na by zrobiæ to wpisuj±c pozosta³e adresy do nag³ówka <code>CC</code>
zg³oszenia, ale wtedy w nag³ówku <code>Reply-To</code> ani <code>Subject</code>
pozosta³ych kopii nie pojawi³by siê numer zg³oszenia. Przy odpowiedzi na
zg³oszenie adresaci kopii prawdopodobnie zostawiliby jako adresata adres
<code>submit@bugs.debian.org</code>, co spowodowa³oby zapisanie ich listu jako
nowego zg³oszenia. Prowadzi to do wielu zduplikowanych zg³oszeñ.</p>

<p><em>W³a¶ciwym</em> sposobem na wys³anie kopii jest u¿ycie nag³ówka
<code>X-Debbugs-CC</code>. Dodaj liniê podobn± do nastêpuj±cej do nag³ówków
listu nowego zg³oszenia (<strong>nie</strong> do pseudo nag³ówka z polem <code>Package</code>):</P>
<pre>
 X-Debbugs-CC: inna-lista@cosmic.edu
</pre>
<p>Spowoduje to, ¿e system ¶ledzenia b³êdów wy¶le kopiê Twojego zg³oszenia na
adres <code>inna-lista@cosmic.edu</code> a tak¿e na listê
<code>debian-bugs-dist</code>.</p>

<P>Unikaj wysy³ania takich kopii na adresy innych zg³oszeñ b³êdów, poniewa¿
zostan± one wy³apane przez oprogramowanie, które zapobiega zapêtlaniu siê
wiadomo¶ci. Poza tym w³a¶ciwie nie ma powodu by u¿ywaæ
<code>X-Debbugs-CC</code> w tym celu, poniewa¿ numer b³êdu nadawany przez
ten mechanizm i tak bêdzie zast±piony nowym; w zamian u¿ywaj zwyk³ego
nag³ówka <code>CC</code>.</p>

<p>Cechê t± mo¿na czêsto wykorzystaæ w u¿yteczny sposób wysy³aj±c zg³oszenie na
adres <code>quiet</code> - patrz ni¿ej.</p>


<h2><a name="severities">Stopnie wa¿no¶ci</a></h2>

<p>Je¶li zg³oszenie dotyczy szczególnie powa¿nego b³êdu, lub jest tylko pro¶b± o
now± opcjê, mo¿esz ustawiæ stopieñ wa¿no¶ci b³êdu przy jego zg³aszaniu. Nie jest to
jednak konieczne, a rozwijaj±cy ustawi± odpowiedni±, je¶li Ty tego nie
zrobisz.</p>

<p>Aby ustawiæ stopieñ wa¿no¶ci b³êdu, dodaj nastêpuj±cy <a href="pseudoheader">pseudo-nag³ówek</a>:

<pre>
Severity: &lt;<var>stopieñ_wa¿no¶ci</var>&gt;
</pre>

<p>W miejsce &lt;<var>stopieñ_wa¿no¶ci</var>&gt; wstaw angielsk± nazwê stopnia wa¿no¶ci. S± one
opisane w <a href="Developer#severities">dokumentacji dla
rozwijaj±cych</a>.</p>


<h2><a name="tags">Dodawanie etykiet</a></h2>

<p>Mo¿esz dodaæ do raportu tak zwan± ,,etykietê'' - ang. <I> tag </I>Na przyk³ad, je¶li
dodajesz poprawkê do swojego raportu, móg³by¶ dodaæ etykietê typu
<code>patch</code>. Nie jest to jednak wymagane a deweloperzy dodadz± etykiety
do Twojego raportu, gdy to bêdzie wymagane.</p>

<p>By dodaæ etykiety, dodaj liniê tak± jak ta do 
<a href="#pseudoheader">pseudo-nag³ówka</a>:</p>

<pre>
Tags: &lt;<var>tags</var>&gt;
</pre>

<p>Zast±p &lt;<var>tags</var>&gt; jedn± z mo¿liwych etykiet,
tak jak opisano to w
<a href="Developer#tags">dokumentacji dla deweloperów</a>. Rozdzielaj od siebie etykiety spacjami, przecinkami lub i spacj± i przecinkiem.</p>

<h2>Ró¿ne adresy zg³oszeñ (zg³oszenia drobnych b³êdów lub zg³oszenia masowe)</h2>

<p>Je¶li zg³aszany b³±d jest drobny (na przyk³ad literówka w dokumentacji lub
trywialny b³±d w budowie pakietu) prosimy ustawiæ odpowiednio stopieñ wa¿no¶ci
b³êdu oraz przes³aæ zg³oszenie na adres
<code>maintonly@bugs</code> zamiast <code>submit@bugs</code>.
Wys³anie na adres <code>maintonly</code> spowoduje, ¿e zg³oszenie zostanie
przekazane tylko do opiekuna, a nie zostanie wys³ane na ¿adn± z list systemu
¶ledzenia b³êdów.</p>

<p>Je¶li zg³aszasz wiele b³êdów na raz, z pewno¶ci± powiniene¶/powinna¶ u¿yæ
adresu <code>maintonly@bugs</code> aby nie powodowaæ zbêdnego ruchu na listach
systemu ¶ledzenia b³êdów. Przed zg³oszeniem wielu podobnych b³êdów nale¿y te¿
wys³aæ podsumowanie na listê <code>debian-bugs-dist</code>.</p>

<p>Je¶li chcesz zamie¶ciæ w systemie ¶ledzenia zg³oszenie wys³ane wcze¶niej do
opiekuna, mo¿esz u¿yæ adresu <code>quiet@bugs</code>. Zg³oszenia wys³ane na ten
adres nie zostan± nigdzie przekazane, jedynie zapisane w systemie.

<p>Je¶li danego adresu zg³oszenia, system ¶ledzenia b³êdów ustawi nag³ówek
<code>Reply-To</code> w przekazywanych wiadomo¶ciach tak, aby odpowiedzi na nie
by³y przetwarzane w taki sam sposób jak oryginalne zg³oszenie. Znaczy to na
przyk³ad, ¿e odpowiedzi na zg³oszenie <code>maintonly</code> trafi± na adres
<var>nnn</var><code>-maintonly@bugs</code> zamiast na
<var>nnn</var><code>@bugs</code>, chyba ¿e kto¶ rêcznie zmieni adres.

<h2>Potwierdzenie</h2>

<p>Normalnie system ¶ledzenia b³êdów po zg³oszeniu b³êdu lub przes³aniu
dodatkowych informacji ode¶le do Ciebie potwierdzenie. ¯eby zapobiec
przes³aniu tego potwierdzenia umie¶æ w swoim li¶cie nag³ówek
<code>X-Debbugs-No-Ack</code> (zawarto¶æ tego nag³ówka nie ma znaczenia,
jednak¿e musi siê on znale¼æ w czê¶ci nag³ówków wiadomo¶ci, a <em>nie</em>
w pseudonag³ówku znajduj±cym siê w polu <code>Package</code>). Je¶li
u¿yjesz tego nag³ówka do zg³oszenia nowego b³êdu, by dowiedzieæ siê jaki
numer mu przyznano konieczne bêdzie sprawdzenie stron WWW.</p>

<p>Zwróæ uwagê na to, ¿e nie mo¿na wy³±czyæ potwierdzenia z serwera
<code>control@bugs</code>, poniewa¿ te wiadomo¶ci mog± zawieraæ informacje
o b³êdach, a po ich przeczytaniu nale¿y podj±æ odpowiednie kroki w celu ich
wyeliminowania.</p>

<h2>Zg³oszenia b³êdów w nieznanych pakietach</h2>

<p>Je¶li system ¶ledzenia b³êdów nie wie kto jest opiekunem danego pakietu,
przeka¿e zg³oszenie na listê <code>debian-bugs-dist</code> nawet je¶li u¿yto
adresu <code>maintonly</code>.</p>

<p>Przy wysy³aniu na adres <code>maintonly@bugs</code> lub
<var>nnn</var><code>-maintonly@bugs</code> nale¿y zwróciæ uwagê na to, aby
zg³oszenie by³o przyporz±dkowane do w³a¶ciwego pakietu, dodaj±c odpowiedni
pseudo-nag³ówek <code>Package</code> na pocz±tku zg³oszenia, lub u¿ywaj±c
<a href="server-control">us³ugi <code>control@bugs</code></a> w celu zmiany /
ustawienia odpowiedniego pakietu je¶li pierwsze ustawienie nie by³o
poprawne.</p>


<h2><a name="findpkgver">U¿ywanie <code>dpkg</code> w celu znalezienia pakietu i
wersji do zg³oszenia</a></h2>

<p>Je¶li zg³aszasz b³±d w konsoli, mo¿esz dowiedzieæ siê który pakiet j±
zainstalowa³ u¿ywaj±c <code>dpkg --search</code>. Zainstalowan± wersjê danego
pakietu mo¿na sprawdziæ przy pomocy <code>dpkg --list</code> lub <code>dpkg --status</code>.
</P>

<P>Na przyk³ad:
<pre>
$ which apt-get
/usr/bin/apt-get
$ type apt-get
apt-get is /usr/bin/apt-get
$ dpkg --search /usr/bin/apt-get
apt: /usr/bin/apt-get
$ dpkg --list apt
Desired=Unknown/Install/Remove/Purge/Hold
| Status=Not/Installed/Config-files/Unpacked/Failed-config/Half-installed
|/ Err?=(none)/Hold/Reinst-required/X=both-problems (Status,Err: uppercase=bad)
||/ Name           Version        Description
+++-==============-==============-============================================
ii  apt            0.3.19         Advanced front-end for dpkg
$ dpkg --status apt
Package: apt
Status: install ok installed
Priority: standard
Section: base
Installed-Size: 1391
Maintainer: APT Development Team &lt;deity@lists.debian.org&gt;
Version: 0.3.19
Replaces: deity, libapt-pkg-doc (&lt;&lt; 0.3.7), libapt-pkg-dev (&lt;&lt; 0.3.7)
Provides: libapt-pkg2.7
Depends: libapt-pkg2.7, libc6 (&gt;= 2.1.2), libstdc++2.10
Suggests: dpkg-dev
Conflicts: deity
Description: Advanced front-end for dpkg
 This is Debian's next generation front-end for the dpkg package manager.
 It provides the apt-get utility and APT dselect method that provides a
 simpler, safer way to install and upgrade packages.
 .
 APT features complete installation ordering, multiple source capability
 and several other unique features, see the Users Guide in
 /usr/doc/apt/guide.text.gz

</pre>

<hr>

#use "otherpages.inc"

#use "$(ENGLISHDIR)/Bugs/footer.inc"
