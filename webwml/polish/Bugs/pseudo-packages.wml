#use wml::debian::template title="System ¶ledzenia b³êdów - pseudo-pakiety" NOHEADER="true" NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.11"

<h1>Pseudo-pakiety systemu ¶ledzenia b³êdów Debiana</h1>

<p>Ta strona zawiera listê pseudo-pakietów, których mo¿na u¿yæ w linii
<code>Package:</code> zg³oszenia b³êdu.</p>

<p>Wiêcej informacji na temat nag³ówka <code>Package:</code> mo¿na znale¼æ na
<a href="Reporting">stronie opisuj±cej procedurê zg³aszania b³êdu</a>.</p>

<hrline>

<:
my $file = "$(ENGLISHDIR)/Bugs/pseudo-packages.description";
open I, "$file" or die "Can't open $file file: $!\n";
while (<I>) { if (/^(\S+)\s+(.*)$/) { my ($foo) = $2; $pseudos{$1} = $foo =~ s/&/&amp;/g; } }
close I;
my $plfile = "$(ENGLISHDIR)/../polish/Bugs/pseudo-packages.translated-descriptions";
if (open I, "$plfile") {
	while (<I>) { if (/^(\S+)\s+(.*)$/) { my ($foo) = $2; $pseudos{$1} = $foo =~ s/&/&amp;/g if exists $pseudos{$1}; } }
	close I;
}
%pseudos || die "%pseudos is not set -- empty $file file?\n";
foreach $a (sort keys %pseudos) {
  if ($a ne "spam" && $a ne "slink-cd") {
    print "<p><code><a href=\"http://bugs.debian.org/$a\">$a</a></code> &mdash; $pseudos{$a}\n";
  }
}
:>

<hrline>

#use "otherpages.inc"

#use "$(ENGLISHDIR)/Bugs/footer.inc"
