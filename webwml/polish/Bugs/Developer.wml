#use wml::debian::template title="System ¶ledzenia b³êdów - informacje dla rozwijaj±cych" NOHEADER=yes NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.51"

<h1>Informacje dla rozwijaj±cych dotycz±ce systemu ¶ledzenia b³êdów</h1>

<p>Pierwszym etapem jest nades³anie zg³oszenia w postaci zwyk³ej wiadomo¶ci
poczty elektronicznej na adres <code>submit@bugs.debian.org</code>. Zg³oszeniu zostaje nadany
numer, zg³aszaj±ca osoba otrzymuje potwierdzenie, a zg³oszenie jest
przekazywane na listê <code>debian-bugs-dist</code>. Je¶li zg³aszaj±cy doda³
nag³ówek <code>Package</code> zawieraj±cy nazwê pakietu, którego opiekun jest
znany, kopia zg³oszenia zostaje wys³ana tak¿e do opiekuna.</p>

<p>Na pocz±tku nag³ówka <code>Subject</code> zostaje dodany napis
<code>Bug#</code><var>nnn</var><code>:</code>, a nag³ówek
<code>Reply-To</code> zostaje ustawiony tak, by zawiera³ zarówno adres
zg³aszaj±cego jak i <var>nnn</var><code>@bugs.debian.org</code>.</p>

<hrline>

<ul>
  <li><a href="#closing">Zamykanie zg³oszenia b³êdu</a></li>
  <li><a href="#followup">Wiadomo¶ci e-mail zwi±zane ze zg³oszeniem</a></li>
  <li><a href="#severities">Stopnie wa¿no¶ci b³êdów</a></li>
  <li><a href="#tags">Znaczniki przy zg³oszeniach</a></li>
  <li><a href="#forward">Zaznaczanie faktu przekazania zg³oszenia b³êdu</a></li>
  <li><a href="#owner">Zmiana w³a¶ciciela b³êdu</a></li>
  <li><a href="#maintincorrect">Nieprawid³owo przyporz±dkowani opiekunowie</a></li>
  <li><a href="#requestserv">Powtórne otwieranie, przekierowywanie i
  inne manipulacje na zg³oszeniach</a></li>
  <li><a href="#subjectscan">Czê¶ciowo wycofana opcja skanowania tematów</a></li>
  <li><a href="#x-debian-pr">Wycofana opcja <code>X-Debian-PR: quiet</code></a></li>
</ul>

<hrline>


<h2><a name="closing">Zamykanie zg³oszenia b³êdu</a></h2>

<p>Zg³oszenia b³êdów w Debianie powinny byæ zamykane w momencie usuniêcia
problemu. Problemy w pakietach mo¿na uznaæ za rozwi±zane tylko wtedy, gdy
pakiet zawieraj±cy poprawkê dla danego b³êdu trafi do archiwum Debiana.</p>

<p>Zazwyczaj jedynymi osobami uprawnionymi do zamykania zg³oszenia s±
zg³aszaj±cy ten b³±d i opiekun(owie) danego pakietu. S± jednak wyj±tki od tej
regu³y, na przyk³ad w przypadku b³êdów zg³oszonych w nieznanych pakietach lub
w niektórych pseudo-pakietach. Je¶li masz w±tpliwo¶ci, nie zamykaj zg³oszenia
- popro¶ o radê na li¶cie debian-devel.</p>

<p>Zg³oszenia nale¿y zamykaæ przez wys³anie wiadomo¶ci e-mail na adres
<var>nnn</var><code>-done@bugs.debian.org</code>. Tre¶æ wiadomo¶ci musi
zawieraæ obja¶nienie sposobu, w jaki zosta³ poprawiony dany b³±d.</p>

<p>Dziêki wiadomo¶ciom e-mail wysy³anym przez system ¶ledzenia b³êdów
zamkniêcie zg³oszenia sprowadza siê do odpowiedzenia na taki list po
uprzedniej zmianie pola <code>To</code> na
<var>nnn</var><code>-done@bugs.debian.org</code> zamiast
<var>nnn</var><code>@bugs.debian.org</code> (adres <var>nnn</var><code>-done</code> ma
te¿ alias <var>nnn</var><code>-close</code>).</p>

<p>Osoba zamykaj±ca zg³oszenie, osoba która zg³osi³a b³±d, oraz lista
<code>debian-bugs-closed</code> otrzymaj± powiadomienie dotycz±ce zmiany stanu
danego zg³oszenia. Do zg³aszaj±cego oraz na listê zostanie tak¿e wys³ana
wiadomo¶æ zawieraj±ca tre¶æ wiadomo¶ci wys³anej na adres
<var>nnn</var><code>-done</code>.</p>


<h2><a name="followup">Wiadomo¶ci e-mail zwi±zane ze zg³oszeniem</a></h2>

<p>System ¶ledzenia b³êdów dodaje do nag³ówka <code>Reply-To</code>
przekazywanej wiadomo¶ci adres osoby zg³aszaj±cej b³±d oraz adres b³êdu
(<var>nnn</var><code>@bugs.debian.org</code>). Nale¿y zwróciæ uwagê na fakt, 
¿e s± to dwa oddzielne adresy.</p>

<p>Je¶li rozwijaj±cy pragnie odpowiedzieæ na zg³oszenie, powinien po prostu
u¿yæ funkcji odpowiedzi swojego programu pocztowego, adresuj±c list zgodnie z
nag³ówkiem <code>Reply-To</code>. <strong>Nie</strong> spowoduje to zamkniêcia
b³êdu.</p>

<p>System ¶ledzenia b³êdów otrzyma wiadomo¶æ wys³an± na adres
<var>nnn</var><code>@bugs.debian.org</code>, przeka¿e j± do opiekuna pakietu, dopisze do
zg³oszenia, a tak¿e przeka¿e na listê <code>debian-bugs-dist</code>.</p>

<p>Rozwijaj±cy mo¿e te¿ wys³aæ wiadomo¶æ e-mail do osoby, która zg³osi³a b³±d
<var>nnn</var> pisz±c na adres
<var>nnn</var><code>-submitter@bugs.debian.org</code>.</p>

<p>Je¶li chcesz wys³aæ wiadomo¶æ, która nie nadaje siê do przekazania na listê
<code>debian-bugs-dist</code>, mo¿esz wys³aæ j± na adres
<var>nnn</var><code>-quiet@bugs.debian.org</code> lub
<var>nnn</var><code>-maintonly@bugs.debian.org</code>. Poczta wysy³ana na pierwszy z tych
adresów jest zapisywana w systemie ¶ledzenia b³êdów, ale nie jest przekazywana
do ¿adnych osób ani list dyskusyjnych. Poczta wysy³ana na adres
<var>nnn</var><code>-maintonly@bugs.debian.org</code> jest zapisywana w systemie i
przekazywana tylko do opiekuna danego pakietu.</p>

<p><em>Nie</em> nale¿y u¿ywaæ opcji ,,odpowiedz wszystkim'' programu pocztowego,
chyba ¿e samodzielnie usunie siê pewnych adresatów. Nale¿y zwróciæ uwagê na
to, aby nie wysy³aæ wiadomo¶ci dotycz±cych istniej±cych zg³oszeñ na adres
<code>submit@bugs.debian.org</code>.</p>

<p>Wiêcej informaji o nag³ówkach powstrzymuj±cych wiadomo¶ci potwierdzaj±ce
(ACK) oraz o wysy³aniu kopii listów za pomoc± Systemu ¶ledzenia b³êdów jest
dostêpnych w <a href="Reporting">instrukcji zg³aszania b³êdów</a>.</p>

<h2><a name="severities">Stopnie wa¿no¶ci b³êdów</a></h2>

<p>System ¶ledzenia b³êdów zapisuje stopieñ wa¿no¶ci ka¿dego ze zg³oszonych b³êdów. Jest
ona domy¶lnie ustawiona na ,,zwyk³±'' (ang. <i><code>normal</code></i>), ale
mo¿na j± zmieniæ dodaj±c do zg³oszenia pseudo-nag³ówek <code>Severity</code>
(patrz <a href="Reporting#pseudoheader">Jak zg³osiæ b³±d</a>) lub przy pomocy
polecenia <code>severity</code> <a href="#requestserv">serwera
pocztowego</a>.</p>

<p>Dostêpne s± nastêpuj±ce stopnie wa¿no¶ci:

<dl>
<dt><code>krytyczny</code> (ang. <code>critical</code>)
<dd>powoduje uszkodzenie niezwi±zanego oprogramowania w systemie (lub ca³ego
systemu) lub powoduje powa¿ne utraty danych, albo powoduje dziurê w
bezpieczeñstwie systemów, na których instaluje siê dany pakiet.

<dt><code>bardzo&nbsp;powa¿ny</code> (ang. <code>grave</code>)
<dd>uniemo¿liwia w ogóle lub w wiêkszej czê¶ci korzystanie z danego pakietu
lub powoduje utratê danych, albo powoduje dziurê w bezpieczeñstwie pozwalaj±c±
na uzyskanie dostêpu do kont u¿ytkowników danego pakietu.

<dt><code>powa¿ny</code> (ang. <code>serious</code>)
<dd>jest <a href="http://release.debian.org/sarge_rc_policy.txt">powa¿nym
naruszeniem polityki Debiana</a> (to znaczy narusza dyrektywê
&quot;musi&quot; (ang <i>&quot;must&quot;</i>) lub &quot;wymagany&quot; (ang.
<i>&quot;required&quot;</i>), lub, w opinii opiekuna pakietu, powoduje, ¿e
pakiet nie nadaje siê do wydania.

<dt><code>wa¿ny</code> (ang. <code>important</code>)
<dd>b³±d maj±cy du¿y wp³yw na u¿yteczno¶æ danego pakietu, nie powoduj±cy
jednocze¶nie jego ca³kowitej bezu¿yteczno¶ci dla kogokolwiek. Aby poznaæ
kompletne zasady dotycz±ce tego, jakie problemy zas³uguj± na te stopnie
wa¿no¶ci, zobacz listê <a
href="http://release.debian.org/sarge_rc_policy.txt">problemów
krytycznych dla wydania Sarge</a>.</p>

<dt><code>zwyk³y</code> (ang. <code>normal</code>)
<dd>warto¶æ domy¶lna, pasuj±ca do wiêkszo¶ci b³êdów.
 
<dt><code>drobny</code> (ang. <code>minor</code>)
<dd>problem nie powoduj±cy uszczerbku w u¿yteczno¶ci pakietu, prawdopodobnie
bardzo ³atwy do usuniêcia.

<dt><code>¿yczenie</code> (ang. <code>wishlist</code>)
<dd>odpowiedni w przypadku pro¶by o jak±¶ funkcjê, a tak¿e w przypadku b³êdów,
które s± bardzo trudne do usuniêcia z powodu za³o¿eñ projektowych.
</dl>

<p><strong>Uwaga:</strong> przy ustawianiu stopnia wa¿no¶ci b³êdu nale¿y u¿ywaæ
angielskich nazw. Serwer obs³uguj±cy te ¿±dania nie zna ich polskich (ani
¿adnych innych) t³umaczeñ.</p>

<p>Pewne stopnie wa¿no¶ci s± traktowane jako 
<em><a href="http://bugs.debian.org/release-critical/">uniemo¿liwiaj±ce
wydanie (ang. <i>release-critical</i>)</a></em>, co znaczy, ¿e b³±d ten bêdzie
mia³ wp³yw na to, czy dany pakiet zostanie wydany w stabilnej edycji Debiana.
Obecnie status taki maj± stopnie <strong>krytyczny</strong>,
<strong>bardzo&nbsp;powa¿ny</strong> i <strong>powa¿ny</strong>.</p>

<h2><a name="tags">Znaczniki przy zg³oszeniach</a></h2>

<p>Ka¿dy b³±d mo¿e mieæ pewn± ilo¶æ znaczników. S± one wy¶wietlane na li¶cie
b³êdów dla ka¿dego z pakietów oraz na pe³nej li¶cie b³êdów.</p>

<p>Znaczniki mo¿na ustawiaæ dodaj±c do zg³oszenia pseudo-nag³ówek
<code>Tags</code> (patrz <a href="Reporting#pseudoheader">Jak zg³osiæ
b³±d</a>), lub przy pomocy polecenia <code>tags</code> <a
href="#requestserv">serwera pocztowego</a>.
Znaczniki nale¿y rozdzielaæ przecinkami lub spacjami.

<p>Dostêpne s± obecnie nastêpuj±ce znaczniki:

<dl>

<dt><code>patch</code> (³ata)
  <dd>W dzienniku danego b³êdu dostêpna jest ³ata lub inny opis ³atwej
  procedury prowadz±cej do rozwi±zania problemu. Je¶li dostêpna ³ata nie
  rozwi±zuje problemu w odpowiedni sposób lub powoduje inne problemy, nie
  nale¿y u¿ywaæ tego znacznika.

<dt><code>wontfix</code> (nie naprawiê)
  <dd>Ten b³±d nie zostanie naprawiony. Mo¿e tak byæ poniewa¿ jest to jeden z
  dwóch równorzêdnych sposobów na osi±gniêcie jakiego¶ celu, a opiekun pakietu
  i zg³aszaj±cy b³±d preferuj± odmienne sposoby. Mo¿e tak te¿ byæ gdy zmiana
  obecnego zachowania spowoduje inne, gorsze problemy dla innych, itp.

<dt><code>moreinfo</code> (wiêcej informacji)
  <dd>Tego b³êdu nie da siê rozwi±zaæ je¶li zg³aszaj±cy nie dostarczy wiêcej
  informacji w sensownym czasie (rzêdu kilku miesiêcy). Znacznik ten przydaje
  siê przy b³êdach typu: &quot;Nie dzia³a.&quot; Co nie dzia³a?

<dt><code>unreproducible</code> (nie da siê powtórzyæ)
  <dd>Tego b³êdu nie da siê powtórzyæ w systemie opiekuna. Do zdiagnozowania
  przyczyny problemu potrzebna jest pomoc osób trzecich.

<dt><code>help</code> (pomocy)
  <dd>Opiekun prosi o pomoc w rozwi±zaniu tego b³êdu.

<dt><code>pending</code> (poprawiany)
  <dd>Znaleziono rozwi±zanie problemu, wkrótce b³±d powinien byæ poprawiony.

<dt><code>fixed</code> (poprawiony)
  <dd>Ten b³±d jest poprawiony, ca³kowicie lub tymczasowo (na przyk³ad przez
  wersjê stworzon± przez osobê nie bêd±c± opiekunem tego pakietu), ale mimo to
  co¶ trzeba jeszcze zrobiæ. Ten znacznik zastêpuje stary stopieñ wa¿no¶ci
  &quot;fixed&quot;.

<dt><code>security</code> (bezpieczeñstwo)
  <dd>B³±d ten opisuje problem z bezpieczeñstwem w danym pakiecie (na przyk³ad
  nieprawid³owe uprawnienia umo¿liwiaj±ce dostêp do danych, które nie powinny
  byæ dostêpne; b³êdy przepe³nienia bufora umo¿liwiaj±ce niepo¿±dane przejêcie
  kontroli nad systemem; b³êdy umo¿liwiaj±ce ataki powoduj±ce odmowê us³ugi,
  itp). Wiêkszo¶æ b³êdów z tym znacznikiem powinno tak¿e mieæ stopieñ wa¿no¶ci
  &quot;krytyczny&quot; lub &quot;bardzo powa¿ny&quot;.

<dt><code>upstream</code> (¼ród³o)
  <dd>Ten b³±d odnosi siê do czê¶ci pakietu pochodz±cej od ¼ród³owego
  opiekuna.

<dt><code>confirmed</code> (potwierdzony)
  <dd>Opiekun sprawdzi³ dane zg³oszenie b³êdu, rozumie je i generalnie zgadza
  siê ze zg³aszaj±cym, ale nie ma jeszcze sposobu na poprawienie b³êdu. (U¿ycie
  tego znacznika jest opcjonalne; jest on przeznaczony g³ównie dla opiekunów,
  którzy musz± radziæ sobie z du¿± ilo¶ci± otwartych zg³oszeñ.)

<dt><code>fixed-upstream</code> (poprawiony przez zewnêtrznego opiekuna)
  <dd>B³±d zosta³ naprawiony przez zewnêtrznego opiekuna, ale nie znajduje
  siê jeszcze w pakiecie (z jakiegokolwiek powodu: prawdopodobnie zbyt
  trudno na³o¿yæ poprawki albo zmiany s± zbyt ma³e by siê nimi zajmowaæ).

<dt><code>fixed-in-experimental</code> (poprawiony w dystrybucji
eksperymentalnej)
  <dd>Ten b³±d zosta³ naprawiony w pakiecie znajduj±cym siê w
  dystrybucji eksperymentalnej, ale jeszcze nie w dystrybucji
  niestabilnej.

<dt><code>d-i</code>
  <dd>Ten b³±d odnosi siê do rozwoju systemu instalacji Debiana
  (debian-installer). Znacznik ten jest przeznaczony dla takich b³êdów, które
  wp³ywaj± na rozwój instalatora mimo ¿e dotycz± pakietów nie bêd±cych
  bezpo¶rednio jego czê¶ci±.

<dt><code>ipv6</code>
  <dd>Ten b³±d wp³ywa na obs³ugê protoko³u IP w wersji 6.

<dt><code>lfs</code>
  <dd>Ten b³±d wp³ywa na obs³ugê du¿ych plików (ponad 2 gigabajty).

<dt><code>l10n</code>
  <dd>Ten b³±d dotyczy lokalizacji pakietu.


<dt><code>potato</code>
  <dd>Ten b³±d odnosi siê do wersji znajduj±cej siê w wydaniu Debiana o nazwie
  kodowej &quot;potato&quot;.

<dt><code>woody</code>
  <dd>Ten b³±d odnosi siê do wersji znajduj±cej siê w wersji Debiana o nazwie
  kodowej &quot;woody&quot;.

<dt><code>sarge</code>
  <dd>Ten b³±d odnosi siê do wersji znajduj±cej siê w niewydanej jeszcze
  wersji Debiana o nazwie kodowej &quot;sarge&quot;.

<dt><code>sarge-ignore</code>
  <dd>Ten b³±d opó¼niaj±cy wydanie ma byæ ignorowany na potrzeby wydania
  sarge. <strong>Ten znacznik powinien byæ u¿ywany tylko przez mened¿era
  wydañ. Nie ustawiaj go samodzielnie bez jego wyra¼nej zgody.</strong>

<dt><code>sid</code>
  <dd>Ten b³±d odnosi siê do wersji znajduj±cej siê w niestabilnej wersji
  Debiana (o nazwie kodowej &quot;sid&quot;).

<dt><code>experimental</code>
  <dd>Ten b³±d odnosi siê do dystrybucji experimental.
</dl>

<p>Ostatnie piêæ znaczników jest u¿ywanych g³ównie przy b³êdach uniemo¿liwiaj±cych
wydanie, w przypadku których wa¿na jest wiedza na temat tego, do których wersji
Debiana siê odnosz±, aby poprawiaæ (lub usuwaæ) odpowiednie pakiety.</p>


<h2><a name="forward">Zaznaczanie faktu przekazania zg³oszenia b³êdu</a></h2>

<p>Rozwijaj±cy, który przekazuje zg³oszenie b³êdu opiekunowi ¼ród³owemu
pakietu, powinien zaznaczyæ ten fakt w systemie ¶ledzenia b³êdów w nastêpuj±cy
sposób:</p>

<p>W polu <code>To</code> wiadomo¶ci e-mail nale¿y umie¶ciæ tylko adres(y)
opiekuna(ów) ¼ród³owego(ych); w polu <code>CC</code> nale¿y umie¶ciæ adres
osoby, która zg³osi³a b³±d oraz adres
<var>nnn</var><code>-forwarded@bugs.debian.org</code>.</p>

<p>Nale¿y poprosiæ osobê wymienion± w polu <code>To</code>, aby pozostawi³a
przy odpowiadaniu adres <var>nnn</var><code>-forwarded@bugs.debian.org</code> w polu
<code>CC</code>, dziêki czemu jej wiadomo¶æ zostanie zapisana razem z reszt±
zg³oszenia.</p>

<p>Kiedy system ¶ledzenia b³êdów otrzyma wiadomo¶æ wys³an± na adres
<var>nnn</var><code>-forwarded</code>, zaznaczy dany b³±d jako przekazany na
adres wymieniony w polu <code>To</code> danej wiadomo¶ci, je¶li b³±d nie ma ju¿ statusu ,,przekazany''.</p>

<p>Mo¿na te¿ ustawiæ informacjê &quot;przekazane do&quot; przy pomocy
odpowiedniej wiadomo¶ci wys³anej na adres <a
href="server-control"><code>control@bugs.debian.org</code></A>.</p>

<h2><a name="owner">Zmiana w³a¶ciciela b³êdu</a></h2>

<p>Zdarza siê, ¿e osoba odpowiedzialna za naprawienie b³êdu nie jest
opiekunem danego pakietu (np. gdy pakietem zajmuje siê grupa opiekunów).
W takim przypadku warto odnotowaæ to w systemie ¶ledzenia b³êdów - ka¿demu
b³êdowi mo¿na przypisaæ w³a¶ciciela.

<p>W³a¶ciciel b³êdu mo¿e zostaæ ustalone przez podanie linii <code>Owner</code>
w pseudo-nag³ówku podczas zg³oszenia b³edu (wiêcej w <a
href="Reportin#pseudoheader">instrukcji zg³aszania b³êdu</a>) lub za pomoc±
poleceñ <code>owner</code> i <code>noowner</code> <a
href="#requestserv">serwera kontroli ¿±dzañ</a>.


<h2><a name="maintincorrect">Nieprawid³owo przyporz±dkowani opiekunowie</a></h2>

<p>Najczê¶ciej powodem przyporz±dkowania do pakietu niew³a¶ciwego opiekuna,
jest to, ¿e opiekun niedawno siê zmieni³, a nowy opiekun jeszcze nie wys³a³ na
serwer nowej wersji pakietu ze zmienionym polem kontrolnym
<code>Maintainer</code>. Opiekun zostanie zmieniony automatycznie gdy na
serwer archiwum zostanie wys³ana nowa wersja pakietu. Je¶li jednak nowa wersja
nie jest wkrótce spodziewana, opiekunowie systemu ¶ledzenia b³êdów mog±
rêcznie zmieniæ t± informacjê. Mo¿na siê z nimi skontaktowaæ pod
adresem <code>override-change@debian.org</code>.


<h2><a name="requestserv">Powtórne otwieranie, przekierowywanie i inne manipulacje na zg³oszeniach</a></h2>

<p>Mo¿liwa jest zmiana przyporz±dkowania b³êdu do pakietu, powtórne otwarcie
omy³kowo zamkniêtego zg³oszenia, modyfikacja informacji mówi±cej o tym do
kogo, o ile w ogóle, zosta³o przekazane zg³oszenie, zmiana stopnia wa¿no¶ci
i tytu³u b³êdu, ustalenia w³a¶ciciela b³êdu a tak¿e ³±czenie i rozdzielanie
poszczególnych zg³oszeñ. Mo¿na to zrobiæ wysy³aj±c odpowiedni± wiadomo¶æ na
adres <code>control@bugs.debian.org</code>.</p>

<p><a href="server-control">Format tej wiadomo¶ci</a> jest opisany w innym
dokumencie dostêpnym na stronie WWW lub w pliku
<code>bug-maint-mailcontrol.txt</code>. Wersjê tekstow± tego dokumentu mo¿na
tak¿e uzyskaæ wysy³aj±c s³owo <code>help</code> na wymieniony wy¿ej adres.</p>


<h2><a name="subjectscan">Czê¶ciowo wycofana opcja skanowania tematów</a></h2>

<p>Wiadomo¶ci przychodz±ce na adres <code>submit@bugs.debian.org</code> lub
<code>bugs@bugs.debian.org</code>, których adres zaczyna siê od
<code>Bug#</code><var>nnn</var> bêd± traktowane tak, jakby by³y wys³ane na
adres <var>nnn</var><code>@bugs.debian.org</code>. Dzieje siê tak ze wzglêdu 
na zgodno¶æ wstecz jak i na to, aby wy³apywaæ pocztê wysy³an± na adres
<code>submit@bugs.debian.org</code> przez pomy³kê (na przyk³ad przez u¿ycie 
opcji odpowiedzi do wszystkich adresatów).</p>

<p>Podobna zasada obowi±zuje dla adresów <code>maintonly</code>,
<code>done</code>, <code>quiet</code> oraz <code>forwarded</code>,
które traktuj± pocztê nadchodz±c± ze znacznikiem w tytule jakby nadesz³a na
odpowiadaj±cy adres <var>nnn-co¶tam</var><code>@bugs.debian.org</code>.</p>

<p>Wiadomo¶ci nadchodz±ce na adresy <code>forwarded@bugs.debian.org</code> i
<code>done@bugs.debian.org</code> - tzn. bez numeru b³êdu w adresie, a nie 
zawieraj±ce numeru w temacie, zostaj± zapamiêtane jako 
&quot;¶mieci&quot; przez parê tygodni i zignorowane.</p>


<h2><a name="x-debian-pr">Wycofana opcja <code>X-Debian-PR: quiet</code></a></h2>

<p>Kiedy¶ mo¿na by³o zapobiec przekazywaniu poczty przez system ¶ledzenia
b³êdów przez dodanie nag³ówka pocztowego <code>X-Debian-PR: quiet</code>.</p>

<p>Nag³ówek ten jest teraz ignorowany. Zamiast niego nale¿y u¿ywaæ adresów
<code>quiet</code> lub <var>nnn</var><code>-quiet</code> (wzglêdnie
<code>maintonly</code> lub <var>nnn</var><code>-maintonly</code>).</p>

<hr>

#use "otherpages.inc"

#use "$(ENGLISHDIR)/Bugs/footer.inc"
