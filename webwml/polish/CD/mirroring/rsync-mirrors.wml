#use wml::debian::cdimage title="Lista serwerów lustrzanych rsync obrazów p³yt CD Debiana" BARETITLE=true
#use wml::debian::translation-check translation="1.4"

<p>
	Tutaj s± adresy kopii lustrzanych p³yt Debiana
	udostêpnianych przy u¿yciu serwera
	<a href="http://rsync.samba.org/">rsync</a>.
	Nale¿y pamiêtaæ, ¿e niektóre serwery mog± nie byæ
	ca³kiem aktualne. Ta lista zawiera tylko miejsca
	udostêpniaj±ce <em>oficjalne</em> obrazy p³yt Debiana.
</p>

<p>
	<strong>Uwaga:</strong>
	Te miejsca <em>niekoniecznie</em> udostêpniaj± obrazy przez
	FTP czy HTTP, wiêc proszê raczej nie próbowaæ takich po³±czeñ!
	U¿ywaj± one protoko³u rsync, który jest bardziej
	odpowiedni dla takich zadañ. Je¶li potrzebne s± odsy³acze
	do zasobów HTTP/FTP, mo¿na zajrzeæ do
	<a href="../http-ftp/">listy kopii lustrzanych HTTP/FTP</a>.
</p>

<p>
	Je¿eli po³±czenie z wiêkszo¶ci± serwerów skutkuje
	komunikatem o b³êdzie &#8222;Access Denied&#8221;,
	prawdopodobnie po drodze znajduje siê firewall,
	który blokuje port rsync (873) i nale¿y skontaktowaæ
	siê z jego administratorem w celu odblokowania tego portu.
</p>

#use wml::debian::countries
#include "$(ENGLISHDIR)/CD/mirroring/cdimage_mirrors.list"
