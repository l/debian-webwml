#use wml::debian::template title="Distribution Archives"
#use wml::debian::translation-check translation="1.3"

#<P>If you need to access one of the old distributions of Debian, you can
#find them at the <A HREF="http://archive.debian.org/debian-archive/">Debian
#Archives</A>, <tt>ftp://archive.debian.org/debian-archive/</tt>.
<p>如果您要找舊版本的 Debian 系統套件，您可以在 <a href="http://archive.debian.org/debian-archive/">Debian Archives</a> 與 <tt>ftp://archive.debian.org/debian-archive/</tt> 找到。</p>

#<P>Releases are stored by their codenames under the dists/ directory.
<p>所有發行的套件都依他們的發行代號放在 dists 目錄下。</p>
<ul>
  #<LI><A HREF="../releases/slink/">slink</A> is Debian 2.1
  <li><a href="../releases/slink/">slink</A> 為 Debian 2.1</li>
  #<LI><A HREF="../releases/hamm/">hamm</A> is Debian 2.0
  <li><A HREF="../releases/hamm/">hamm</A> 為 Debian 2.0</li>
  #<LI>bo   is Debian 1.3
  <li>bo 為 Debian 1.3</li>
  #<LI>rex  is Debian 1.2
  <li>rex 為 Debian 1.2</li>
  #<LI>buzz is Debian 1.1
  <li>buzz 為 Debian 1.1</li>
</ul>

#<P>As time goes on we will expire the binary packages for old releases.
#Currently we have binaries for <I>slink</I>, <I>hamm</I> and <I>bo</I>
#available, and only source code for the other releases.
<p>等到了一段時間之後，我們就會把舊版本的二進位套件給刪除。目前我們還保\
留 <i>slink</i>、<i>hamm</i> 與 <i>bo</i> 等發行版本中的二進位套件，其他的都只\
提供[HKTW:程式:]源碼套件。</p>

#<P>If you are using APT the relevant sources.list entries are like:
<p>如果您使用 APT，在 sources.list 中請加入：</p>

<pre>
  deb http://archive.debian.org/debian-archive/ hamm contrib main non-free
</pre>

#or
或

<pre>
  deb http://archive.debian.org/debian-archive/ bo bo-unstable contrib main non-free
</pre>

#<p>The following is a list of mirrors that include the archive:</p>
<p>底下是一份這些舊版本系統的映射站台<tw清單>：</p>

#include "$(ENGLISHDIR)/distrib/archive.mirrors"
<archivemirrors>
