#use wml::debian::template title="軟件包"

# $Id$
#<!--translation 1.15-->

<DL>
<DT><A href="$(HOME)/Packages/stable/">\
	檢視 <STRONG>穩定 (stable)</STRONG> 版的軟件包</A></DT>
  <DD>這是 Debian GNU/Linux 發佈的最新官方發行版，\
  亦稱為「主發佈」(`<EM>Main</EM>')。
  正如<A HREF="$(HOME)/social_contract#guidelines">《Debian 自由軟件指導方針》
  (Debian Free Software Guideline, DFSG)</A>
  裏關於「自由」(free) 的說明，所有的軟件包都是「自由」的，以確保這些軟件包\
  及其完整源代碼皆可以自由地使用和重新發佈。

  <P><EM>「主發佈」(main)</EM> 以外，還有一些額外的附加。\
  這些附加，因為有相當的限制，所以不能成為 Debian GNU/Linux 一類的\
  百分之百自由軟件發佈的一部分。然而，為了服務我們的[CN:用戶:][TW:使用者:]，\
  我們也額外提供了這些附加，包括：

  <BLOCKQUOTE><DL>
    <DT><EM>Contrib</EM></DT>
    <DD>在這個目錄裡的軟件包本身都是自由的，\
	但是它們必須依靠一些非自由軟件或硬件，才能正常運作。</DD>
    <DT><EM>Non-Free（非自由）</EM></DT>
    <DD>在這個目錄裡的軟件包並不一定需要付費才能使用，\
	但有一些麻煩的法律條件，限制了使用或散佈這些軟體的自由。</DD>
    <DT><EM>Non-US（非美國）</EM></DT>
# in potato: <DT><EM>Non-US/Main</EM> and <EM>Non-US/Non-Free</EM></DT>
    <DD>這些軟件包不允許從美國出口，所以只能從美國以外的地方發佈。\
    絕大多數都是加密軟體套件，或者是一些受專利權問題所妨害的。\
    大部份是自由軟件，但也有一些是不自由 (non-free) 的。</DD>
    </DL></BLOCKQUOTE>
  </DD>

# <: if ( -d '../Packages/frozen' ) { _:>
<DT><A href="$(HOME)/Packages/frozen/">檢視
	<STRONG>凍結 (frozen)</STRONG> 版的套件</A></DT>
  <DD>This is a temporary designation a release goes through when in
  the testing stage just prior to being released as a new stable distribution.</DD>
# <: } :>

<DT><A href="$(HOME)/Packages/unstable/">檢視 <STRONG>開發 (unstable)</STRONG> 版的軟件包</A>
    <DD>這個目錄包含了所有將來會被納入 stable 版的軟件包。\
    只是可能還沒完全測試好（即有可能弄壞您的系統），\
    或者是剛進 Debian 不久，未納入 stable 版中。</DD>
</DL>

<HR>

<H2>[CN:檢索:][TW:搜索:]軟件包目錄</H2>

<P>此[CN:檢索:][TW:搜索:]引擎可從軟件包標頭或簡介項\
[CN:檢索:][TW:搜索:]您指定的關鍵字。

<FORM method="get" action="http://cgi.debian.org/cgi-bin/search_packages.pl">
<LABEL for="kw">關鍵字：
<INPUT type="text" SIZE="30" name="keywords" id="kw"></LABEL>
<INPUT type="submit" value="搜尋"> <INPUT type="reset" value="重置">
<BR>以
<INPUT type=radio name="searchon" value="names" id="onlynames" checked>
<LABEL for="onlynames">軟件包名稱</LABEL>&nbsp;&nbsp;
<INPUT type=radio name="searchon" value="all" id="descs">
<LABEL for="descs">簡介&nbsp;&nbsp;</LABEL>來尋找
<BR>發佈版本 (Distribution):
<SELECT NAME="version">
  <OPTION VALUE="unstable">開發版 (unstable)</OPTION>
  <OPTION VALUE="stable" SELECTED>穩定版 (stable)</OPTION>
  <OPTION VALUE="frozen">凍結版 (frozen)</OPTION>
  <OPTION VALUE="all">任何版本</OPTION>
</SELECT>
部分 (Section):
<SELECT NAME="release">
  <OPTION VALUE="main">main</OPTION>
  <OPTION VALUE="contrib">contrib</OPTION>
  <OPTION VALUE="non-free">non-free</OPTION>
  <OPTION VALUE="non-us">non-US</OPTION>
  <OPTION VALUE="all" SELECTED>任何部分</OPTION>
</SELECT>
</FORM>

<P>用軟件包名稱來搜尋時，應當要包含一個單字，<B>字尾</B>可以加上萬用字元 '*'。\
用簡介來搜尋時可以使用標準的[CN:布爾:][TW:布林:] (Boolean) 邏輯 'and' 和 'or'
關鍵字。

<HR>

<H2>搜尋最新發行版本的內容</H2>

<P>以上的表格只會搜尋軟件包的標頭與簡介，\
所以如果想要搜尋軟件包裡某個沒有記載在標頭或簡介的檔案就沒辦法了。\
在這裡，你可以搜尋發行版本的內容並且找到所以包含你所指定的關鍵字的軟件包。

<P>
<FORM METHOD="POST" ACTION="http://cgi.debian.org/cgi-bin/search_contents.pl">
<LABEL FOR="keyword">關鍵字：</LABEL>
<INPUT TYPE="text" SIZE=30 NAME="word" ID="keyword"> &nbsp;
<INPUT TYPE="submit" VALUE="搜尋"> &nbsp;<INPUT TYPE="reset" value="重置">
<BR>
<LABEL FOR="casesens">區分大小寫：</LABEL>
<SELECT NAME="case" ID="casesens">
  <OPTION VALUE="insensitive" SELECTED>忽略</OPTION>
  <OPTION VALUE="sensitive">區分</OPTION>
</SELECT>
<LABEL FOR="distro">發佈版本 (Distribution)：</LABEL>
<SELECT NAME="version" ID="distro">
  <OPTION VALUE="stable" SELECTED>stable</OPTION>
  <OPTION VALUE="frozen">frozen</OPTION>
  <OPTION VALUE="unstable">unstable</OPTION>
</SELECT>
<LABEL FOR="architecture">體系結構 (Architecture)：</LABEL>
<SELECT NAME="arch" ID="architecture">
  <OPTION VALUE="i386" SELECTED>Intel x86</OPTION>
  <OPTION VALUE="m68k">Motorola 680x0</OPTION>
  <OPTION VALUE="alpha">Alpha</OPTION>
  <OPTION VALUE="sparc">SPARC</OPTION>
  <OPTION VALUE="powerpc">PowerPC</OPTION>
  <OPTION VALUE="arm">ARM</OPTION>
# Hurd should be a separate distribution
#  <OPTION VALUE="hurd-i386">Hurd (i386)</OPTION>
</SELECT>
</FORM>

<P>搜索軟件包名稱，也會顯示軟件包裏所有[CN:文件:][TW:檔案:]。</P>
