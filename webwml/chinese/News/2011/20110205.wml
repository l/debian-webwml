# The content of this announcement has been frozen
# please refrain from changing it, but feel free to fix gramar / typoes /
# style (if not intrusive)

# <define-tag pagetitle>Debian 6.0 <q>Squeeze</q> released</define-tag>
<define-tag pagetitle>Debian 6.0 <q>Squeeze</q> 正式发布</define-tag>
<define-tag release_date>2011-02-05</define-tag>
#use wml::debian::news

# <p>After 24 months of constant development, the Debian Project is
# proud to present its new stable version 6.0 (code name
# <q>Squeeze</q>). Debian 6.0 is a free operating system, coming for the
# first time in two flavours. Alongside Debian GNU/Linux, Debian
# GNU/kFreeBSD is introduced with this version as a <q>technology
# preview</q>.</p>

<p>经过 24 个月的不懈努力，Debian 项目荣幸地在此宣布推出新的稳定版：
Debian 6.0 (代号 <q>Squeeze</q>)。Debian 6.0 是一个自由的操作系統，这是它第一次推出
两种版本：除了常见的 Debian GNU/Linux，还首次引入了 Debian GNU/kFreeBSD 作为 
<q>技术预览</q>。</p>

# <p>Debian 6.0 includes the KDE Plasma Desktop and Applications, the
# GNOME, Xfce, and LXDE desktop environments as well as all kinds of
# server applications.  It also features compatibility with the FHS v2.3
# and software developed for version 3.2 of the LSB.</p>

<p>Debian 6.0 包含了 KDE Plasma 桌面和应用程序、GNOME、Xfce 和 LXDE 桌面环境，
还有各种各样的服务程序。此外，它兼容 FHS v2.3，可以使用兼容于 LSB 3.2 的各种软件。</p>

# <p>Debian runs on computers ranging from palmtops and handheld systems
# to supercomputers, and on nearly everything in between.  A total of nine
# architectures are supported by Debian GNU/Linux: 32-bit PC / Intel IA-32
# (<code>i386</code>), 64-bit PC / Intel EM64T / x86-64
# (<code>amd64</code>), Motorola/IBM PowerPC (<code>powerpc</code>),
# Sun/Oracle SPARC (<code>sparc</code>), MIPS (<code>mips</code>
# (big-endian) and <code>mipsel</code> (little-endian)), Intel Itanium
# (<code>ia64</code>), IBM S/390 (<code>s390</code>), ARM EABI
# (<code>armel</code>).</p>

<p>Debian 可运行于各种计算设备之上，从掌上电脑到超级服务器，以及几乎所有介于之间的其他设备。
Debian GNU/Linux 目前正式支持九种构架：32 位 PC / Intel IA-32 (<code>i386</code>)、
64 位 PC / Intel EM64T / x86-64 (<code>amd64</code>)，Motorola/IBM PowerPC 
(<code>powerpc</code>)、Sun/Oracle SPARC (<code>sparc</code>)、MIPS 
(<code>mips</code> (big-endian) 和 <code>mipsel</code> (little-endian))、
Intel Itanium (<code>ia64</code>)、IBM S/390 (<code>s390</code>)、ARM EABI
(<code>armel</code>)。</p>

# <p>Debian 6.0 <q>Squeeze</q> introduces technical previews of two new
# ports to the kernel of FreeBSD project using the known Debian/GNU userland:
# Debian GNU/kFreeBSD for the 32-bit PC (<code>kfreebsd-i386</code>) and
# the 64-bit PC (<code>kfreebsd-amd64</code>). These ports are the first
# ones ever to be included in a Debian release which are not based on
# the Linux kernel. The support of common server software is strong and
# combines the existing features of Linux-based Debian versions with the
# unique features known from the BSD world. However, for this release
# these new ports are limited; for example, some advanced desktop
# features are not yet supported.</p>

<p>Debian 6.0 <q>Squeeze</q> 引入了来自 FreeBSD 项目的系统核心，并使用知名的 
Debian/GNU 用户空间：Debian GNU/kFreeBSD 32 位 PC 的 <code>kfreebsd-i386</code> 
和 64 位 PC 的 <code>kfreebsd-amd64</code>。它们是 Debian 有史以来第一个不是基于 
Linux 核心的版本，它对于一般的服务器软件的支持已经比较完善，结合了原有 Linux 版本的特性和来自 
BSD 世界都有的特色。但它仍有一些限制，例如某些高级桌面应用特性尚未支持。</p>

# <p>Another first is the completely free Linux kernel, which no longer
# contains problematic firmware files. These were split out into
# separate packages and moved out of the Debian main archive into the
# non-free area of our archive, which is not enabled by default. In this
# way Debian users have the possibility of running a completely free
# operating system, but may still choose to use non-free firmware files
# if necessary. Firmware files needed during installation may be loaded
# by the installation system; special CD images and tarballs for USB
# based installations are available too.  More information about this
# may be found in the Debian <a
# href="http://wiki.debian.org/Firmware">Firmware wiki page</a>.</p>

<p>这次发布也是 Debian 首次提供完全自由的 Linux 核心，完全沒有没有包含有问题的
固件文件。它们被分离出了 Debian 主仓库(main)，而改为默认不启用的 non-free 仓库。
于是 Debian 的使用者便有机会选择使用完全自由的操作系统，同时也可以在必要的时候选用非
自由软件。安装过程中所需的固件可能由安装程序自动加载，我们也提供特殊的 CD 或 USB 安装镜像。
更多信息请参考 Debian Wiki <a href="http://wiki.debian.org/Firmware">固件</a>页面。</p>

# <p>Furthermore, Debian 6.0 introduces a dependency based boot system,
# making system start up faster and more robust due to parallel
# execution of boot scripts and correct dependency tracking between
# them. Various other changes make Debian more suitable for small form
# factor notebooks, like the introduction of the KDE Plasma Netbook
# shell.</p>

<p>以上各点之外，Debian 6.0 使用了全新的基于依赖的启动系统，使开机过程更加迅速、稳健。
其他的改变，如 KDE Plasma Netbook shell 则使得 Debian 更加适合安装在笔记本上。</p>

<p>这一版本包含了数量巨大的软件包更新，例如：
<ul>
<li>KDE Plasma Workspaces 和 KDE Applications 4.4.5</li>
<li>GNOME 桌面环境 2.30 的维护更新</li>
<li>Xfce 4.6 桌面环境</li>
<li>LXDE 0.5.0</li>
<li>X.Org 7.5</li>
<li>OpenOffice.org 3.2.1</li>
<li>GIMP 2.6.11</li>
<li>Iceweasel 3.5.16 (无商标版本的 Mozilla Firefox)</li>
<li>Icedove 3.0.11 (无商标版本的 Mozilla Thunderbird)</li>
<li>PostgreSQL 8.4.6</li>
<li>MySQL 5.1.49</li>
<li>GNU Compiler Collection (GCC) 4.4.5</li>
<li>Linux 2.6.32</li>
<li>Apache 2.2.16</li>
<li>Samba 3.5.6</li>
<li>Python 2.6.6, 2.5.5 和 3.1.3</li>
<li>Perl 5.10.1</li>
<li>PHP 5.3.3</li>
<li>Asterisk 1.6.2.9</li>
<li>Nagios 3.2.3</li>
<li>Xen Hypervisor 4.0.1 (支持 dom0 和 domU)</li>
<li>OpenJDK 6b18</li>
<li>Tomcat 6.0.18</li>
<li>以及其他构建自约 15,000 个源代码包的超过 29,000 个开箱即用的软件包。</li>
</ul>
Debian 6.0 包含了包括 Chromium 浏览器、Icinga 监控解决方案、软件中心、Wicd 网络管理器、Linux 
容器工具 lxc 和 corosync 集群框架在内的超过 10,000 个新软件包。
</p>

<p>数量众多的可选软件包使 Debian 保持了它一贯追求的目标——“通用操作系统”。Debian 适用于
各种常见使用案例：从桌面系统到上网本、从开发服务器到集群系统、数据以及存储服务器。与此同时，对仓库内
所有软件包进行的自动测试（如安装/升级测试）确保了 Debian 6.0 不辜负广大用户对 Debian 稳定版的
期盼。Debian 6.0 通过了苛刻的测试，品质坚若磐石。

# maybe add a nice quote from the DPL/RMs here
</p>

<p>自 Debian 6.0 起，<q>Custom Debian Distribution</q> 重命名为
<a href="http://blends.alioth.debian.org/"><q>Debian Pure
Blends</q></a>。其覆盖面也随着 Debian 6.0 在原有的<a
href="http://wiki.debian.org/DebianEdu">Debian Edu</a>、<a
href="http://www.debian.org/devel/debian-med/">Debian Med</a> 和 <a
href="http://wiki.debian.org/DebianScience">Debian Science</a>
基础之上增加了 <a
href="http://www.debian.org/devel/debian-accessibility/">Debian 
辅助功能</a>、<a href="http://debichem.alioth.debian.org/">DebiChem</a>、
<a href="http://wiki.debian.org/DebianEzGo">Debian EzGo</a>、<a
href="http://wiki.debian.org/DebianGis">Debian GIS</a> 和 <a
href="http://blends.alioth.debian.org/multimedia/tasks/index">Debian
多媒体</a>。所有相关的内容 <a href="http://blends.alioth.debian.org/">均可浏览</a>，
用户也可以提名受欢迎的软件包，使其可能被加入下一版本。</p>

<p>Debian 可通过许多种方式来安装，如蓝光光盘、DVD、CD 和 USB 储存介质，也可通过网络安装。
GNOME 是默认的桌面环境，它被包含于第一张 CD 之中。其他桌面环境 &mdash; KDE Plasma 桌面和应用程序、
Xfce 或 LXDE &mdash; 可通过其他可选 CD 安装。您也可以在 CD/DVD 的启动菜单中选取希望使用的桌面环境。
Debian GNU/Linux 6.0 也再次提供同时支持多个硬件构架的 CD 和 DVD 映像，允许使用一张光盘在多种硬件平
台上执行安装作业。制作 USB 启动介质的过程也被大幅简化，详情请参看
 <a href="$(HOME)/releases/squeeze/installmanual">安装指南</a>。</p>

<p>除常规的安装介质外，Debian GNU/Linux 也可以通过 Live 镜像的方式直接使用而无需安装。
Debian 提供用于 CD、USB 储存介质和网络启动的 Live 镜像，目前仅有用于 <code>amd64</code> 和 
<code>i386</code> 构架的镜像。这些 Live 镜像也可用于安装 Debian GNU/Linux。</p>

<p>Debian GNU/Linux 6.0 的安装过程有了多个方面的改进，包括更加易用的语言选择和键盘设置、
对逻辑卷分区、RAID 和加密文件系统的支持。此外还添加了对 ext4 和 btrfs 文件系统的支持
&mdash; 在 kFreeBSD 构架上还有 ZFS 支持 &mdash;。目前 Debian 支持使用 70 种语言进行安装。
</p>

<p>Debian 安装镜像现在已可通过
<a href="$(HOME)/CD/torrent-cd/">bittorrent</a> (推荐)、
<a href="$(HOME)/CD/jigdo-cd/#which">jigdo</a> 或
<a href="$(HOME)/CD/http-ftp/">HTTP</a> 方式下载；更多信息请参看 
<a href="$(HOME)/CD/">Debian on CDs</a>。多个 <a href="$(HOME)/CD/vendors">供应商</a> 
也将很快开始提供 DVD、CD-ROM 以及蓝光光盘。</p>

<p>从前一版本 Debian GNU/Linux 5.0 （代号 <q>Lenny</q>）升级到 Debian 6.0，在大多数情况下
均可使用 apt-get 软件包管理工具自动处理，某种程度来说也可以用 aptitude 管理工具进行。一如往常，
Debian GNU/Linux 系统可以平稳无痛地升级，没有任何必须的停机时间，但还是强烈建议事先
阅读 <a href="$(HOME)/releases/squeeze/releasenotes">发行注记</a> 和 
<a href="$(HOME)/releases/squeeze/installmanual">安装指南</a>，以便了解潜在会发生的问题，
并取得详细的安装和升级说明。本发行说明可能在发行之后的数周内持续更新，并将翻译成其他语言。</p>


<h2>关于 Debian</h2>

<p>Debian 是一个自由的操作系统，由来自世界各地的千余位志愿，借助互联网并肩协作、倾心打造。Debian 项目
的原动力来自志愿者的奉献，执着对 Debian 社会契约的坚持以及信守打造最佳操作系统的承诺。Debian 6.0 的
发布，是在这个方向上迈出的重要一步。</p>


<h2>联系信息</h2>

<p>更多信息，请访问 Debian 项目首页 
<a href="$(HOME)/">http://www.debian.org/</a> 或发送电子邮件至 
&lt;press@debian.org&gt;。</p>

