#use wml::debian::cdimage title="Debian CD 映像的 rsync 镜像列表" BARETITLE=true
#use wml::debian::translation-check translation="1.4"

<p>这些是 Debian CD 映像的\
<a href="http://rsync.samba.org/">rsync</a> 镜像地址。请注意，一些镜像\
可能不一定是最新的。此列表仅列出包含\
<em>官方</em> Debian 映像的站点。</p>

<p><strong>注意：</strong>这些网站<em>不一定</em>通过\
FTP 或 HTTP 提供映像——不要这样做！相反他们使用 rsync
协议，这对我们来说更有效率。如果您正在寻找\
HTTP/FTP 下载链接，请参阅 <a href="../http-ftp/">HTTP/FTP 镜像\
列表</a>。</p>

<p>如果您在许多站点都得到了“访问被拒绝”，则您的\
防火墙可能阻止了 rsync 端口（873）。\
请联系系统管理员重新配置\
防火墙。</p>

#use wml::debian::countries
#include "$(ENGLISHDIR)/CD/mirroring/cdimage_mirrors.list"
