#use wml::debian::template title="Debian ``potato'' 發行情報"

<p>

下一個Debian版本的代號名稱將會是“potato”。很可能這個版本的編號會是“2.2”，
但仍沒有確定。

<p>

這個版本在目前被認為是“unstable”。那意味著如果你運行它<em>說不定會</em>某些東西會不能正常運作。

<p>

Debian有一個決不宣布版本發行日期的嚴謹方針；因此，“potato”這個版本的發行日期
至今未知。


<h2>“potato”的主要變動</h2>

下面是我們知道的potato部分的主要變動：

<ul>

  <li> 基於Linux kernel 2.2 (for architectures with Linux kernels at all).

  <li> 基於GNU libc 2.1 (和GNU libc 2.0向上二進制碼兼容)

  <li> <tt>gcc</tt>版本的<a href="http://egcs.cygnus.com/">egcs</a>，也叫做
  <tt>egcc</tt>，將會是默認的C編譯器。

</ul>

這也是一個好機會，在potato發行版裡將包括powerpc和arm體系結構。

<p>

這個發行版很可能還會有許多改變，但這些還是未知的。

