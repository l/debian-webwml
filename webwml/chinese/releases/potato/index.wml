#use wml::debian::template title="Debian ``potato'' 发行情报"

<p>

下一个Debian版本的代号名称将会是“potato”。很可能这个版本的编号会是“2.2”，
但仍没有确定。

<p>

这个版本是目前经过考虑的“unstable”。那意味着如果你运行它<em>说不定会</em>中
断些事情。

<p>

Debian有一个决不宣布版本发行日期的严谨方针；因此，“potato”这个版本的发行日期
至今未知。


<h2>“potato”的主要变动</h2>

下面是我们知道的potato部分的主要变动：

<ul>

  <li> 基于Linux kernel 2.2 (for architectures with Linux kernels at all).

  <li> 基于GNU libc 2.1 (和GNU libc 2.0向上二进制码兼容)

  <li> <tt>gcc</tt>版本的<a href="http://egcs.cygnus.com/">egcs</a>，也叫做
  <tt>egcc</tt>，将会是默认的C编译器。

</ul>

这也是一个好机会，在potato发行版里将包括powerpc和arm体系结构。

<p>

这个发行版很可能还会有许多改变，但这些还是未知的。

