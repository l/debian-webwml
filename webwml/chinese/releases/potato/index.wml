#use wml::debian::template title="Debian “potato” 發行情報"

<p>

下一個 Debian 版本的代號名稱將會是“potato”，也就是 Debian 2.2 。

<p>

這個版本當前仍在凍結中。這意味著在它被作下一個穩定發行版發布以前
它仍然在進行測試工作。

<p>

Debian有一個決不宣布版本發行日期的嚴謹方針；因此，“potato”的發行
日期至今仍然未知。


<h2>“potato” 的主要變動</h2>

下面是我們已知的 patato 的部分主要變動：

<ul>

  <li> 基於 Linux 核心 2.2 （所有使用 Linux 核心的體系結構）。

  <li> 基於 GNU libc 2.1 （和 GNU libc 2.0 向上二進制碼兼容）

  <li> <tt>gcc</tt> 版本的 <a href="http://egcs.cygnus.com/">egcs</a> ，
  也叫作 <tt>egcc</tt> ，將會是默認的 C 編譯器。

</ul>

這對在 potato 發行版裡將包括 powerpc 和 arm 體系結構來說也是個好機會。

<p>

這個發行版本很可能還會有許多改變，但這些仍是未知的。


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:"../.doctype"
End:
-->

