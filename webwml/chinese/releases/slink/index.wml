#use wml::debian::template title="Debian 2.1 (slink)發行情報"

<:
# 體系結構列表
%arches = ( i386   => 'Intel x86',
	    m68k   => 'Motorola 680x0',
	    sparc  => 'SPARC',
	    alpha  => 'Alpha',
	  );

# 輸出格式的語言特殊名稱
%formats = ( html => "HTML",
	     pdf  => "PDF",
	     txt  => "ASCII",
           );

$about_lang_setting = '如果你已設置了你的<a href="../../intro/cn">首選語言</a>，
你應該自動獲得正確的譯本。';

sub permute_file {
    my($file, $name, @exts) = @_;
    my($ext, $arch);

    foreach $arch (keys %arches) {
        print "<li> <a href=\"./$arch/$file\">" . $arches{$arch} . $name "</a>";
	if ( $#exts != -1 ) {
	    foreach $ext (@exts) {
	        print " (<a href=\"./$arch/$file.$ext\"><em>" . 
			$formats{$ext} . "</em></a>)";
	    }
	}
	print "\n";
    }
}
 :>

<ul>
	<li><a href="#release-notes">發行附注</a>
	<li><a href="#new-inst">新的安裝</a>
	<li><a href="#errata">勘誤表</a>
	<li><a href="#unofficial-updates">非正式的修正</a>
	<li><a href="#acquiring">獲得Debian 2.1</a>
      </ul>

<p>

Debian 2.1，也就是“slink”,現在可以使用。
在Debian 2.1中包含對下列體系結構的支持：

<ul>
<: foreach $arch (keys %arches) {
      print "<li> " . $arches{$arch} . "\n";
   } :>
</ul>


<h2><a name="release-notes">發行附注</a></h2>

<p>

查看你所使用的體系結構的發行附注以發現在Debian 2.1裡面有什麼更新。
版本附注裡也包含有從舊版本升級的用戶指南。

<ul>
<: &permute_file('release-notes/index', '發行附注'); :>
</ul>

目前發行附注可用的語言版本有克羅地亞語，捷克語，英語，德語，日語和俄語。

<: print $about_lang_setting; :>

<p>

對於i386體系結構，有一個<a href="i386/reports">詳細報告</a>描述自最後兩個
發行版以來哪些包做了哪些改變。


<h2><a name="new-inst">新的安裝</a></h2>

<p>

安裝指南， along with downloadable files, are broken
out by architecture:
<ul>
<: &permute_file('install', '安裝手冊'); :>
</ul>

許多的工作已經使Debian安裝手冊很適合去支持non-i386發行版了。可是，有些結構
(特別是<:=$arches{'alpha'}:>)仍然需要工作 -- 查閱這份文檔以了解關於你可以怎
樣來出一份力的信息。

<p>

與現有的一樣，當發行slink的新版本的安裝手冊時這些網頁也將會被更新。如果你想
要提出改變 ，查看這段和這本手冊；你也可以檢索<a href="source/">SGML源碼</a>
-- 補丁是注釋的最好形式。投稿者或讀者們想確切知道有哪些更些應該查閱
<a href="source/ChangeLog">變更記錄</a>.

<p>

安裝手冊可用的語言版本有克羅地亞語，捷克語，英語，芬蘭語，法語，德語，日語和俄語。

<: print $about_lang_setting; :>



<h2><a name="errata">勘誤表</a></h2>

<p>

有時，在發現嚴重問題或安全更新的情況，已發行版本的發布（假若這樣，Slink)已被
修正。Generally,these are indicated as point releases.當前版本是Debian 2.1r2。
你可以在所有的Debian文檔鏡像找到<a 
href="ftp://ftp.debian.org/debian/dists/slink/ChangeLog">變更記錄</a>。

<p>

Fixes to the released stable distribution often go through an extended
testing period before they are accepted into the archive.  However,
these fixes are available in the <a
href="ftp://ftp.debian.org/debian/dists/slink-proposed-updates/">
dists/slink-proposed-updates</a> directory of any Debian archive
mirror.  If you use <tt>apt</tt> to update your packages, you can
install the proposed updates by adding the following line to
<tt>/etc/apt/sources.list</tt>:
<pre>
  deb http://http.us.debian.org/debian dists/proposed-updates/
</pre>
Next, run <kbd>apt-get update; apt-get upgrade</kbd>.

<p>

Slink is certified for use with the 2.0.x series of Linux kernels.  If
you wish to run the Linux 2.2.x kernel with slink, see the <a
href="running-kernel-2.2">list of known problems</a>.


<h2><a name="unofficial-updates">Unofficial Updates</a></h2>

<p>

Released Debian versions are generally not updated, except in the case
of critical errors or security issues.  However, for user convenience,
there are certain updated packages which are made available.


<h3>Gnome</h3>

<p>

Gnome is an important set of programs and many people desire the latest
now.  Accordingly, A number of developers have put together the latest
stable software into debs for slink.  Checkout <a href="gnome">the
Slink/Gnome page</a> for more details.


<h3>Apt</h3>

<p>

An updated version of the potato <tt>apt</tt> is available compiled
for Slink as well.  Download the Debian package at <a
href="http://netgod.net/x/apt_0.3.7slink0_i386.deb">
http://netgod.net/x/apt_0.3.7slink0_i386.deb</a>. 

<p>

The benefits of this updated version is primarily that it is able to
handle installation from multiple CD-ROMs itself.  This makes the
<tt>dpkg-multicd</tt> acquisition option in <tt>dselect</tt>
unnecessary.



<h2><a name="acquiring">獲得Debian 2.1</a></h2>

<p>

Debian is available electronically or from CD vendors.

<h3>購買Debian光盤</h3>

<p>

我們維護著一個出售Debian 2.1光盤的<a href="../../distrib/vendors">CD銷售者列表</a>。


<h3>通過Internet下載Debian</h3>

<p>

我們維護著一個鏡像發行的<a href="../../distrib/ftplist">站點列表</a>。


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:"../.doctype"
End:
-->
