#use wml::debian::template title="Debian 2.0 運行於x86機器的勘誤表"

<h3>安裝</h3>
<p>沒有報告說在安裝過程中存在錯誤。

<h3>升級</h3>
<p>Debian 2.0 光盤中包含的一個cd_autoup.sh中存在著少許臭虫。它沒有什麼危害，僅僅是導致一些
   本來應該由cd_autoup.sh來安裝的包沒有被安裝。下次運行dselect時將會把它挑選出來。
   <p>雖然如此，還是應該使用
   <a href="ftp://ftp.debian.org/debian/dists/stable/main/upgrade-i386/cd_autoup.sh">autoup.sh
   的修復過的版本</a>來升級。2.0 r1版的光盤沒有這個問題。

<h3>包</h3>
<p>你應該注意下列有問題的軟件升級：                   
<ul>                                                                                                  
   <li>(7月24日) 在CVS 2.0的版本中存在著一個Y2K問題。一旦其作者發布一個新版本，一個修復過的版本
   就將會在slink中發行。
   <li>(7月24日) 在svgalib中存在著一個安全錯誤，但發現太晚已經不允許在2.0版本中修復。如果你使用
   svgalib的話你應該升級到unstable版本。它將會存在一段日子。
   <li>(7月24日) 在cfingrd中存在著一個安裝漏洞，雖然Debian不太容易受到攻擊。你需要在配置文件中打開
   某些選項來使其脆弱性暴露出來。（You need to turn on certain options in the config file to allow
       the vulnerability.） 想要安全，升級到slink版本。
</ul>

