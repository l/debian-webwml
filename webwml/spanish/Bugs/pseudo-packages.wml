#use wml::debian::template title="SSF de Debian - pseudo-paquetes" NOHEADER="true" NOCOPYRIGHT=true
#use wml::debian::translation-check translation="1.11"

<H1>Pseudo-paquetes del sistema de seguimiento de fallos de Debian</H1>

<p>Esta página lista los pseudo-paquetes disponibles para su uso en
la línea <code>Package:</code> en los informes de error.

<p>Vea las <a href="Reporting">instrucciones para informar de un
fallo</a> para saber los detalles de cómo especificar una línea
<code>Package:</code>.

<hrline>

<:
my $file = "$(ENGLISHDIR)/Bugs/pseudo-packages.description";
open I, "$file" or die "Can't find $file file: $!\n";
while (<I>) { if (/^(\S+)\s+(.*)$/) { my ($foo) = $2; $pseudos{$1} = $foo =~ s/&/&amp;/g; } }
close I;
%pseudos || die "%pseudos is not set -- empty $file file?\n";
foreach $a (sort keys %pseudos) {
  if ($a ne "spam" && $a ne "slink-cd") {
    print "<p><code><a href=\"http://bugs.debian.org/$a\">$a</a></code> &mdash; $pseudos{$a}\n";
  }
}
:>

<hrline>

#use "otherpages.inc"

#use "$(ENGLISHDIR)/Bugs/footer.inc"
