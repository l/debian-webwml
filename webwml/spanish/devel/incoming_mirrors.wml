#use wml::debian::template title="Notas sobre Incoming"
#use wml::debian::translation-check translation="1.10"

<p>«Incoming» es el directorio en el que los nuevos paquetes se 
colocan antes de ser añadidos a la distribución Debian.

<p>Los paquetes en «incoming» están completamente sin probar (excepto 
probablemente por el mantenedor del paquete) y es posible que contengan 
errores de empaquetado o errores en los ejecutables que podrían hacer 
iutilizable su ordenador. Además, los paquetes en ese directorio <em>no</em> 
han sido comprobados mediante md5sum o pgp para verificar que están intactos o 
que han sido puestos allí por un desarrollador de Debian registrado. Puede, sin 
embargo, instalar el paquete <tt>devscripts</tt> y usar el «script» 
<em>dscverify</em> para comprobar la validez de un paquete.

<p>Recomedamos que NO se haga una réplica de «Incoming». Para acceder al 
directorio «Incoming», use 
<a href="http://incoming.debian.org/">http://incoming.debian.org/</a>

<p>Ha sido advertido.
