#use wml::debian::template title="Arrancar desde disquetes e instalar por red" 
#use wml::debian::translation-check translation="1.6"

<p>Se puede arrancar el sistema de instalación de Debian desde
disquetes, y luego instalar el resto por red.</p>

<ul>
  <li>Descargue las imágenes de los discos del directorio
      <tt>/dists/stable/main/disks-XXXX/</tt> de cualquier 
      <a href="$(HOME)/mirror/list">réplica de Debian</a>; sustituya la
      arquitectura de su ordenador por "<tt>XXXX</tt>", por ejemplo
      «i386» o «alpha». También puede descargar solamente los
      ficheros que necesite, como se describe en el
      <a href="$(HOME)/releases/stable/i386/ch-install-methods#s-file-descs">\
      manual de instalación</a>, o simplemente coger el árbol entero.

# NB: the location of that section has changed in woody to
# ch-appendix#s-file-descs
  </li> 
  <li>Escriba las imágenes a los discos, de acuerdo con las
  instrucciones en el <a href="$(HOME)/releases/stable/i386/ch-install-methods#s-install-floppies">\
  manual de instalación</a>.

# NB: the anchor within the file has changed in woody, we'll need to
# change the above to #s-create-floppy
  </li>

   <li>Arranque desde los discos de arranque, siguiendo las instrucciones 
       del
       <a href="$(HOME)/releases/stable/i386/ch-rescue-boot#s-features-rescue">\
       manual de instrucciones</a>.
   </li>    

  <li>Cuando la instalación proceda a pedirle los ficheros, elija el
  método "Network" con el fin de conectar a una réplica de Debian
  desde donde conseguir los ficheros.

# NB: the location of that section has changed in woody to
# ch-install-system#s-install-os-netfetch
  </li>
</ul>

<p>Por favor refiérase al <a href="$(HOME)/releases/stable/installmanual">\
manual de instalación para su arquitectura</a> para más
información.</p>

# NB: not sure installing the base system over the network is supported in
# potato, the above wording might indicate otherwise!

# NB2: it might be that the installation manuals for other architectures
# explain differences with dealing with diskettes, someone should probably
# investigate if that is so and place additional links
