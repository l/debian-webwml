#use wml::debian::template title="Traducciones del servidor web pendientes de revisión"
<P>Las siguientes traducciones necesitan ser revisadas, posiblemente porque
el documento sobre el que se tradujeron ha cambiado. Es necesario
que, para revisar el documento recoja tanto la versión original sobre
la que se tradujo el documento como la versión actual para ver las diferencias
(para esto es útil el programa <em>diff</em> vea <code>info diff</code>).
<P>CVS permite hacer esto automáticamente, haciendo <code>cvs diff -u -r X.X
documento</code>, donde <em>documento</em> es el fichero original en
inglés y X.X la versión sobre la que se tradujo. Puede saberlo si el
documento ha utilizado el esquema sugerido para <a
href="$(HOME)/devel/HOWTO_translate">traducción de la web</A>.
<UL>
<perl>
do 'ltcp.pl';
update_db_CVS();
update_db_format();
dump_html(5,"web");
</perl>
</UL>
 
