#use wml::debian::template title="Traducciones que necesitan revisores" NOHEADER=yes

<H1><a name="a-revisar"></a>Traducciones a revisar</H1>
<P>Las siguientes traducciones necesitan ser revisadas, posiblemente porque
el documento sobre el que se tradujeron ha cambiado. Es necesario
que, para revisar el documento recoja tanto la versión original sobre
la que se tradujo el documento como la versión actual para ver las diferencias
(para esto es útil el programa <em>diff</em> vea <code>info diff</code>).
</P>
<UL>
<PERL>
do 'ltcp.pl';
update_db_format();
dump_html(5);
</PERL>
</UL>
