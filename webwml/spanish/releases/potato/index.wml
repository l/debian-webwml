#use wml::debian::template title="Debian ``potato'' Release Information"
<!--translation 1.1-->

<p>

El nombre en clave para la siguiente versión de Debian es ``potato''. No
es probable que esta versión se numere ``2.2'', pero esto no es aún seguro.

<p>
Esta versión se considera actualmente ``inestable''. Esto significa que las
cosas <em>seguro</em> se romperan si intenta utilizarla.

<p>
Debian tiene una política estricta de nunca anunciar sus fechas
de distribución; por tanto, la fecha de salida de ``potato'' aún no es 
conocida.


<h2>Cambios importantes para ``potato''</h2>

Los cambios más importantes que sabemos serán parte de potato son los siguientes:

<ul>

  <li> Basado en el núcleo Linux 2.2 (para arquitecturas que utilizan el núcleo de Linux).

  <li> Basado en GNU libc 2.1 (conocido como <tt>libc6.1</tt> en Linux).

  <li> La versión <a href="http://egcs.cygnus.com/">egcs</a> de
<tt>gcc</tt>, también conocida como <tt>egcc</tt>, será el compilador por defecto.

</ul>

<p>
También hay  bastantes posibilidades que la distribución de potato incluya las
arquitecturas powerpc y arm.

<p>

Hay posiblemente muchos otros cambios en esta distribución, pero aún no son
conocidos.

