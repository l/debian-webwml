#use wml::debian::translation-check translation="1.6"
<define-tag moreinfo>
Hemos encontrado que el paquete fte suministrado en nuestros archivos de slink
(congelado) y potato (inestable) no liberan sus privilegios de root tras
inicializar el dispositivo de consola virtual. Esto permite a todos los
usuario leer y escribir archivos con privilegios de root, y ejecutar todos los
programas como root.

<p>Un paquete nuevo (versión 0.46b-4.1) ha sido subido para reparar este
problema.

<p>Le recomendamos que actualice su paquete fte inmediatamente.
</define-tag>
<define-tag description>no libera sus privilegios de root</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19981207.data'
