#use wml::debian::template title="데비안 릴리스"

      <h2>상태에 따른 구별</h2>

<p>

아래의 릴리스는 각각의 상태에 따라 구별한 것이다. 데비안은 최소한
``안정판''과 ``개발판'' 두 가지 릴리스를 항상 관리하기 때문에 아래의
링크는 언제든지 유효하다.

<dl>
	<dt><a href="stable/">안정판</a>
	<dd>
``안정판'' 배포본은 가장 최근에 공식적으로 발표된 데비안 배포본이며,
데비안의 프로덕션 릴리스이다. 현재의 ``안정판'' 배포본은 <em>slink</em> 이다.
</dd>
	<dt><a href="unstable/">개발판</a>
	<dd>
``개발판'' 배포본은 개발중인 데비안 배포본이다. 이 배포본은 개발자와
위험을 마다하지 않는 사용자들이 사용한다. 현재 ``개발판'' 배포본은
<em>potato</em> 이다.

<: if ( -d 'frozen' ) { _:>
	<dt><a href="frozen/">frozen</a>
	<dd>
The ``frozen'' distribution is a release candidate as the next
``stable'' distribution.  At this point, there is a ``frozen''
distribution; usually, there is only ``stable'' and ``unstable''
distributions. The current ``frozen'' distribution is <em>
<: print readlink('frozen'); # FYI, this doesn't work, I had it above too :>
</em>
<: } :>

      </dl>

<h2>이름에 의한 구별</h2>
<p>

아래의 릴리스는 단지 위에서 언급한 배포본의 또 다른 이름일 뿐이다.
<ul>
<: if ( -d 'hamm' ) { _:>
	<li><a href="hamm/">Debian 2.0 (hamm)</a>
<: } :>
<: if ( -d 'slink' ) { _:>
	<li><a href="slink/">Debian 2.1 (slink)</a>
<: } :>
<: if ( -d 'potato' ) { _:>
	<li><a href="potato/">Debian 2.2 (potato)</a>
<: } :>
</ul>


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:".doctype"
End:
-->
