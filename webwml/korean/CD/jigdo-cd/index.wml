#use wml::debian::cdimage title=" jigdo로 데비안 CD 이미지들 다운로드하기" BARETITLE=true
#use wml::debian::toc
#use wml::debian::translation-check translation="1.33"

<p>Jigsaw Download 혹은 줄여서 <a href="http://atterer.net/jigdo/">jigdo</a>라 부르는 이것은, 앞으로 데비안 CD 이미지를 제공하는 주요 수단이 될 계획입니다.  현재 이 방식이 쓰이고 있지만 다운로드 매니저 프로그램이 완성되지 않아 사용하기에 매우 편리하지는 않습니다.</p>

<toc-display/>

<hr>

<toc-add-entry name="why">왜 jigdo가 직접 다운로드보다 좋은가</toc-add-entry>

<p>빠르기 때문입니다! 여러 가지 이유로 "일반" 데비안 아카이브 미러들보다 CD 이미지들을 제공하는 곳이 훨씬 적습니다. 특히 갓 릴리스된 것을 CD 이미지를 제공하는 미러로부터 다운로드한다면, 비록 미러가 멀리 떨어져 있지 않더라도 그 미러에는 과부하가 걸릴 것입니다.</p>

<p>물론 "일반" 데비안 미러에는 어떠한 CD 이미지도 없습니다. 그렇다면 jigdo는 어떻게 미러들로부터 다운로드할 수 있을까요? jigdo는 CD에 담길 모든 개별적인 파일들을 다운로드함으로써 이를 달성합니다. 다음 단계에서 다운로드된 모든 파일은 CD 이미지와 같은 하나의 큰 파일로 조합됩니다. 그러나 이 과정은 화면 뒤에서 이루어지므로 <em>여러분</em>은 오로지 "<tt>.jigdo</tt>" 파일의 위치만 다운로드 도구가 처리하도록 알려주면 됩니다.</p>

<p>더 자세한 정보는
<a href="http://atterer.net/jigdo/">jigdo homepage</a>에
있습니다. jigdo 개발 자원 봉사자들은 언제나 환영합니다!</p>

<toc-add-entry name="how">어떻게 jigo로 다운로드하나요</toc-add-entry>

<ul>

  <li><a href="http://atterer.net/jigdo/#download">jigdo
홈페이지</a>에서 <tt>jigdo-lite</tt>를 포함한 GNU/리눅스, MS 윈도우,
솔라리스 버전의 패키지를 다운로드합니다.
  <br><strong>데비안 사용자들은 최신 버전인 0.6.8로
업그레이드하세요</strong> - 데비안 3.0(Woody)에 포함된 버전은 최신
이미지를 다운로드하는 데 부적합합니다!</li>

  <li><tt>jigdo-lite</tt> 스크립트를 실행합니다. 처리할 "<tt>.jigdo</tt>" 파일의 위치를 물을 것입니다(원한다면 명령행에서 URL을 직접 입력할 수 있습니다).</li>

  <li><a href="#which">다음</a>에 열거된 곳 중 한 곳에서
다운로드하려는 "<tt>.jigdo</tt>" 파일을 선택하고 <tt>jigdo-lite</tt>
프롬프트에서 URL을 입력합니다. 각각의 "<tt>.jigdo</tt>" 파일은
"<tt>.iso</tt>" CD/DVD 이미지 하나에 해당합니다.</li>

<li>처음 사용한다면 "Files to scan" 프롬프트에서 그냥 리턴을 누릅니다.
</li>

<li>"Debian mirror" 프롬프트에서 <kbd>http://ftp.<strong><var>XY</var></strong>.debian.org/debian/</kbd>을 입력합니다. <strong><var>XY</var></strong>는 두 글자 코드로 된 국가
코드입니다(예를 들어, <tt>us</tt>, <tt>de</tt>, <tt>uk</tt>.
 <a href="$(HOME)/mirror/list">사용할 수 있는
  ftp.<var>XY</var>.debian.org 위치</a> 목록을 참고하세요).

<li>"Debian non-US mirror"(보인다면) 프롬프트에서
  <kbd>http://ftp.<strong><var>XX</var></strong>.debian.org/debian-non-US/</kbd\
>를 입력하세요(<strong><var>XX</var></strong> 부분은 정확한 국가
코드로 바꿔야 합니다).

  <li>수행 과정은 스크립트에 의해 출력됩니다. 모두 올바르게 수행되면, 이미지를 생성하고 체크섬을 계산하고 원래 이미지와 체크섬이 일치하는지를 알려주는 것으로 스크립트는 끝납니다.</li>

</ul>

<p>이 과정들에 대한 하나하나의 자세한 설명은 <a href="http://www.tldp.org/HOWTO/mini/Debian-Jigdo/">Debian jigdo mini-HOWTO</a>에서 볼 수 있습니다. 이 하우투 문서는 jigdo의 오래된 버전의 CD 이미지를 최신 버전으로 업그레이드하는 특성에 대한 내용도 포함합니다(새로운 이미지 전체를 다운로드하는 것이 아니라, 변화된 것들만 다운로드합니다). (역주: <a href="http://kldp.org/KoreanDoc/html/Jigdo-KLDP/">데비안 Jigdo 사용하기</a>를 참고하기 바랍니다.)</p>

<p>이미지를 다운로드하여 CD에 기록했다면, <a href="$(HOME)/releases/stable/installmanual">설치 과정에 대한 상세한 정보</a>가 필요할 것입니다.</p>

<toc-add-entry name="which">가능한 이미지들</toc-add-entry>

<ul>

  <li>"stable" 릴리스 CD의 공식 jigdo 파일들:
  <a href="http://us.cdimage.debian.org/jigdo-area/3.0_r1/jigdo/">\
  미국 미러</a>,
  <a href="http://non-us.cdimage.debian.org/jigdo-area/3.0_r1/jigdo/">\
  유럽 미러</a></li>

</ul>

<ul>

  <li><a href="http://gluck.debian.org/cdimage/testing/jigdo-area/">"testing" 배포</a> CD의 공식 jigdo 파일들(<em>모든 아키텍처, 매주 재구성</em>)
  <br>
  "testing"/"unstable" CD들은 현재 부팅/설치가 불가능하다. - stable 버전으로부터 업그레이드만 가능.</li>

</ul>

<ul>

  <li><a href="ftp://ftp.fsn.hu/pub/CDROM-Images/debian-unofficial/sarge/jigdo/">"testing" 배포</a> CD의 비공식 jigdo 파일들(<em>매주 재구성되며, i386용</em>)</li>

  <li><a href="ftp://ftp.fsn.hu/pub/CDROM-Images/debian-unofficial/sarge-dvd/jigdo/">"testing" 배포</a> DVD의 비공식 jigdo 파일들(<em>매주 재구성되며, i386용</em>)</li>

  <li><a href="ftp://ftp.fsn.hu/pub/CDROM-Images/debian-unofficial/sid/jigdo/">"unstable" 배포</a> CD의 비공식 jigdo 파일들(<em>매주 재구성되며, i386용</em>)</li>

  <li><a href="ftp://ftp.fsn.hu/pub/CDROM-Images/debian-unofficial/sid-dvd/jigdo/">"unstable" 배포</a> DVD의 비공식 jigdo 파일들(<em>매주 재구성되며, i386용</em>)
  <br>
  DVD들은 현재 윈도우에서는 다운로드가 <strong>불가능</strong>!</li>

</ul>

<hr>

<toc-add-entry name="faq">자주 질문/답변되는 것들</toc-add-entry>

<p><strong>jigdo가 프록시 설정을 쓰게 하려면?</strong></p>

<p><tt>~/.jigdo-lite</tt>(또는 윈도우 버전에서는 <tt>jigdo-lite-settings.txt</tt>) 파일을 텍스트 편집기로 읽어들여 "wgetOpts"로 시작되는 줄을 찾습니다. 다음과 같이 옵션을 추가할 수 있습니다.</p>

<p><tt>-e ftp_proxy=http://<i>LOCAL-PROXY</i>:<i>PORT</i>/</tt>
<br><tt>-e http_proxy=http://<i>LOCAL-PROXY</i>:<i>PORT</i>/</tt>
<br><tt>--proxy-user=<i>USER</i></tt>
<br><tt>--proxy-passwd=<i>PASSWORD</i></tt></p>

<p>물론 자신의 프록시 서버에 맞는 정확한 값으로 바꾸어야
합니다. 마지막 두 개의 옵션은 프록시가 패스워드 인증을 할 경우에만 필요합니다. 이 옵션들은 wgetOpts 줄의 마지막 <tt>'<tt> 문자 <em>이전<em>에 추가해야 합니다. 모든 옵션은 하나의 줄에 사용되어야만 합니다.</p>

<p>리눅스에서는 <tt>/etc/environment</tt> 혹은 <tt>~/.bashrc</tt> 파일을 수정하여 <tt>ftp_proxy</tt>와 <tt>http_proxy</tt> 환경을 설정하여 사용할 수도 있습니다.</p>

<p><strong>맙소사! 스크립트가 에러와 함께 실패했습니다. 다운로드한
것들은 모두 쓸모 없어지나요?!</strong></p>

<p>물론 이런 현상은 *일어나면 안 됩니다(tm).* 하지만 큰
"<tt>.iso.tmp</tt>" 파일이 만들어진 단계에서 <tt>jigdo-lite</tt>에
문제가 생겨 다운로드를 재시작해야 할 수도 있습니다. 이 경우에는 다음과
같은 방법을 사용할 수 있습니다.</p>

<ul>

  <li>그냥 return을 누르고 다운로드를 재시작합니다. 시간
초과(timeout)나 일시적인 오류로 몇 가지 파일이 다운로드되지 않을 수
있습니다. 한 번 더 시도하면 잃어버린 파일을 다운로드할 수 있을 것입니다.</li>

  <li>다른 미러에 접속해 보세요. 몇몇 데비안 미러는 완전하게
동기화되어 있지 않습니다. 지운 파일이 여전히 들어있거나 이미
업데이트된 파일이 없을 수도 있습니다.</li>

  <li><tt><a href="http://rsync.samba.org/">rsync</a></tt>를 이용해
이미지 중 빠진 부분을 받아보세요. 우선 다운로드하고 있는 이미지의
정확한 rsync URL을 알아야 합니다. <a href="../mirroring/rsync-mirrors">stable</a>이나 <a href="../http-ftp/#testing">testing</a> 이미지에 대한 rsync 접근을 제공하는 서버를 고릅니다. 그리고 나서 정확한 경로와 파일 이름을 결정합니다. 디렉터리 목록은 <tt>rsync&nbsp;rsync://cdimage.debian.org/debian-cd/</tt> 명령으로 얻을 수 있습니다.

  <br>Next, remove the "<tt>.tmp</tt>" extension from
  <tt>jigdo-lite</tt>'s temporary file by renaming it, and pass both
  the remote URL and the local filename to rsync:
  <tt>rsync&nbsp;rsync://server.org/path/binary-i386-1.iso
  binary-i386-1.iso</tt>

  <br>상태 메시지를 보기 위해 rsync의 <tt>--verbose</tt>,
<tt>--progress</tt> 스위치를 사용하거나 속도를 높이기 위해
<tt>--block-size=8192</tt>를 사용할 수도 있습니다.</li>

  <li>모든 방법이 실패한다면 내려받은 파일을 잃어버린 것이
아닙니다. 리눅스에서는 <tt>.tmp</tt> 파일을 loop 마운트해 이미
내려받은 파일에 접근할 수 있고 그 파일들을 이용해 최신 jigdo
파일로부터 이미지를 생성할 수 있습니다(예를 들어 실패한 다운로드가
testing 스냅샷이라면 좀더 최근의 testing 스냅샷). 이렇게 하려면 우선
잘못 된 다운로드가 있는 디렉토리에서 root로서 다음 명령을 실행합니다: <tt>mkdir&nbsp;mnt;
  mount&nbsp;-t&nbsp;iso9660&nbsp;-o&nbsp;loop&nbsp;*.tmp&nbsp;mnt</tt>. 
  그리고 나서 다른 디렉토리에서 새 다운로드를 시작하고 "Files to
scan" 프롬프트에서 <tt>mnt</tt> 디렉토리의 경로를 입력합니다.</li>

</ul>
