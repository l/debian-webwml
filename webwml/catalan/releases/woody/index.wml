#use wml::debian::template title="Informació de la versió Debian ``woody''" BARETITLE="true"
#use wml::debian::translation-check translation="1.10"

<p>El nom en codi per a la versió principal de Debian, la següent després de
<a href="../potato/">potato</a> serà ``woody''.  Aquesta versió serà numerada
``3.0''.

<p>Aquesta distribució està actualment en fase de
``<a href="http://ftp-master.debian.org/testing/">proves (testing)</a>''.
El que significa que les coses <em>poden</em> desestablilitzar-se si la
utilitzeu.  S'espera que qualsevol que usi woody estigui informant de
qualsevol volatilitat, usualment a través de les llistes de correu orientades
als desenvolupadors; la distribució en proves hauria d'ésser molt més estable 
que la inestable (unstable), no obstant això hauria d'ésser precagut.

<p>Com ja és usual <strong>no</strong> s'han establert dates específiques per
a avançar.  És tan simple com "Debian allibera una versió, al seu temps".

<p>Les actualitzacions més esperades per a quan s'alliberi la versió woody,
inclouen el nucli 2.4, GLibC 2.2, GCC 3.0, XFree86 4.0 i Perl 5.6.  També
és possible que el paquet <code>debconf</code> s'usi en tots els paquets que
tinguin que preguntar quelcom a l'usuari als scripts de gestió.

<p>Si esteu usant APT, podeu usar les següents línies al vostre fitxer
<code>/etc/apt/sources.list</code> per a poder accedir als paquets en "proves":

<pre>
  deb http://http.us.debian.org/debian testing main contrib non-free
  deb http://non-us.debian.org/debian-non-US testing/non-US main contrib non-free
</pre>

<p>Llegiu les pàgines del manual <code>apt-get</code>(8) i el
<code>sources.list</code>(5) per a més informació.

# Local variables:
# mode: sgml
# sgml-indent-data:nil
# sgml-doctype:"../.doctype"
# End:
