<define-tag description>prekoraèenje buffera u klocku, kvt sprema datoteke kao root</define-tag>
<define-tag moreinfo>
<p>Primili smo izvje¹taj da se jednostavno prekoraèenje buffera mo¾e
iskoristiti za pristup grupi "shadow" na sustavima na kojima radi klock.
Takoðer je postojao problem s kvtom koji je uzrokovao snimanje konfiguracije
kao root umjesto kao obièan korisnik.

<p>Preporuèamo da hitno nadogradite svoj kdebase paket.
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980530a.data'

#use wml::debian::translation-check translation="1.6"
