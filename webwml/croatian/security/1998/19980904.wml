<define-tag description>prekoraèenje buffera u mountdu</define-tag>
<define-tag moreinfo>
Program rpc.mountd je daemon za montiranje koji radi s NFS montiranjem.
Verzija koja se isporuèuje s trenutnim distribucijama Linuxa sadr¾i
prekoraèenje buffera.

<p>Ovo prekoraèenje buffera se mo¾e koristiti kao dio napada koji dozvoljava
dobivanje root pristupa na stroju koji radi kao NFS poslu¾itelj.

<p>Preporuèamo vam da hitno nadogradite va¹ netstd paket.
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980904.data'

#use wml::debian::translation-check translation="1.5"
