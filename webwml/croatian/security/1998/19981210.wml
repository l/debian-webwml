<define-tag description>prekoraèenje buffera pri logiranju</define-tag>
<define-tag moreinfo>
Nedavno se pojavilo dosta zbunjenosti oko ssha: neki ljudi misle da je u
njihove sustave provaljeno kroz ssh, iako nitko nije mogao napraviti
exploit. Kako bi izbjegli sve moguæe probleme, zakrpali smo ssh i popravili
sva moguæa prekoraèenja buffera. Mislimo da æe ovo zaustaviti svaki napad
koji bi se mogao pojaviti. Ovo takoðer ukljuèuje ispravke za kerberos kod
koji su bili distribuirani.

<p>Molimo primijetite da ova zakrpa ne pati od problema s licencom koje
imaju ostale zakrpe koje su kru¾ile, jer on ne koristi vsnprintf
implementaciju iz ssh 2 veæ kod iz sendmaila (koji je baziran na kodu koji
je plutao na usenetu) za sustave koji nemaju vsnprintf u njihovom libcu.

<p>Preporuèamo vam da hitno nadogradite svoj ssh paket.
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19981210.data'

#use wml::debian::translation-check translation="1.10"
