<define-tag description>Program "screen" prelazi granicu pri kopiranju gecos polja.</define-tag>
<define-tag moreinfo>
<p>Prekoraèenje postoji, ali screen predaje root privilegije prije nego ¹to
se kod izvr¹i.

<p>Originalni po¹iljatelj ovog izvje¹æa: Khelbin &lt;khelbin@connix.com&gt;

<p>Screen 3.07.02, kada je setuid root (a obièno jest), je osjetljiv na
prekoraèenje buffera, makar na nekim platformama. Nisam proèitao cijeli
izvorni kod, ali nakon kratkog pregleda sam primijetio da attacher.c radi
sljedeæe:

<pre>
      struct passwd ppp;
      char fullname[100];

      strcpy(fullname, ppp-&gt;pw_gecos);
</pre>

<p>Mogao sam nabrzinu napisati exploit, ali nije radio ovdje na BSDI 1.1
zato ¹to chpass/chfn ne prihvaæa neke znakove. Zbog toga je uèitavanje
koda za dobivanje ljuske u "Full Name" polje uzrokovalo poruku o gre¹ci
"Illegal Character found in the Full Name field,  re-edit [y]?" ili ne¹to
slièno.

<p>Svaki OS ili verzija chfna/chpassa koja ne provjerava 'ilegalne znakove'
ili du¾inu informacija koje se stavljaju u polje (BSDI 1.1 nije provjeravao
du¾inu, samo ilegalne znakove) bi mogao biti ranjiv (nisam provjerio je li
veæ odustao od suid root privilegija, to sam mislio uèiniti prekoraèujuæi
buffer mojim shellcode stringom).

<p>Ono ¹to jesam radio je puko stavljanje mog shellcode stringa u ENV
varijablu (s NOPovima i povratnom adresom natrag na NOPove) i echoanje ENV
varijable u datoteku. Tada samo uèitavate tu datoteku kada ste u
chpass/chfn kao nove gecos informacije.
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1997/19970220.data'

#use wml::debian::translation-check translation="1.8"
