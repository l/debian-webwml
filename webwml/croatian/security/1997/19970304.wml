<define-tag description>SuperProbe (iz XFree86) sadr¾i vi¹e prekoraèenja buffera</define-tag>
<define-tag moreinfo>
<p>SuperProbe nije instaliran setuid root na Debianu.

<p>Originalni po¹iljatelj ovog izvje¹æa: &lt;solar@ideal.ru&gt;

<p>SuperProbe je program koji se isporuèuje s XFree86 (kako bi odredili tip
video hardvera instaliranog na raèunalu), i instaliran je setuid root u
puno Linux distribucija.

<p>Prekoraèenje koje iskori¹tavam je u TestChip funkciji [...]
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1997/19970304.data'

#use wml::debian::translation-check translation="1.6"
