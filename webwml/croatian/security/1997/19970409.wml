<define-tag description>Mo¾da je moguæe natjerati metamail na izvr¹avanje ¾eljenih naredbi</define-tag>
<define-tag moreinfo>
<p>Originalni po¹iljatelj ovog izvje¹æa: Olaf Kirch &lt;okir@lst.de&gt;

<p>Rupa bi se mogla iskoristiti ako dopustite metamailu da pokreæe showext
za poruke tipa message/external-body. Makar tcsh, i moguæe par drugih
cshova, izgleda rade èudne stvari pri pro¹irivanju argumenata na naredbenom
retku. Ako date skripti argument "foo FTP=/tmp/zlanaredba", i ona uèini

<pre>
        set var=$1
</pre>

<p>to æe dodijeliti foo u $var, i /tmp/zlanaredba u $FTP. Na¾alost, metamail
pokreæe showext s MIME atributima na naredbenom retku, tako da mu u osnovi
mo¾ete poslati zaglavlje kao ovo

<pre>
   Content-type:  message/external-body;
           access-type="anon-ftp";
           name="passwd";
           site="monad.swb.de";
           directory="/etc";
           mode="image FTP=/tmp/zlanaredba"
</pre>

<p>Dalje ispod, skripta æe pokrenuti $FTP kako bi pokrenula FTP vezu. Dosad,
nisam uspijevao proslijediti argumente naredbi, ali to ne znaèi da ne mo¾ete
napraviti zanimljive stvari s ovim gore.

<p>[Zakrpa je obrisana zbog starosti.]

</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1997/19970409.data'

#use wml::debian::translation-check translation="1.9"
