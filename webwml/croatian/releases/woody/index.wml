#use wml::debian::template title="Informacije o izdanju za Debian GNU/Linux 3.0 ``woody''" BARETITLE="true"
#include "$(ENGLISHDIR)/releases/info"
#use wml::debian::translation-check translation="1.16"

<h2>Woody dolazi</h2>

<p>Kodno ime sljedeæeg velikog izdanja Debiana nakon
<a href="../potato/">potatoa</a> je "woody". Ovo æe izdanje imati
verziju "3.0".</p>

<p>Ova distribucija je trenutno u fazi zvanoj
"<a href="http://ftp-master.debian.org/testing/">testing</a>", ali se
stabilizira i blizu je toga da postane
"<a href="$(DOC)/FAQ/ch-ftparchives.html#s-frozen">frozen</a>".

<p>Instalacijski sustav za potato se vi¹e ne odr¾ava aktivno, i preporuèamo
kori¹tenje instalacijskog sustava iz woodya za nove instalacije, iako on jo¹
uvijek nije dostupan na CD-ima. Novi <a href="installmanual">instalacijski
priruènik</a> i <a href="releasenotes">napomene izdanja</a> su dostupni 
za pregled.</p>

<p>Kao i obièno, <strong>ne</strong> postavljaju se specifièni ciljevi
izdanja, <strong>niti</strong> æe datum objave biti unaprijed odreðen.
Jednostavno, "Debian objavljuje kada bude vrijeme".</p>

<p>Oèekivane veæe nadogradnje, u vrijeme objave woodya, ukljuèuju GLibC 2.2,
GCC 3.0, XFree86 4.1 i Perl 5.6. Linux kernel 2.4 æe takoðer biti dostupan
kao izbor, ali ne smatramo da je u ovom trenutku dovoljno zreo da postane
poèetna jezgra za veæinu arhitektura. Takoðer je vjerojatno da æe se
<code>debconf</code> paket koristiti u skoro svim paketima koji moraju ne¹to
pitati u <em>maintainer skriptama</em>.</p>

<p>Ako koristite APT, mo¾ete koristiti sljedeæe retke u va¹oj
<code>/etc/apt/sources.list</code> datoteci kako bi mogli doæi do paketa iz
<em>testing</em>a:</p>

<pre>
  deb http://http.us.debian.org/debian testing main contrib non-free
  deb http://non-us.debian.org/debian-non-US testing/non-US main contrib non-free
</pre>

<p>Proèitajte <code>apt-get</code>(8) i <code>sources.list</code>(5)
manual stranice za vi¹e informacija.</p>
