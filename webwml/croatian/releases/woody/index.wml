#use wml::debian::template title="Informacije o izdanju za Debian ``woody''" BARETITLE="true"
#use wml::debian::translation-check translation="1.8"

<p>Kodno ime sljedeæeg velikog izdanja Debiana nakon
<a href="../potato/">potatoa</a> je ``woody''. Vjerojatno je da æe ovo
izdanje imati verziju ili ``2.3'' ili ``3.0''.

<p>Ova distribucija je trenutno u fazi zvanoj
``<a href="http://ftp-master.debian.org/testing/">testing</a>''. To znaèi da
ukoliko budete radili s njom, stvari bi se <em>mogle potrgati</em>.
Od svakoga tko radi s woodyem se oèekuje da bude informiran o svim
nepredvidivostima, obièno praæenjem mailing lista orijentiranih prema
razvijateljima; testing distribucija bi trebala biti stabilnija od unstable,
ali bi ipak trebali biti oprezni.

<p>Kao i obièno, <strong>ne</strong> postavljaju se specifièni ciljevi
izdanja, <strong>niti</strong> æe datum objave biti unaprijed odreðen.
Jednostavno, "Debian objavljuje kada bude vrijeme".

<p>Oèekivane veæe nadogradnje, u vrijeme objave woodya, ukljuèuju Linux
kernel 2.4, GLibC 2.2, GCC 3.0, XFree86 4.0 i Perl 5.6. Takoðer je
vjerojatno da æe se <code>debconf</code> paket koristiti u svim paketima
koji moraju ne¹to pitati u maintainer skriptama.

<p>Ako koristite APT, mo¾ete koristiti sljedeæe retke u va¹oj
<code>/etc/apt/sources.list</code> datoteci kako bi mogli doæi do paketa iz
testinga:

<pre>
  deb http://http.us.debian.org/debian testing main contrib non-free
  deb http://non-us.debian.org/debian-non-US testing/non-US main non-free
</pre>

<p>Proèitajte <code>apt-get</code>(8) i <code>sources.list</code>(5)
manual stranice za vi¹e informacija.

# Local variables:
# mode: sgml
# sgml-indent-data:nil
# sgml-doctype:"../.doctype"
# End:
