#use wml::debian::template title="Kako podesiti ssh da vas ne pita lozinku" BARETITLE=true
#use wml::debian::translation-check translation="1.5"

<p>Mo¾ete napraviti RSA kljuè za prepoznavanje kako bi se mogli ulogirati na
udaljeno mjesto s va¹eg korisnièkog raèuna, bez potrebe da upisujete
lozinku.

<p>Primijetite da jednom kad ovo podesite, ako neki uljez provali u va¹
korisnièki raèun/sustav, omoguæen mu je pristup sustavu na kojeg smijete bez
lozinke, takoðer! Zbog toga se ovo <strong>nikad</strong> ne bi smjelo
raditi kao root.

<ul>
  <li>Pokrenite <code>ssh-keygen</code> na va¹em stroju, i samo pritisnite
      enter kada vas bude pitao lozinku.
      <br>
      Sa starijim verzijama SSH-a, ovo æe generirati
      <code>~/.ssh/identity.pub</code>; s novijima, generirat æe
      <code>~/.ssh/id_rsa.pub</code>.
  <li>Nadalje, dodajte sadr¾aj spomenute datoteke u
      <code>~/.ssh/authorized_keys</code> na udaljenom sustavu
      (ta datoteka mora imati dozvole 600).
      <br>
      Ako ste razvijatelj i ¾elite pristupati debian.org sustavima s takvim
      kljuèem, baza podataka o razvijateljima vam mo¾e propagirati kljuè na
      sve debian.org strojeve. Pogledajte
      <a href="http://db.debian.org/doc-mail.html">dokumentaciju LDAP
      <i>gatewaya</i></a>.
</ul>

<p>Tako biste trebali moæi koristiti ssh za logiranje na poslu¾itelj
bez pitanja o lozinci.
