#use wml::debian::template title="Kori¹tenje WML-a"
#use wml::debian::translation-check translation="1.11"

<p>WML znaèi <i>web site meta language</i>, tj. opæeniti jezik za web
stranice. To znaèi da WML uzima ulazne .wml datoteke, procesira ¹to god je u
njima (a to mo¾e biti sve od osnovnog HTML-a do Perl kôda!), i ispisuje ¹to
god ¾elite da ispi¹e, npr. .html ili .php.</p>

<p>Nije ba¹ jednostavno uèiti iz dokumentacije WML-a. Zapravo je vrlo
sveobuhvatna, ali dok ne uspijete shvatiti kako sve radi (a to je prilièno
moæno), najlak¹e je uèiti iz primjera. Vjerojatno æe vam biti korisne
datoteke predlo¾aka koje se koriste u Debian stranicama. One se mogu naæi u
<code><a href="http://anonscm.debian.org/viewvc/webwml/webwml/english/template/debian/">\
webwml/english/template/debian/</a></code>.</p>

<p>Ovo podrazumijeva da veæ imate WML instaliran na va¹em stroju.
WML je dostupan kao <a href="http://packages.debian.org/wml">Debian
paket</a>.</p>


<h2>Ureðivanje WML izvornika</h2>

<p>Jedna stvar koju æe imati sve .wml datoteke su jedna ili vi¹e uvodnih
redaka koji poèinju s <code>#use</code>. Ne smijete promijeniti ili prevesti
njihovu sintaksu, samo nizove znakove pod navodnicima kao ¹to su oni nakon
<code>title=</code>, ¹to æe promijeniti &lt;title&gt; element u izlaznim
datotekama.</p>

<p>Izuzev redaka zaglavlja, veæina na¹ih .wml stranica sadr¾i jednostavan
HTML. Ako naiðete na oznake kao ¹to su &lt;define-tag&gt; ili &lt;: ...
:&gt;, budite oprezni jer oni ograðuju kôd koji se procesira jednim od
WML-ovih posebnih prolaza. Za vi¹e informacija pogledajte dolje.</p>


<h2>Izraðivanje Debian web stranica</h2>

<p>Jednostavno otipkajte <kbd>make</kbd> u webwml/&lt;jezik&gt; direktoriju.
Podesili smo <i>makefileove</i> koji pozivaju <kbd>wml</kbd> sa svim pravim
argumentima.</p>

<p>Ako napravite <kbd>make install</kbd> tada æe se HTML datoteke izraditi i
postaviti u direktorij <kbd>../../www/</kbd>.</p>


<h2>Dodatne moguænosti WML-a koje mi koristimo</h2>

<p>Jedna od znaèajki WML-a koju prilièno koristimo je kori¹tenje Perla.
Zapamtite, ovo nisu dinamièke stranice. Perl se koristi u trenutku kada
se HTML stranice generiraju, za, zapravo, bilo ¹to. Dva dobra primjera
u kojima koristimo Perl na stranicama su kreiranje liste najsvje¾ijih
stavki vijesti i generiranje veza na druge prijevode na kraju stranica.

# TODO: add the basic stuff from webwml/english/po/README here

<p>Za izradu predlo¾aka na¹eg <i>web sitea</i> potreban je wml verzije
&gt;= 2.0.6. Za izradu gettext predlo¾aka za ne-engleske prijevode potreban
je mp4h &gt;= 1.3.0.</p>


<h2>Specifièni problemi s WML-om</h2>

<p>Multi-byte jezicima bi moglo biti potrebno posebno pre- ili
post-procesiranje .wml datoteka da bi se ispravno rukovalo znakovnim skupom.
Ovo se mo¾e napraviti odgovarajuæim mijenjanjem varijabli
<kbd>WMLPROLOG</kbd> i <kbd>WMLEPILOG</kbd> u
<kbd>webwml/&lt;jezik&gt;/Make.lang</kbd>. Ovisno o tome kako radi va¹
<kbd>WMLEPILOG</kbd> program, mo¾da æete morati promijeniti vrijednost
varijable <kbd>WMLOUTFILE</kbd>.
<br>
Pogledajte japanski ili kineski prijevod za primjer.
</p>
