#use wml::debian::template title="Kori¹tenje WML-a"

<p>Ovo podrazumijeva da veæ imate WML instaliran na va¹em stroju.
WML je dostupan kao Debian paket ("wml").

<p>Jednostavno otipkajte <kbd>make</kbd> u webwml/&lt;jezik&gt; direktoriju.
Ako napravite <kbd>make install</kbd> tada æete HTML datoteke naæi u
direktoriju <kbd>../../debian.org/</kbd>.

<P>Multi-byte jezicima bi moglo biti potrebno posebno pre- ili post-procesiranje
wml datoteka da bi se ispravno rukovalo znakovnim skupom. Ovo se mo¾e
napraviti odgovarajuæim mijenjanjem varijabli <kbd>WMLPROLOG</kbd> i
<kbd>WMLEPILOG</kbd> u <kbd>webwml/&lt;jezik&gt;/Make.lang</kbd>. Ovisno
o tome kako radi va¹ <kbd>WMLEPILOG</kbd> program, mo¾da æete morati
promijeniti vrijednost varijable <kbd>WMLOUTFILE</kbd>.
Pogledajte japanski ili kineski prijevod za primjer.

<p>Jedna od stvari s kojom æete se suoèiti u WML-u su slice-ovi. Za na¹u
svrhu ovdje, mo¾ete ih smatrati jednim moguæim izborom iz brojnih opcija.
Jednostavan primjer æe pokazati ¹to to znaèi:
<pre><protect>
   [EN:the salad:]
   [DE:der Salat:]
</protect></pre>
U ovom sluèaju postoje dva slice-a, jedan na engleskom i jedan na njemaèkom.
Da biste dodali slice, samo dodajte novi u svom jeziku. Npr.:
<pre><protect>
   [EN:the salad:]
   [DE:der Salat:]
   [FR:la salade:]
</protect></pre>
Kada pokrenete WML na nekoj datoteci, on æe automatski odabrati slice-ove
prikladne va¹em prijevodu i ignorirati ostale.

<h3>Kori¹tenje Perla u WML-u</h3>
<p>Jedna od znaèajki WML-a koju prilièno koristimo je kori¹tenje Perla.
Zapamtite, ovo nisu dinamièke stranice. Perl se koristi u trenutku kada
se HTML stranice generiraju, za, zapravo, bilo ¹to. Dva dobra primjera
u kojima koristimo Perl na stranicama su kreiranje liste najsvje¾ijih
stavki vijesti i generiranje veza na druge prijevode na kraju stranica.

<p>Nije ba¹ jednostavno uèiti iz dokumentacije wml-a. Zapravo je vrlo
sveobuhvatna, ali dok ne uspijete shvatiti kako sve radi (a to je
prilièno moæno), najlak¹e je uèiti iz primjera. Template datoteke koje
se koriste u Debian stranicama æete vjerojatno naæi korisnima. One se
mogu naæi u <code>english/template/debian/</code>.
