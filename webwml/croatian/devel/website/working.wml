#use wml::debian::template title="Kako raditi na Debian Web stranicama"
#use wml::debian::translation-check translation="1.4"

<h2>Dodavanje novih stranica</h2>

<p>Dodavanje novih stranica je vrlo jednostavno. Sav posao dodavanja
potrebnog zaglavlja i dna stranice obavlja WML. Sve ¹to trebate napraviti
je ukljuèiti ovakvu liniju na vrh nove datoteke:

<pre><protect>
#use wml::debian::template title="NASLOV STRANICE"
</protect></pre>

<p>nakon kojeg je tijelo teksta. Sve stranice trebaju koristiti
<code>wml::debian::template</code> datoteku predlo¹ka osim ako ne koriste
neku posebnu stvorenu ba¹ za tu sekciju, npr. News/ ili security/ stranice.

<p>Predlo¹ci koje imamo vam dopu¹taju da definirate odreðene varijable koje
æe utjecati na generirane stranice. Ovo treba zaobiæi potrebu za kreiranjem
razlièitih predlo¾aka za svaku situaciju i dozvoljava lak¹e implementiranje
pobolj¹anja. Ovo su trenutno dostupne varijable i njihova svrha:
<dl>
<dt>BARETITLE="true"
	<dd>Mièe "Debian GNU/Linux --" dio koji se obièno dodaje ispred svih
	&lt;title&gt; tagova.
<dt>NOHEADER="true"
	<dd>Mièe osnovno zaglavlje sa stranice. Posebno zaglavlje se naravno
        mo¾e ukljuèiti u tijelo teksta.
<dt>NOHOMELINK="true"
	<dd>Mièe link koji upuæuje na glavnu Debian stranicu, koji se inaèe
	dodaje na dno stranice.
<dt>NOLANGUAGES="true"
	<dd>Mièe linkove koji upuæuju na verzije na drugim jezicima, koji se
        inaèe dodaju na dnu stranice.
<dt>GEN_TIME="true"
	<dd>Stavlja datum izlaznih datoteka u datumski peèat generiranih
	datoteka, umjesto datumskog peèata ulazne datoteke.
<dt>NOCOPYRIGHT="true"
	<dd>Uklanja obavijest o autorskim pravima s dna stranice.
</dl>

<p>Primijetite kako mo¾ete koristiti bilo koju rijeè kao vrijednost ovih
varijabli, "true", "yes", "bla", nije bitno.

<p>Primjer upotrebe ovoga je na stranicama portova koje imaju vlastita
zaglavlja. <code>ports/arm/index.wml</code> koristi:

<pre><protect>
#use wml::debian::template title="ARM Port" NOHEADER="yes"
</protect></pre>

<p>Ako ¾elite ne¹to napraviti ¹to se ne mo¾e izvr¹iti kori¹tenjem postojeæih
predlo¾aka, prvo razmislite o pro¹irivanju jednog od njih. Ako nije moguæe
pro¹iriti jedan od njih, u smislu da je kompatibilan s ranijima, probajte
napraviti novi predlo¾ak kao nadogradnju na postojeæi tako da æe se stranice
moæi konvertirati na njega u sljedeæoj glavnoj nadogradnji (nadajmo se nikad
daljoj od svako 6 mjeseci).

<p>Ako pi¹ete stranicu koja se generira pomoæu skripte ili ima malo pravog
teksta, razmislite o upotrebi filtera 'odrezaka' (slices) unutar WML-a da bi
olak¹ali zadatak odr¾avanja prijevoda a¾urnima. Dobar primjer ovoga je u 
<kbd>distrib/vendors.wml</kbd>. Novi prodavaèi se mogu dodati na ovu
stranicu i dodatak æe se generirati za svaki jezik automatski.
<kbd>vendors.wml</kbd> ukljuèuje <kbd>vendors.CD</kbd> koji ustvari sadr¾i
pravu listu prodavaèa. Ova datoteka usto ukljuèuje i <kbd>distrib/vendors.CD.def</kbd>
koja sadr¾i definicije svih fraza u <kbd>vendors.CD</kbd>.

<p>Prevoditelji jednostavno trebaju dodati prijevode gdje god je `odrezak'
(slice), npr.
<pre><protect>
	[EN:Country:]
	[ES:Pa-is:]
	[DE:Land:]
	[HR:Zemlja:]
</protect></pre>

<h2>Dodavanje novog direktorija</h2>

<p><strong>Nemojte</strong> napraviti direktorij s imenom
<code>install</code>. To æe zbuniti make i stranice u tom direktoriju se
neæe automatski obnavljati.

<p>Ispod je primjer dodavanja novog direktorija na web site:
<pre>
   mkdir bla
   cvs add bla
   cd bla
   cp ../intro/Makefile .
   cvs add Makefile
</pre>

<p>Uredite Makefile datoteku u direktoriju odmah iznad novoga, i dodajte
direktorij koji ste upravo napravili u <code>SUBS</code> varijablu. To æe
dodati direktorij meðu one koji se generiraju kada se pokrene make.

<p>Na kraju, po¹aljite sve upravo napravljene promjene u glavni repository s
<pre>
  cvs commit Makefile bla
</pre>
