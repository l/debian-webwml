#use wml::debian::template title="Primjeri"
#use wml::debian::translation-check translation="1.4"

<H3>Primjer zapoèinjanja prijevoda</H3>

<p>Francuski jezik æemo koristiti za primjer:

<pre>
   cvs checkout webwml/Makefile.common webwml/english
   cd webwml
   mkdir french
   cvs add french
   cd french
   cp ../english/.wmlrc ../english/Make.* ../english/Makefile .
   cvs add Make* .wmlrc
</pre>

<p>Editirajte <tt>.wmlrc</tt> datoteku i promijenite:
<ul>
  <li>'-D CUR_LANG=English' u '-D CUR_LANG=French'
  <li>'-D CUR_ISO_LANG=en' u '-D CUR_ISO_LANG=fr'
  <li>'-D CUR_LOCALE=en_US' u '-D CUR_LOCALE=fr_FR'
  <li>'-D CHARSET=iso-8859-1' u ¹to god je prikladno.<br>
      Francuski jezik tek sluèajno koristi isti set znakova kao i engleski
      pa ovo nije potrebno mijenjanti, ipak, vjerojatno je da æe novi jezici
      trebati imati ovu postavku promijenjenu.
</ul>

<p>Editirajte Make.lang i promijenite 'LANGUAGE := en' u 'LANGUAGE := fr'.
Ako prevodite na jezik koji koristi multi-byte skup znakova, mo¾da æete
morati mijenjati i neke druge varijable u toj datoteci, za vi¹e informacija
proèitajte ../Makefile.common i mo¾da neke druge primjere (prijevode kao ¹to
je kineski).

<p>Editirajte english/template/debian/*.wml i dodajte prijevode za sve
&lt;define-tag ...&gt; unose. Ovo bi trebalo biti sasvim izravno.

<p>Uvijek provjerite da ste iskopirali Makefile u svaki direktorij koji
prevedete. Ovo je potrebno zato ¹to koristimo program <code>make</code> za
pretvaranje .wml datoteka u HTML, a <code>make</code> koristi Makefileove.

<p>Kad zavr¹ite s dodavanjem i editiranjem stranica, napravite

<pre>
   cvs commit
</pre>

iz webwml direktorija. Sada mo¾ete poèeti prevoditi stranice.

<H3>Primjer prevoðenja jedne stranice</H3>

<p>Kao primjer æemo koristiti francuski prijevod Dru¹tvenog ugovora:

<pre>
   cd webwml/french
   cp ../english/social_contract.wml .
   cvs add social_contract.wml
</pre>

<p>Editirajte social_contract.wml i prevedite tekst. Nemojte poku¹avati
prevesti veze ili ih ikako mijenjati - ako ¾elite ne¹to promijeniti,
zatra¾ite to na debian-www listi. Kada zavr¹ite, napi¹ite

<pre>
   cvs commit -m "short description of the changes you made" social_contract.wml
</pre>

<H3>Primjer dodavanja novog direktorija</H3>

<p>Ovaj primjer pokazuje dodavanje francuskog prijevoda intro/ direktorija:

<pre>
   cd webwml/french
   mkdir intro
   cvs add intro
   cd intro
   cp ../english/intro/Makefile .
</pre>

Provjerite da novi direktorij sadr¾i Makefile i da je u CVSu. Inaèe æe
pokretanje make-a dati gre¹ku svima drugima koji to budu poku¹ali.

<pre>
   cvs add Makefile
   cd ..
   cvs commit -m "added the intro dir to CVS" intro
</pre>
