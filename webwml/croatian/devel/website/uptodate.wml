#use wml::debian::template title="Odr¾avanje prijevoda web stranica aktualnima"
#use wml::debian::translation-check translation="1.15"

<P>Buduæi da web stranice nisu statiène, dobra je ideja pratiti na koju se
verziju originala neki prijevod odnosi, i koristiti tu informaciju za
provjeravanje koje stranice su se promijenile od zadnjeg prevoðenja.
Ova informacija treba biti upisana na vrh dokumenta (no ispod ostalih "use"
zaglavlja) u ovom obliku:

<pre>
\#use wml::debian::translation-check translation="X.x"
</pre>

<p>gdje je <var>X.x</var> broj CVS verzije originala (engleskog). Ovaj broj
mo¾ete dobiti gledajuæi u CVS/Entries koji ima oblik:
<code>/ime_datoteke/X.x/datum//</code>. Takoðer ga mo¾ete dobiti tako da
pokrenete <kbd>cvs status file_name</kbd> u direktoriju engleskog izvornika.
Ako koristite <kbd>copypage.pl</kbd> skriptu iz webwml direktorija, ova
linija æe biti dodana automatski.

<P>Ovo je korisno i zato ¹to neki prijevodi mo¾da ne budu nadopunjavani
prilièno vremena, iako se dokument na originalnom jeziku (engleskom)
mijenja. Zbog pregovaranja sadr¾aja, èitatelj prevedene stranice neæe biti
svjestan ovoga i mo¾da neæe proèitati bitne informacije, uvedene u novim
verzijama originala. <code>translation-check</code> template koristi kod
koji æe provjeriti je li va¹ prijevod zastario, i prikazati prikladnu poruku
koja æe korisnika upozoriti na to.

<p>Postoje i dodatni parametri koje mo¾ete koristiti u <code>#use</code>
retku:

<dl>
 <dt><code>original="<var>jezik</var>"</code>
 <dd>gdje je <var>jezik</var> ime jezika s kojeg prevodite, ako nije
 engleski ("english").
 Ime mora odgovarati imenu vr¹nog poddirektorija u CVS-u, i imenu u
 predlo¹ku <code>languages.wml</code>.

 <dt><code>mindelta="<var>broj</var>"</code>
 <dd>koji definira najveæu razliku u CVS revizijama prije nego se
 prijevod proglasi starim.
 Pretpostavljena vrijednost je <var>1</var>.
 Za manje va¾ne stranice, postavite je na <var>2</var>, ¹to znaèi da æe se
 morati promijeniti dva puta kako bi se to shvatilo kao zastarjelost prijevoda.

 <dt><code>maxdelta="<var>broj</var>"</code>
 <dd>koji definira najveæu razliku u CVS revizijama prije nego se prijevod
 proglasi prestarim.
 Pretpostavljena vrijednost je <var>5</var>.
 Za vrlo va¾ne stranice, smanjite je.
 Vrijednost <var>1</var> znaèi da æe se svaka promjena shvatiti kao
 progla¹avanje prijevoda prestarim.
</dl>

<p>Na osnovu ovoga stvara se <a href="stats/">izvje¹taj</a> koji pokazuje
zastarjele prijevode kao i stranice koje uopæe nisu prevedene. Ovo je
namijenjeno tome da pomogne prevoditeljima i da privuèe nove ljude da
pomognu.</p>

<p>
Kako bi izbjegli prikazivanje informacija koje su previ¹e zastarjele
korisnicima, prijevodi koji nisu a¾urirani unutar ¹est mjeseci od promjene
originalne stranice æe biti automatski izbaèeni.
Molimo pogledajte
<a href="http://people.debian.org/~peterk/outdated/">popis zastarjelih
prijevoda</a> kako bi na¹li stranice koje su opasnosti od izbacivanja.
</p>

<p>Dodatno, dostupna je skripta <code>check_trans.pl</code> u webwml
direktoriju, kako bi vam pokazala izvje¹taj o stranicama koje trebaju
nadopune:

<pre>
check_trans.pl <var>jezik</var>
</pre>

<p>zamijenite <var>jezik</var> imenom direktorija koji sadr¾i va¹ prijevod,
npr. "croatian".

<p>Stranice koje nemaju prijevod æe biti prikazane kao
"<code>Missing <var>datoteka</var></code>", a stranice koje nisu a¾urne s
originalom æe biti prikazane kao
"<code>NeedToUpdate <var>datoteka</var> to version <var>x.y</var></code>".

<P>Ako ¾elite vidjeti koje su to toène promjene, mo¾ete vidjeti promjene
tako da dodate <code>-d</code> opciju gornjoj naredbi. Molimo primijetiti da
ovo mo¾e potrajati, zato ¹to mo¾e ukljuèiti nekoliko pristupa CVS serveru.

<P>Ako ¾elite ignorirati upozorenja o stranicama koje nedostaju (naprimjer
stare vijesti), mo¾ete napraviti datoteku s imenom <code>.transignore</code>
u direktoriju gdje ¾elite potisnuti upozorenja, u koju æete upisati sve
datoteke koje neæete prevesti, jedno ime po liniji.

<p>Slièna skripta za praæenje prijevoda opisa mailing lista je takoðer
dostupna. Molimo proèitajte komentare u <code>check_desc_trans.pl</code>
skripti kao dokumentaciju.</p>
