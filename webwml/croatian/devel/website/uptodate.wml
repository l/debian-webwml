#use wml::debian::template title="Odr¾avanje prijevoda aktualnima"

<P>Buduæi da web stranice nisu statiène, dobra je ideja pratiti na koju se
verziju originala neki prijevod odnosi, i koristiti tu informaciju za
provjeravanje koje stranice su se promijenile od zadnjeg prevoðenja.
Da vam pomogne u ovom, skripta <code>check_trans.pl</code> je dostupna u
webwml direktoriju.

<P>Skripta se oslanja na to da prevedene WML datoteke sadr¾e informacije
o tome koju verziju (originala) prevode. Ova informacija treba biti
ukljuèena negdje u dokumentu u obliku
<code>&lt;!--translation <var>x.x</var>--&gt;</code>
gdje je <var>x.x</var> verzija originala (engleskog).

To bi trebalo biti upisano u WML datoteci na zasebnoj liniji, a mo¾ete i
staviti prefiks <code>#</code> (WML komentar) da ju ne stavite u generirani
HTML.

<P>Ako pokrenete <code>check_trans.pl <var>jezik</var></code> (zamijenite
<var>jezik</var> s imenom direktorija koji sadr¾i va¹ prijevod, npr.
"croatian"), dobit æete izvje¹taj o stranicama koje nemaju prijevod
(prikazane kao "<code>Missing <var>datoteka</var></code>") i stranice koje
nisu aktualne (prikazane kao "<code>NeedToUpdate <var>datoteka</var> to
version <var>x.x</var></code>").

<P>Ako ¾elite vidjeti koje su to toène promjene, mo¾ete vidjeti promjene
tako da pokrenete <code>check_trans.pl -d <var>jezik</var></code>.
Molimo primijetiti da ovo mo¾e potrajati, zato ¹to mo¾e ukljuèiti nekoliko
pristupa CVS serveru.

<P>Ako ¾elite ignorirati upozorenja o stranicama koje nedostaju (naprimjer
stare vijesti), mo¾ete napraviti datoteku s imenom <code>.transignore</code>
u direktoriju gdje ¾elite potisnuti upozorenja, u koju æete upisati sve
datoteke koje neæete prevesti, jedno ime po liniji.
