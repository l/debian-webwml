#use wml::debian::template title="Efterfrågade paket, sorterade efter ålder" GEN_TIME="true"
#use wml::debian::translation-check translation="1.1"

#include "$(ENGLISHDIR)/devel/wnpp/wnpp.data"

<p>Den här listan finns också tillgänglig sorterad
<a href="requested">efter paketnamn</a>.</p>

<requested_byage />

<p>Den här listan finns också tillgänglig sorterad
<a href="requested">efter paketnamn</a>.</p>
