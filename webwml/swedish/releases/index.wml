#use wml::debian::template title="Versionsfakta"

      <h2>Utgåvor efter status</h2>

<p>

Följande utgåvor presenteras baserat på deras utgåvestatus.
Länkarna är alltid tillgängliga, eftersom Debian alltid åtminstone
har två utgåvor som aktivt underhålls: "stable" (stabil) och
"unstable" (instabil).

<dl>
	<dt><a href="stable/">stable</a>
	<dd>
Den "stabila" versionen innehåller den mest nyligen officiellt
släppta Debiandistributionen.
Detta är produktionsversionen av Debian.
För närvarande är det <em>slink</em> som är den "stabila" versionen.
</dd>
	<dt><a href="unstable/">unstable</a>
	<dd>
Den "instabila" versionen är var aktiv utveckling av Debian pågår.
Vanligtvis används den av utvecklarna, och av de som vill leva på
avgrunden.
För närvarande är det <em>potato</em> som är den "instabila" versionen.

<: if ( -d 'frozen' ) { _:>
	<dt><a href="frozen/">frozen</a>
	<dd>
Den "frusna" versionen är kandidaten för nästa "stabila" versionen.
Just nu finns det en "frusen" version (vanligtvis finns det
bara "stabila" och "instabila" versioner).
För närvarande är det <em>
<: print readlink('frozen'); # FYI, this doesn't work, I had it above too :>
</em> som är den "frusna" versionen.
<: } :>

      </dl>

<h2>Utgåvor efter namn</h2>
<p>

Följande utgåvor är helt enkelt andra sätt att adressera samma
distributioner som ovan.
<ul>
<: if ( -d 'hamm' ) { _:>
	<li><a href="hamm/">Debian 2.0 (hamm)</a>
<: } :>
<: if ( -d 'slink' ) { _:>
	<li><a href="slink/">Debian 2.1 (slink)</a>
<: } :>
<: if ( -d 'potato' ) { _:>
	<li><a href="potato/">Debian ? (potato)</a> (versionsnummer ännu
	ej tilldelat)
<: } :>
</ul>


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-indent-data:nil
sgml-doctype:".doctype"
End:
-->
