#use wml::debian::template title="Avbeställa sändlistor" GEN_TIME="yes"

<p>Se <a href="./#subunsub">sändlistesidan</a> för information om hur du
avbeställer via e-post.
Det finns även en
<a href="unsubscribe">webbisda för att prenumerera</a> på sändlistorna.

<hr>

<p>Välj de listor du vill avbeställa:

<FORM METHOD=POST ACTION="http://cgi.debian.org/cgi-bin/subscribe.pl">

<h3><a name="user">Användarlistor</a></h3>

<perl>
 my $copy = 0;
 open ENGLISH, "$(ENGLISHDIR)/MailingLists/unsubscribe.wml"
  or die "Can't open $(ENGLISHDIR)/MailingLists/unsubscribe.wml";
 LINE: while (<ENGLISH>)
 {
  # Keep count of where we are
  $copy ++ if m(^</?table>$);

  # If this is in a list, just print it (with some minor changes)
  if ($copy == 1 || $copy == 4 || $copy == 7 || $copy == 10 ||
      $copy == 13|| $copy == 16 || m(</table>))
  {
   print $_;
  }
  elsif ($copy == 2)
  {
   print '<h3><a name="devel">Utvecklarlistor</a></h3>', "\n";
   $copy = 3;
  }
  elsif ($copy == 5)
  {
   print '<h3><a name="int">Landsanpassning och översättning</a></h3>', "\n";
   $copy = 6;
  }
  elsif ($copy == 8)
  {
   print '<h3><a name="ports">Anpassningar till icke-i386-arkitekturer</a></h3>', "\n";
   $copy = 9;
  }
  elsif ($copy == 11)
  {
   print '<h3><a name="bugs">Felrapporteringssystemet</a></h3>', "\n";
   $copy = 12;
  }
  elsif ($copy == 14)
  {
   print '<h3><a name="foreign">Listor vi handhar för andra projekt</a></h3>', "\n";
   $copy = 15;
  }
 }
</perl>

</table>
<HR><STRONG>Din e-postadress:</STRONG>
<BR><INPUT TYPE="text" NAME="user_email" SIZE="60"><BR>
<INPUT TYPE="SUBMIT" value="Sänd"> <INPUT TYPE="RESET" value="Återställ">
</FORM>

<p>Vänligen respektera
<a href="./#ads">Debians annonseringspolicy för sändlistorna</a>.
