#use wml::debian::translation-check translation="1.2"
<define-tag description>virheelliset lukuoikeudet</define-tag>
<define-tag moreinfo>
<p>mantis, PHP:ll‰ ja MySQL:ll‰ toteutettu www-pohjainen
vianhallintaj‰rjestelm‰, s‰ilytt‰‰ tietokantaansa p‰‰syyn k‰ytett‰v‰n
salasanan asetustiedostossa, joka on kaikkien luettavissa.  Paikallisen
hyˆkk‰‰j‰n on n‰in mahdollista lukea salasana ja saada luku/kirjoitusoikeudet
tietokantaan.</p>

<p>Ongelma on korjattu vakaan jakelun (woody) versiossa 0.17.1-3 .</p>

<p>Aiempi vakaa jakelu (potato) ei sis‰ll‰ mantis-pakettia.</p>

<p>Ongelma on korjattu ep‰vakaan jakelun (sid) versiossa 0.17.5-6 .</p>

<p>Suosittelemme p‰ivitt‰m‰‰n mantis-paketin.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-335.data"
