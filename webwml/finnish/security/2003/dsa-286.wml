#use wml::debian::translation-check translation="1.2"
<define-tag description>ep‰luotettava v‰liaikaistiedosto</define-tag>
<define-tag moreinfo>
<p>Paul Szabo havaitsi ep‰luotettavan v‰liaikaistiedoston luontitavan
ps2epsi:ss‰, komentosarjassa jota levitet‰‰n osana gs-common-pakettia (joka
sis‰lt‰‰ eri Ghostscript-versioiden yhteiset tiedostot).  ps2epsi k‰ytt‰‰
v‰liaikaistiedostoa kutsuessaan ghostscripti‰.  T‰m‰ tiedosto luotiin
ep‰luotettavalla tavalla, jonka johdosta paikallisen hyˆkk‰‰j‰n saattoi olla
mahdollista ylikirjoittaa ps2epsi:‰ kutsuneen k‰ytt‰j‰n omistamia tiedostoja.</p>

<p>Ongelma on korjattu vakaan jakelun (woody) versiossa 0.3.3.0woody1 .</p>

<p>Aiempi vakaa jakelu (potato) ei ole altis t‰lle ongelmalle.</p>

<p>Ongelma on korjattu ep‰vakaan jakelun (sid) versiossa 0.3.3.1 .</p>

<p>Suosittelemme p‰ivitt‰m‰‰n gs-common-paketin.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-286.data"
