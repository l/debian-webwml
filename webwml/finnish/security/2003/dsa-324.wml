#use wml::debian::translation-check translation="1.6"
<define-tag description>useita haavoittuvuuksia</define-tag>
<define-tag moreinfo>
<p>Useat etherealin pakettianalysoijien merkkijonon k‰sittelyrutiinit
sis‰lt‰v‰t vikoja joita voidaan hyv‰ksik‰ytt‰‰ pahantahtoisesti muotoillun
paketin avulla ja saadaan ethereal kuluttamaan liiallisia m‰‰ri‰ muistia,
kaatumaan, tai suorittamaan mielivaltaista koodia.</p>

<p>Kyseiset haavoittuvuudet ilmoitettiin seuraavassa
Ethereal-tietoturvatiedotteessa:</p>

<p><url "http://www.ethereal.com/appnotes/enpa-sa-00010.html"></p>

<p>Debian 3.0:n (woody) ethereal-paketin versio 0.9.4 on altis useimmille
t‰ss‰ tiedotteessa kuvailluille ongelmille, mukaanluettuna:</p>

<ul>
<li>DCERPC-analysoija saattoi yritt‰‰ varata liian paljon muistia
yritt‰ess‰‰n dekoodata NDR-merkkijonoa
<li>V‰‰r‰t IPv4- tai IPv6-etuliitteiden pituudet saattoivat aiheuttaa
ylivuodon OSI-analysoijassa
<li>tvb_get_nstringz0()-rutiini k‰sitteli virheellisesti nollapituisen
puskurin koon
<li>BGP-, WTP-, DNS-, 802.11-, ISAKMP-, WSP-, CLNP- and ISIS-analysoijat
k‰sitteliv‰t merkkijonoja virheellisesti
</ul>

<p>Seuraavat ongelmat <strong>eiv‰t</strong> vaikuta t‰h‰n versioon:</p>

<ul>
<li> SPNEGO-analysoija saattoi segfaultata j‰sennelless‰‰n virheellist‰
ASN.1-arvoa
<li> RMI-analysoija k‰sitteli merkkijonoja virheellisesti koska kyseiset
moduulit eiv‰t ole k‰ytˆss‰.
</ul>

<p>N‰m‰ ongelmat on korjattu vakaan jakelun (woody) versiossa 0.9.4-1woody5 .</p>

<p>N‰iden ongelmien korjauksista aiemmalle vakaalle jakelulle (potato)
ilmoitetaan myˆhemmin ilmestyv‰ss‰ tiedotteessa.</p>

<p>N‰m‰ ongelmat on korjattu ep‰vakaan jakelun (sid) versiossa 0.9.13-1 .</p>

<p>Suosittelemme p‰ivitt‰m‰‰n ethereal-paketin.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-324.data"
