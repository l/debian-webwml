#use wml::debian::translation-check translation="1.1"
<define-tag description>ep‰luotettava ohjelman suoritus</define-tag>
<define-tag moreinfo>
<p>KDE-ryhm‰ havaitsi haavoittuvuuden tavassa, jolla KDE k‰ytt‰‰
Ghostscript-ohjelmistoa k‰sitelless‰‰n PostScript (PS)- ja PDF-tiedostoja.
Hyˆkk‰‰j‰ voi toimittaa pahantahtoisen PostScript- tai PDF-tiedoston
s‰hkˆpostitse tai www-sivun kautta, mik‰ saattaa johtaa mielivaltaisten
komentojen suorittamiseen niill‰ k‰ytt‰j‰oikeuksilla joilla tiedosto avataan,
tai kun selain luo hakemistoluettelon, jossa k‰ytet‰‰n pienoiskuvia.</p>

<p>Ongelma on korjattu vakaan jakelun (woody) kdebase:n ja siihen
liittyvien pakettien versiossa 2.2.2-14.4 .</p>

<p>Aiempi vakaa jakelu (potato) ei ole altis t‰lle ongelmalle sill‰ se
ei sis‰ll‰ KDE:t‰.</p>

<p>Korjaus ep‰vakaalle jakelulle (sid) ilmestyy piakkoin.</p>

<p>Ongelma on korjattu Ralf Noldenin ep‰virallisen takautuvan KDE 3.1.1:n
woody-siirroksen (download.kde.org) kdebase:n versiossa 3.1.1-0woody3 .
P‰ivitys onnistuu k‰ytt‰m‰ll‰ normaalia backport-rivi‰ apt-get:issa :</p>

<p>  deb http://download.kde.org/stable/latest/Debian stable main</p>

<p>Suosittelemme p‰ivitt‰m‰‰n kdebase:n ja siihen liittyv‰t paketit.</p>
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2003/dsa-296.data"
