<define-tag description>tiedoston loppuun lis‰‰minen/uuden luominen et‰koneelta</define-tag>
<define-tag moreinfo>
Michal Zalewski huomasi, ett‰ Samba ei kunnolla tarkista NetBIOS-nimi‰
et‰koneille.

<p>T‰m‰ itsess‰‰n ei ole ongelma paitsi jos Samba on asetettu
kirjoittamaan lokitiedostot tiedostoihin, joiden nimi sis‰lt‰‰
et‰koneen NetBIOS-nimen k‰ytt‰en "%m"-makroa "log file"-asetuksessa.
T‰llaisessa tapauksessa hyˆkk‰‰j‰ voi k‰ytt‰‰ NetBIOS-nime‰ kuten
"../tmp/evil".  Jos lokitiedosto on asetettu "/var/log/samba/%m":ksi,
Samba sallii kirjoituksen tiedostoon /var/tmp/evil.

<p>Koska NetBIOS-nimet on rajoitettu 15 merkkiin ja "log file"-asetus
lis‰‰ p‰‰tteen tiedostonimeen t‰m‰n vaikutukset ovat rajoitetut.
Kuitenkin hyˆkk‰‰j‰ voi luoda symbolisia linkkej‰ Samba-palvelimelle
joilla h‰n huijaa Samban lis‰‰m‰‰n mit‰ tahansa haluamaansa dataa
mihin tahansa tiedostoon levyll‰, johon Samballa on oikeudet
kirjoittaa.

<p>Debian GNU/Linux-paketin versiolla Sambasta on turvalliset
asetukset ja ei siis ole alttiina t‰lle.

<p>V‰liaikainen ongelman kiert‰minen j‰rjestelmille, jotka ovat
alttiina on vaihtaa kaikki "%m"-makron esiintym‰t
<code>smb.conf</code>:ssa "%l":ksi ja uudelleenk‰ynnist‰‰ Samba.

<p>T‰m‰ on korjattu versiossa 2.0.7-3.4 ja suosittelemme ett‰ p‰ivit‰t
Samba-pakettisi mahdollisimman pian.
</define-tag>

# do not modify the following line
#include "$(ENGLISHDIR)/security/2001/dsa-065.data"
#use wml::debian::translation-check translation="1.3" mindelta="1"
