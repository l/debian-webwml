#! /bin/bash

. `dirname $0`/../config.sh

test -d ${archivedir} || mkdir -p ${archivedir}
cd ${archivedir}

# scp -q auric:/org/ftp.debian.org/ftp/indices/Maintainers .

ln -s /home/org/ftp.root/debian/indices/Maintainers .
