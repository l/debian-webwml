#! /bin/bash

. `dirname $0`/../config.sh

test -d ${filesdir} || mkdir -p ${filesdir}

for dist in ${dists} experimental
do
   grep -h "^Section" ${filesdir}/archive/${dist}_*_Packages | perl -pe 's/^Section: //' | sort | uniq > ${filesdir}/sections.${dist}
done
