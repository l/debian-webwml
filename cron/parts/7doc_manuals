#!/bin/bash -e

# This file updates the doc/manuals/ directory of the web pages
# with the current CVS stuff

. `dirname $0`/../common.sh

echo "Running the DDP CVS update+build..."
$webtopdir/ddp/cvsbuild

echo
